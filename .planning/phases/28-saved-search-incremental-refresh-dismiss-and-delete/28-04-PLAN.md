---
phase: 28-saved-search-incremental-refresh-dismiss-and-delete
plan: 04
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - src/components/layout/nav-items.tsx
  - src/components/layout/mobile-bottom-nav.tsx
  - src/app/[orgId]/personas/components/persona-card.tsx
  - src/app/[orgId]/personas/components/persona-form-dialog.tsx
  - src/app/[orgId]/personas/components/persona-list.tsx
  - src/app/[orgId]/personas/components/personas-layout.tsx
  - src/app/[orgId]/personas/components/personas-library-sidebar.tsx
  - src/app/[orgId]/personas/page.tsx
  - src/app/[orgId]/search/components/persona-pills.tsx
  - src/app/[orgId]/search/components/search-content.tsx
  - src/app/[orgId]/page.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "No user-facing text says 'Persona' — all say 'Saved Search' instead"
    - "Database tables, API routes, TypeScript type names, internal variable names are UNCHANGED"
    - "Navigation says 'Saved Searches' instead of 'Personas'"
    - "Build passes with zero errors"
  artifacts:
    - path: "src/components/layout/nav-items.tsx"
      provides: "Sidebar nav with 'Saved Searches' label"
      contains: "Saved Searches"
    - path: "src/components/layout/mobile-bottom-nav.tsx"
      provides: "Mobile nav with 'Searches' label"
      contains: "Searches"
  key_links: []
---

<objective>
Rename all user-facing "Persona" strings to "Saved Search" throughout the UI. Then run a full build verification to ensure the entire phase compiles cleanly.

Purpose: The user decided to rename the concept from "Persona" to "Saved Search" in user-facing text only. This is the final polish step.
Output: All user-facing strings updated. Build verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-saved-search-incremental-refresh-dismiss-and-delete/28-CONTEXT.md

<interfaces>
<!-- Files containing user-facing "Persona" strings -->

From src/components/layout/nav-items.tsx (line 33):
```typescript
{ label: "Personas", href: "/personas", icon: Users, exact: false },
```

From src/components/layout/mobile-bottom-nav.tsx (line 44):
```typescript
{ label: "Personas", icon: Users, href: `/${orgId}/personas`, exact: false },
```

Files to grep for user-facing "Persona" strings:
- src/app/[orgId]/personas/ (page titles, headings, descriptions, button labels)
- src/app/[orgId]/search/components/ (persona-pills labels, search-content text)
- src/app/[orgId]/page.tsx (dashboard may reference "personas")
- src/components/layout/ (nav items)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Persona to Saved Search in all user-facing strings</name>
  <files>src/components/layout/nav-items.tsx, src/components/layout/mobile-bottom-nav.tsx, src/app/[orgId]/personas/components/persona-card.tsx, src/app/[orgId]/personas/components/persona-form-dialog.tsx, src/app/[orgId]/personas/components/persona-list.tsx, src/app/[orgId]/personas/components/personas-layout.tsx, src/app/[orgId]/personas/components/personas-library-sidebar.tsx, src/app/[orgId]/personas/page.tsx, src/app/[orgId]/search/components/persona-pills.tsx, src/app/[orgId]/search/components/search-content.tsx, src/app/[orgId]/page.tsx</files>
  <read_first>
    - src/components/layout/nav-items.tsx (nav label)
    - src/components/layout/mobile-bottom-nav.tsx (mobile nav label)
    - src/app/[orgId]/personas/page.tsx (page title)
    - src/app/[orgId]/personas/components/persona-card.tsx (card labels)
    - src/app/[orgId]/personas/components/persona-form-dialog.tsx (dialog title, button labels)
    - src/app/[orgId]/personas/components/persona-list.tsx (list labels)
    - src/app/[orgId]/personas/components/personas-layout.tsx (layout heading)
    - src/app/[orgId]/personas/components/personas-library-sidebar.tsx (sidebar heading)
    - src/app/[orgId]/search/components/persona-pills.tsx (pill labels)
    - src/app/[orgId]/search/components/search-content.tsx (button labels, empty states)
    - src/app/[orgId]/page.tsx (dashboard references)
  </read_first>
  <action>
Find and replace ONLY user-facing text strings (NOT code identifiers). The exact replacements:

**Navigation:**
- `nav-items.tsx` line 33: `"Personas"` -> `"Saved Searches"`
- `mobile-bottom-nav.tsx` line 44: `"Personas"` -> `"Searches"` (shorter for mobile)

**Persona page components** — search for string literals in JSX text content, `title`, `description`, `placeholder`, `label`, `aria-label` attributes:
- `"Personas"` (heading) -> `"Saved Searches"`
- `"Persona"` (singular) -> `"Saved Search"`
- `"New Persona"` -> `"New Search"`
- `"Create Persona"` -> `"Save Search"`
- `"Edit Persona"` -> `"Edit Search"`
- `"Delete Persona"` -> `"Delete Search"`
- `"persona"` (lowercase in descriptions/tooltips/helper text) -> `"saved search"`
- `"personas"` (lowercase in descriptions) -> `"saved searches"`
- `"No personas"` -> `"No saved searches"`

**search-content.tsx:**
- The "New Persona" button label (around line 462): -> `"New Search"`
- Any "select a saved persona" text -> `"select a saved search"`
- Empty state descriptions referencing "persona" -> "saved search"

**Dashboard (page.tsx):**
- Any "personas" in UI text -> "saved searches"

**DO NOT RENAME:**
- Component names (PersonaCard, PersonaPills, PersonaFormDialog, etc.)
- Import paths (`@/lib/personas/types`)
- Variable names (`personas`, `selectedPersona`)
- Prop names (`personas: Persona[]`)
- CSS class names
- File names
- Database table names (`personas`)
- API route paths (`/api/personas/...` if any)
- TypeScript type names (`Persona`, `PersonaFilters`)

**Method:** For each file listed:
1. Read the full file
2. Identify all JSX string literals (text nodes, title props, description strings, aria-labels, placeholders)
3. Replace only those that are user-facing text
4. Leave all code identifiers untouched
5. Write the updated file

After all renames, run a grep to verify no straggler user-facing "Persona" strings remain:
```bash
grep -rn '"Persona\b' src/ --include="*.tsx" --include="*.ts" | grep -v "import\|type\|interface\|const\|let\|var\|function\|export\|Persona[A-Z]\|PersonaF\|\.persona"
```
Any remaining hits should be code identifiers (type names, component names), not user-facing text.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - nav-items.tsx contains `"Saved Searches"` (NOT "Personas") for the label
    - mobile-bottom-nav.tsx contains `"Searches"` (NOT "Personas") for the label
    - No JSX text content in any .tsx file under src/app/[orgId]/personas/ contains the word "Persona" in user-facing context
    - search-content.tsx contains "New Search" (NOT "New Persona") for the create button
    - All component names (PersonaCard, PersonaPills, etc.) are UNCHANGED
    - All TypeScript type names (Persona, PersonaFilters, etc.) are UNCHANGED
    - All import paths are UNCHANGED
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All user-facing strings renamed from Persona/Personas to Saved Search/Saved Searches. No code identifiers changed. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification</name>
  <files></files>
  <read_first></read_first>
  <action>
Run full build verification:

1. `npx tsc --noEmit` — TypeScript type check
2. `pnpm build --no-lint` — Next.js production build (skip lint to focus on compilation)

If either fails:
- Read the error output
- Fix the issue in the failing file
- Re-run until both pass

Then verify the rename is complete:
```bash
# Should return ZERO user-facing "Persona" strings (only code identifiers)
grep -rn '"Persona"' src/ --include="*.tsx" | grep -v "import\|type\|interface\|Component\|FormDialog\|Pills\|Card\|List\|Sparkline\|Selector\|Grid\|Layout\|Sidebar\|Stream"
```

Also verify no `org_id` leaked into any new files:
```bash
grep -rn 'org_id' src/lib/personas/refresh.ts src/app/api/search/ supabase/migrations/20260405* 2>/dev/null
```
Should return zero results (all should use `tenant_id`).
  </action>
  <verify>
    <automated>pnpm build --no-lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `pnpm build --no-lint` exits 0
    - grep for user-facing "Persona" returns zero results
    - grep for `org_id` in new files returns zero results
    - All new files use `tenant_id` exclusively
  </acceptance_criteria>
  <done>Full build passes. No TypeScript errors. No user-facing "Persona" strings remain. No org_id usage in new files. Phase 28 code is complete and ready for manual QA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | String renames and build verification — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Tampering | nav-items.tsx | accept | Only label strings changed; no href or routing logic modified; no security impact |
</threat_model>

<verification>
- Full build (`pnpm build --no-lint`) exits 0
- TypeScript type check (`npx tsc --noEmit`) exits 0
- No user-facing "Persona" text remains
- No `org_id` in any Phase 28 files
</verification>

<success_criteria>
- All user-facing text says "Saved Search" / "Saved Searches" instead of "Persona" / "Personas"
- Zero code identifier changes (types, components, variables, imports unchanged)
- Production build compiles successfully
- No tenant_id/org_id naming mistakes
</success_criteria>

<output>
After completion, create `.planning/phases/28-saved-search-incremental-refresh-dismiss-and-delete/28-04-SUMMARY.md`
</output>
