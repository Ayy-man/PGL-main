---
phase: 05-ui-revamp
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/[orgId]/lists/page.tsx
  - src/app/[orgId]/page.tsx
  - src/app/[orgId]/dashboard/analytics/page.tsx
  - src/app/admin/page.tsx
  - src/app/[orgId]/personas/page.tsx
autonomous: true
requirements:
  - LIST-02
  - LIST-06
  - ANLY-02
  - ANLY-05
  - SA-01
must_haves:
  truths:
    - "Lists page renders full-width horizontal cards (not a grid) with Cormorant list names and gold member counts"
    - "Dashboard page uses Cormorant greeting heading and persona cards as primary content"
    - "Analytics page stat cards use Cormorant 36px values with gold for non-zero data"
    - "Admin dashboard stat cards use Cormorant 36px values matching analytics treatment"
    - "All pages use design system tokens (no raw zinc-*, gray-*, etc.)"
  artifacts:
    - path: "src/app/[orgId]/lists/page.tsx"
      provides: "Full-width horizontal list cards with Cormorant names"
      contains: "font-serif"
    - path: "src/app/[orgId]/page.tsx"
      provides: "Dashboard with Cormorant greeting and persona card content"
      contains: "font-serif"
    - path: "src/app/[orgId]/dashboard/analytics/page.tsx"
      provides: "Analytics stat cards with Cormorant values"
      contains: "font-serif"
  key_links:
    - from: "src/app/[orgId]/lists/page.tsx"
      to: "src/components/ui/button.tsx"
      via: "Create List button uses gold variant"
      pattern: "variant.*gold"
    - from: "src/app/[orgId]/page.tsx"
      to: "src/app/[orgId]/search/components/persona-card.tsx"
      via: "Dashboard reuses persona cards from search page"
      pattern: "PersonaCard"
---

<objective>
Apply design system to Lists, Dashboard, Analytics, Admin Dashboard, and Personas pages.

Purpose: These five pages are the remaining tenant-facing surfaces. Each has page-specific overrides documented in design-system/pages/ that deviate from the MASTER.md baseline.

Output: Five page files updated with design system typography, surface treatment, and layout per their respective page override specs.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@design-system/MASTER.md
@design-system/pages/dashboard.md
@design-system/pages/lists.md
@src/app/[orgId]/lists/page.tsx
@src/app/[orgId]/page.tsx
@src/app/[orgId]/dashboard/analytics/page.tsx
@src/app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Lists page with horizontal card layout and Cormorant typography</name>
  <files>src/app/[orgId]/lists/page.tsx</files>
  <action>
Per lists.md page override:

**Page header:**
- Title: `font-serif text-3xl font-bold text-foreground` (standard MASTER h1)
- Subtitle: `text-sm text-muted-foreground` with dynamic count (e.g. "3 lists")
- CTA: "Create List" button with `variant="gold"`
- Layout: `flex items-center justify-between`

**List items — horizontal full-width cards:**
Replace existing grid layout with vertical stack: `flex flex-col gap-3`

Each list card:
- Container: `flex items-center justify-between p-6 px-7 rounded-xl transition-colors cursor-pointer` with `style={{ background: "var(--bg-card-gradient)", border: "1px solid rgba(255,255,255,0.04)" }}`
- Hover: swap background to `var(--bg-card-hover)` and border to `var(--border-hover)`

Left side:
- List name: `font-serif text-[20px] font-semibold text-foreground`
- Description: `text-[13px] text-muted-foreground mt-0.5`

Right side (`flex items-center gap-6`):
- Member count block (vertical stack, centered):
  - Count: `font-serif text-[22px] font-bold text-[var(--gold-primary)]`
  - Label: `text-xs text-muted-foreground`
- Updated date: `text-xs text-muted-foreground`
- Export button: `variant="ghost"` with `text-muted-foreground hover:text-foreground`
- View button: `variant="ghost"` with `text-[var(--gold-primary)] hover:text-[var(--gold-muted)]`, text: "View →"

**Empty state:** Use shared EmptyState component with List icon, "No lists yet", and gold "Create List" CTA button.
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/Phronesis-main" && grep -q "font-serif" "src/app/[orgId]/lists/page.tsx" && grep -q "gold" "src/app/[orgId]/lists/page.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Lists page renders full-width horizontal cards with Cormorant list names (20px/600), gold member counts (22px/700), and gold "Create List" CTA. No grid layout — pure vertical stack.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard, Analytics, Admin Dashboard, and Personas pages</name>
  <files>src/app/[orgId]/page.tsx, src/app/[orgId]/dashboard/analytics/page.tsx, src/app/admin/page.tsx, src/app/[orgId]/personas/page.tsx</files>
  <action>
**Dashboard (src/app/[orgId]/page.tsx):**

Per dashboard.md:
- Greeting heading: `font-serif text-[38px] font-semibold text-foreground` — "Good {morning/afternoon/evening}, {userName}"
- Date subtitle: `text-sm text-muted-foreground` below greeting
- Body: Remove any action cards grid. Instead, render persona cards as primary content (reuse PersonaCard component from Plan 04). Import from `@/app/[orgId]/search/components/persona-card` or create a shared export.
- If stats available: single horizontal row of muted stat pills above persona cards, NOT full stat cards. Each pill: `inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-xs text-muted-foreground` with `style={{ background: "var(--bg-elevated)", border: "1px solid var(--border-subtle)" }}`

**Analytics (src/app/[orgId]/dashboard/analytics/page.tsx):**

Per dashboard.md "Analytics Page":
- Period toggle: Segmented control with gold active state: `bg-[var(--gold-bg)] text-[var(--gold-primary)] rounded-sm shadow-sm`
- Stat cards (4-column grid): `grid grid-cols-2 lg:grid-cols-4 gap-4`
  - Value: `font-serif text-[36px] font-bold` — gold (`text-[var(--gold-primary)]`) for non-zero, muted for zero
  - Label: `text-xs font-semibold uppercase tracking-wider text-muted-foreground`
- Chart containers: `rounded-[14px] p-7` with `style={{ background: "var(--bg-card-gradient)", border: "1px solid var(--border-subtle)" }}`
- Replace any hardcoded hex colors in charts with CSS variable references where possible

**Admin Dashboard (src/app/admin/page.tsx):**

Per dashboard.md "Admin Dashboard":
- Stat cards: Same Cormorant 36px treatment as analytics
  - Values: `font-serif text-[36px] font-bold`
  - Gold for active data, muted for zero
- Replace any raw `zinc-*`, `gray-*`, `emerald-*`, `yellow-*`, `green-*`, `red-*`, `blue-*`, `orange-*` classes with design system tokens:
  - `text-zinc-100/300` -> `text-foreground`
  - `text-zinc-400/500` -> `text-muted-foreground`
  - `bg-zinc-900/950` -> use surface-card or bg-card
  - `border-zinc-800` -> `border`
  - `text-green-*` -> `text-success`
  - `text-red-*` -> `text-destructive`
  - `text-blue-*` -> `text-info`
  - `text-orange-*` -> `text-warning`
- "Coming Soon" badge: `inline-flex items-center rounded-full px-3 py-0.5 text-xs font-semibold bg-[var(--gold-bg-strong)] text-[var(--gold-primary)] border border-[var(--border-gold)]`

**Personas page (src/app/[orgId]/personas/page.tsx):**

Apply design system tokens:
- Page title: `font-serif text-3xl font-bold text-foreground`
- Update any raw color classes to token classes
- Persona list items: Use surface-card treatment (if not already using Card component)
- "Create Persona" button: `variant="gold"`
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/Phronesis-main" && grep -q "font-serif" "src/app/[orgId]/page.tsx" && grep -q "font-serif" "src/app/[orgId]/dashboard/analytics/page.tsx" && grep -q "font-serif" "src/app/admin/page.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Dashboard shows Cormorant greeting + persona cards. Analytics uses Cormorant 36px stat values with gold active state. Admin dashboard uses same stat treatment with raw color classes replaced by tokens. Personas page uses design system tokens and gold CTA.</done>
</task>

</tasks>

<verification>
1. Lists page renders horizontal cards, not a grid
2. Dashboard shows Cormorant greeting and persona card content
3. Analytics stat values are Cormorant 36px with gold for non-zero
4. Admin dashboard has no raw zinc-*/gray-*/emerald-* classes
5. All pages use surface-card or gradient backgrounds for cards
6. `npx next build` compiles without errors
</verification>

<success_criteria>
- Lists: Full-width horizontal cards, Cormorant list names (20px/600), gold member counts, gold CTA
- Dashboard: Cormorant 38px greeting, persona cards as body, stat pills for metrics
- Analytics: Cormorant 36px stat values, gold active period toggle, gradient chart containers
- Admin: Cormorant 36px stats, all raw colors replaced with tokens, Coming Soon gold badge
- Personas: Design system tokens applied, gold Create Persona CTA
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-revamp-apply-design-system-dark-luxury-aesthetic-gold-accents-layered-glass-surfaces-cormorant-dm-sans-typography-css-variable-token-system/05-05-SUMMARY.md`
</output>
