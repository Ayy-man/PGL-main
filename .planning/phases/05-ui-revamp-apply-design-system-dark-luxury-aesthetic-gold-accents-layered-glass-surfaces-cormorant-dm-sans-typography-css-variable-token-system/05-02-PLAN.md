---
phase: 05-ui-revamp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/empty-state.tsx
  - src/components/ui/dialog.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
must_haves:
  truths:
    - "Button component has ghost and gold variants via CVA"
    - "Card component uses surface-card utility class or gradient background, 14px border radius"
    - "Badge component uses 20px border radius (rounded-full) with semantic color variants"
    - "EmptyState component has 64px gold-tinted icon circle and Cormorant heading"
    - "Dialog component uses gradient background, Cormorant title, heavy shadow"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Ghost and Gold button variants"
      contains: "gold"
    - path: "src/components/ui/card.tsx"
      provides: "Gradient surface card with 14px radius"
      contains: "14px"
    - path: "src/components/ui/badge.tsx"
      provides: "Semantic badge variants with 20px radius"
      contains: "rounded-full"
    - path: "src/components/ui/empty-state.tsx"
      provides: "Gold icon circle and Cormorant heading"
      contains: "font-serif"
    - path: "src/components/ui/dialog.tsx"
      provides: "Gradient background dialog with Cormorant title"
      contains: "font-serif"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/app/globals.css"
      via: "Gold variant references gold CSS variables"
      pattern: "gold"
---

<objective>
Update shared UI primitives (Button, Card, Badge, EmptyState, Dialog) to match the design system spec.

Purpose: These components are used across every page. Updating them first means all pages automatically inherit the correct surface treatment, button styling, and badge appearance — reducing per-page work in later plans.

Output: Five updated shadcn component files with design system-compliant styling.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@design-system/MASTER.md
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/empty-state.tsx
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Button, Card, and Badge components to match design system</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx</files>
  <action>
**Button (src/components/ui/button.tsx):**

Add two new variants to the CVA `variants.variant` object:

- `gold`: Background is a gold gradient, gold border, gold text. Classes:
  ```
  "border border-[rgba(212,175,55,0.25)] text-[var(--gold-primary)] font-semibold transition-all duration-200 cursor-pointer hover:opacity-90"
  ```
  Use inline `style` for the gradient background since Tailwind v3 cannot apply gradient CSS variables via bg- classes. Add a wrapper approach: use `bg-[rgba(212,175,55,0.08)]` as a fallback and a gold-tinted background.

  Simplified gold variant class string:
  ```
  "bg-[rgba(212,175,55,0.1)] border border-[rgba(212,175,55,0.25)] text-[#d4af37] font-semibold hover:bg-[rgba(212,175,55,0.18)] transition-all duration-200 cursor-pointer"
  ```

- Update the existing `ghost` variant (if it exists) or add it:
  ```
  "bg-[rgba(255,255,255,0.03)] border border-[rgba(255,255,255,0.08)] text-foreground/60 hover:border-[rgba(255,255,255,0.15)] hover:text-foreground transition-all duration-200 cursor-pointer"
  ```

Also update the `size` variants to include a `sm` size:
```
sm: "h-8 rounded-[8px] px-3.5 text-xs"
```

Ensure ALL button variants include `cursor-pointer` and `transition-all duration-200`. Update border radius for `default` size to `rounded-[8px]` per spec.

**Card (src/components/ui/card.tsx):**

Update the `Card` component base classes:
- Add `surface-card` utility class (from globals.css, Plan 01)
- Update border radius from `rounded-lg` (or `rounded-xl`) to `rounded-[14px]`
- The `surface-card` class provides gradient background, subtle border, and hover transition automatically
- Keep the existing CardHeader, CardContent, CardFooter, CardTitle, CardDescription subcomponents
- Update CardTitle to use `font-serif` class for Cormorant headings per spec

**Badge (src/components/ui/badge.tsx):**

Update the badge component:
- Change border radius from existing to `rounded-[20px]` (or `rounded-full`)
- Add semantic variants to CVA if not present:
  - `success`: `bg-[rgba(34,197,94,0.15)] text-[#22c55e] border-[rgba(34,197,94,0.3)]`
  - `warning`: `bg-[rgba(245,158,11,0.15)] text-[#f59e0b] border-[rgba(245,158,11,0.3)]`
  - `info`: `bg-[rgba(96,165,250,0.15)] text-[#60a5fa] border-[rgba(96,165,250,0.3)]`
  - `gold`: `bg-[rgba(212,175,55,0.15)] text-[#d4af37] border-[rgba(212,175,55,0.3)]`
- Keep existing `default`, `secondary`, `destructive`, `outline` variants
- Ensure base classes include `inline-flex items-center rounded-[20px] px-2.5 py-0.5 text-xs font-semibold`
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/Phronesis-main" && grep -q "gold" src/components/ui/button.tsx && grep -q "surface-card\|14px" src/components/ui/card.tsx && grep -q "20px\|rounded-full" src/components/ui/badge.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Button has ghost and gold variants with 8px radius. Card uses surface-card gradient with 14px radius. Badge uses 20px radius with semantic color variants (success, warning, info, gold).</done>
</task>

<task type="auto">
  <name>Task 2: Update EmptyState and Dialog components to match design system</name>
  <files>src/components/ui/empty-state.tsx, src/components/ui/dialog.tsx</files>
  <action>
**EmptyState (src/components/ui/empty-state.tsx):**

Update the empty state component per MASTER.md Section 9 "Empty States":
- Container: Add `surface-card` class, ensure 80px vertical padding (`py-20`)
- Icon container: 64px gold-tinted circle. Use:
  ```tsx
  <div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full" style={{ background: "var(--gold-bg)" }}>
    <Icon className="h-8 w-8" style={{ color: "var(--gold-muted)" }} />
  </div>
  ```
- Heading: `font-serif text-[22px] font-semibold text-foreground` (Cormorant Garamond)
- Body: `text-[13px] text-muted-foreground max-w-[400px] mx-auto text-center`
- CTA slot: Children rendered below body text

**Dialog (src/components/ui/dialog.tsx):**

Update the shadcn Dialog component overrides:
- DialogContent: Add gradient background via `surface-card` class OR inline style `background: "var(--bg-card-gradient)"`. Set border radius to `rounded-[14px]`. Ensure heavy shadow class.
- DialogTitle: Add `font-serif text-[22px] font-semibold` for Cormorant heading
- DialogClose (if X button exists): Top-right position, add `sr-only` label
- Keep all existing Radix Dialog primitives unchanged (focus trap, keyboard handling, ARIA)
- Only modify className strings, not component logic
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/Phronesis-main" && grep -q "font-serif" src/components/ui/empty-state.tsx && grep -q "font-serif\|14px" src/components/ui/dialog.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>EmptyState has 64px gold icon circle and Cormorant heading. Dialog uses gradient background, 14px radius, and Cormorant title. Both match design system spec.</done>
</task>

</tasks>

<verification>
1. `grep gold src/components/ui/button.tsx` returns variant definition
2. `grep surface-card src/components/ui/card.tsx` returns class usage
3. `grep rounded-full src/components/ui/badge.tsx` returns badge radius
4. `grep font-serif src/components/ui/empty-state.tsx` returns heading class
5. `npx next build` compiles without errors
</verification>

<success_criteria>
- Button component exports ghost and gold variants with 8px radius and cursor-pointer
- Card component uses gradient surface treatment with 14px radius
- Badge uses 20px fully-rounded radius with semantic color variants
- EmptyState uses 64px gold icon circle and Cormorant heading
- Dialog uses gradient background and Cormorant title
- All components remain backward-compatible (existing usage doesn't break)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-revamp-apply-design-system-dark-luxury-aesthetic-gold-accents-layered-glass-surfaces-cormorant-dm-sans-typography-css-variable-token-system/05-02-SUMMARY.md`
</output>
