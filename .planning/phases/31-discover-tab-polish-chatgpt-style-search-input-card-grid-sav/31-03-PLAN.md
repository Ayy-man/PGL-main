---
phase: 31-discover-tab-polish-chatgpt-style-search-input-card-grid-sav
plan: 03
type: execute
wave: 2
depends_on:
  - "31-01"
  - "31-02"
files_modified:
  - src/app/[orgId]/search/components/search-sidebar-rail.tsx
autonomous: true
requirements:
  - DISC-POLISH-E

must_haves:
  truths:
    - "Active sidebar item has 3px solid var(--gold-primary) left border indicator"
    - "Active item: both expanded and collapsed states show the left border"
    - "Inactive expanded items use font-weight 300 (font-light)"
    - "Collapse/expand behavior is unchanged"
    - "pnpm build exits 0 after all three plans"
  artifacts:
    - path: "src/app/[orgId]/search/components/search-sidebar-rail.tsx"
      provides: "Sidebar with gold left-border active indicator + light inactive weight"
      contains: "3px solid var(--gold-primary)"
  key_links:
    - from: "active item button"
      to: "borderLeft: '3px solid var(--gold-primary)'"
      via: "isActive style conditional"
      pattern: "gold-primary.*borderLeft|borderLeft.*gold-primary"
---

<objective>
Polish the search sidebar rail active state and verify the full phase build.

Purpose: The sidebar currently signals active state only via background color with no left border indicator. A 3px gold left border provides a clear visual anchor. Inactive items shift to font-light for elegance. Then a build verification confirms all three plans compile cleanly.

Output:
- search-sidebar-rail.tsx with gold left-border active indicator + font-light inactive weight
- Confirmed pnpm build exit 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Current sidebar button styles (from file read) -->
// Expanded active:
// { background: "var(--gold-bg)", border: "1px solid var(--border-gold)", color: "var(--gold-primary)" }
// Expanded inactive:
// { background: "transparent", border: "1px solid transparent", color: "var(--text-secondary-ds)" }
// font-medium on the span (text-[13px] font-medium truncate)

// Collapsed active:
// { background: "var(--gold-bg)", borderLeft: "2px solid var(--border-gold)" }
// Collapsed inactive:
// { background: "transparent" }

<!-- Target changes -->
// Expanded active → add borderLeft: "3px solid var(--gold-primary)"
// Expanded inactive span → change font-medium to font-light
// Collapsed active → change borderLeft from 2px var(--border-gold) to 3px var(--gold-primary)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add gold left border to active items + font-light inactive</name>
  <files>src/app/[orgId]/search/components/search-sidebar-rail.tsx</files>
  <read_first>
    - src/app/[orgId]/search/components/search-sidebar-rail.tsx (full file — 143 lines)
  </read_first>
  <action>
Make surgical style changes to search-sidebar-rail.tsx. Do NOT change any logic, props, or structure.

**Change 1 — Collapsed active button** (around line 80-85):
Find:
```typescript
style={
  isActive
    ? {
        background: "var(--gold-bg)",
        borderLeft: "2px solid var(--border-gold)",
      }
    : { background: "transparent" }
}
```
Change to:
```typescript
style={
  isActive
    ? {
        background: "var(--gold-bg)",
        borderLeft: "3px solid var(--gold-primary)",
      }
    : { background: "transparent" }
}
```

**Change 2 — Expanded button active/inactive styles** (around line 103-115):
Find:
```typescript
style={
  isActive
    ? {
        background: "var(--gold-bg)",
        border: "1px solid var(--border-gold)",
        color: "var(--gold-primary)",
      }
    : {
        background: "transparent",
        border: "1px solid transparent",
        color: "var(--text-secondary-ds)",
      }
}
```
Change to:
```typescript
style={
  isActive
    ? {
        background: "var(--gold-bg)",
        border: "1px solid var(--border-gold)",
        borderLeft: "3px solid var(--gold-primary)",
        color: "var(--gold-primary)",
      }
    : {
        background: "transparent",
        border: "1px solid transparent",
        color: "var(--text-secondary-ds)",
      }
}
```

**Change 3 — Expanded inactive item text weight** (around line 129):
Find:
```tsx
<span className="text-[13px] font-medium truncate">{persona.name}</span>
```
Change to:
```tsx
<span className={`text-[13px] truncate ${isActive ? "font-medium" : "font-light"}`}>{persona.name}</span>
```

No other changes. Collapse/expand behavior, Tooltip usage, createButton slots — all unchanged.
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "search-sidebar-rail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "3px solid var(--gold-primary)" src/app/\[orgId\]/search/components/search-sidebar-rail.tsx returns at least 2 matches (collapsed + expanded)
    - grep -n "font-light" src/app/\[orgId\]/search/components/search-sidebar-rail.tsx returns at least 1 match
    - TypeScript check above returns 0 errors on search-sidebar-rail
  </acceptance_criteria>
  <done>Active sidebar items show a 3px gold left border. Inactive expanded items use font-light. Collapsed active state updated to same border. Behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification — pnpm build exit 0</name>
  <files></files>
  <read_first>
    - src/app/[orgId]/search/components/nl-search-bar.tsx (confirm Task 1 plan 01 complete)
    - src/app/[orgId]/search/components/discover-tab.tsx (confirm Task 2 plan 01 complete)
    - src/app/[orgId]/search/components/saved-search-shortcut-list.tsx (confirm plan 02 complete)
    - src/app/[orgId]/search/components/search-sidebar-rail.tsx (confirm Task 1 above complete)
  </read_first>
  <action>
Run pnpm build and verify exit 0. Fix any TypeScript or build errors found in the four modified files.

Common issues to check for:
- `useState` missing from imports in discover-tab.tsx (if not added in plan 01)
- `"use client"` directive present in all four files (they are already client components — must remain)
- `onPrefillSearch` prop type mismatch between discover-tab and saved-search-shortcut-list
- NLSearchBar new `onToggleFilters` and `filtersOpen` props — optional (?) so no required-prop errors in existing callers

If build errors appear:
1. Read the exact TypeScript error message
2. Identify which file and line
3. Apply minimal fix (add missing import, fix type, add `?` to optional prop)
4. Re-run build until exit 0

Do NOT change any business logic while fixing build errors.

Build command:
```bash
cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && pnpm build 2>&1 | tail -30
```
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits 0 (last line includes "Route (app)" table or "Compiled successfully")
    - No TypeScript errors referencing nl-search-bar, discover-tab, saved-search-shortcut-list, or search-sidebar-rail
  </acceptance_criteria>
  <done>pnpm build exits 0. All four modified files compile cleanly. Phase 31 changes are production-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar selection → onSelect | User clicks sidebar item, triggers persona load. Same trust boundary as before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | search-sidebar-rail style changes | accept | Pure visual changes to CSS properties. No logic, no data paths altered. |
</threat_model>

<verification>
Full phase verification:
```bash
cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main"
pnpm build 2>&1 | tail -10
```
Expected: exit 0, route table displayed, no TypeScript errors.
</verification>

<success_criteria>
- Active sidebar items: 3px gold left border (both collapsed and expanded states)
- Inactive expanded items: font-light weight (more elegant)
- Collapse/expand behavior unchanged
- pnpm build exits 0 across all phase 31 changes
- Phase 28 behaviors preserved: dismiss, refresh, NEW badge, bulk actions, slide-over all still functional
</success_criteria>

<output>
After completion, create `.planning/phases/31-discover-tab-polish-chatgpt-style-search-input-card-grid-sav/31-03-SUMMARY.md`

Then update:
- `.planning/ROADMAP.md` — Phase 31 status: COMPLETE, Plans: 3/3 complete, plan list checked
- `.planning/STATE.md` — current position: Phase 31 complete
</output>
