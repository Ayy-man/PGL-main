---
phase: 32-discover-tab-v2-layout-tightening-search-depth-filter-pills-suggested-personas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[orgId]/search/components/discover-tab.tsx
  - src/app/[orgId]/search/components/nl-search-bar.tsx
autonomous: true
requirements:
  - DISC-V2-01  # Heading scale-down ~30%, reduce margin-bottom
  - DISC-V2-02  # Search box depth + gold focus glow + remove Filters chip
  - DISC-V2-06  # Stats bar between heading and search box

must_haves:
  truths:
    - "Hero h1 renders at text-[32px] sm:text-[40px] (scaled down ~30% from 44/56px)"
    - "Hero subtext margin-top reduced from mt-3 to mt-2 (tighter)"
    - "Wrapper div around hero uses mb-4 instead of mb-8 (no dead space before stats bar)"
    - "Stats bar strip renders between hero and search box: '12,400+ prospects indexed · Updated 2 hours ago'"
    - "Stats bar text is text-[11px] uppercase tracking-wider, color var(--text-tertiary), centered"
    - "NLSearchBar background is rgba(255,255,255,0.03) (slightly elevated, no longer var(--bg-input))"
    - "NLSearchBar resting border is rgba(212,175,55,0.18) (slightly stronger than 0.15)"
    - "NLSearchBar focused border is rgba(212,175,55,0.55) (more prominent gold)"
    - "NLSearchBar focused box-shadow stacks inner glow + outer ring: 'inset 0 0 0 1px rgba(212,175,55,0.25), 0 0 0 4px rgba(212,175,55,0.08)'"
    - "NLSearchBar bottom toolbar no longer renders the Filters chip on the left"
    - "NLSearchBar toolbar now shows only Mic + ArrowUp send button (right-aligned)"
    - "NLSearchBar onToggleFilters and filtersOpen props are removed from the interface"
    - "discover-tab.tsx no longer passes onToggleFilters or filtersOpen to NLSearchBar"
    - "discover-tab.tsx no longer renders AdvancedFiltersPanel inline (will be replaced by Plan 02 pills)"
    - "Save this search ghost link still appears below pill when keywords present (preserved)"
  artifacts:
    - path: "src/app/[orgId]/search/components/nl-search-bar.tsx"
      provides: "Pill-style search bar with elevated background, prominent gold focus, no Filters chip"
      contains: "rgba(255,255,255,0.03)"
    - path: "src/app/[orgId]/search/components/discover-tab.tsx"
      provides: "Tightened hero (32/40px), stats bar, search area with inline filter panel removed"
      contains: "12,400+ prospects indexed"
  key_links:
    - from: "discover-tab hero wrapper"
      to: "stats bar div"
      via: "vertical layout flow"
      pattern: "prospects indexed"
    - from: "NLSearchBar toolbar right side"
      to: "Mic + ArrowUp buttons"
      via: "flex items-center gap-2"
      pattern: "ArrowUp"
---

<objective>
Tighten the discover-tab hero, add the stats bar strip, and deepen the NLSearchBar with a more elevated background and prominent gold focus state. Remove the Filters chip from inside NLSearchBar (Plan 02 will replace it with an external filter pills row).

Purpose: Scale down the heading ~30% to claw back vertical space, remove the dead space between heading and search box, add a subtle "trust strip" stats bar showing indexed prospects + freshness, and make the search bar feel intentionally elevated with a stronger focus state. Stripping the Filters chip from inside the bar prepares it for the external filter pills row added in Plan 02.

Output:
- discover-tab.tsx with hero text-[32px] sm:text-[40px], reduced margin-bottom, new stats bar strip, and AdvancedFiltersPanel removed from inline render
- nl-search-bar.tsx with rgba(255,255,255,0.03) background, prominent gold focus border + inner glow + outer ring, Filters chip removed, onToggleFilters/filtersOpen props removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@src/app/[orgId]/search/components/discover-tab.tsx
@src/app/[orgId]/search/components/nl-search-bar.tsx

<interfaces>
<!-- Existing NLSearchBar props (will be slimmed down) -->
```typescript
// BEFORE (current state)
interface NLSearchBarProps {
  initialValue?: string;
  onSearch: (keywords: string) => void;
  isLoading?: boolean;
  onToggleFilters?: () => void;  // REMOVE
  filtersOpen?: boolean;          // REMOVE
}

// AFTER (Plan 01 result)
interface NLSearchBarProps {
  initialValue?: string;
  onSearch: (keywords: string) => void;
  isLoading?: boolean;
}
```

<!-- DiscoverTab continues to receive the same props from search-content.tsx -->
<!-- onApplyFilters is preserved (Plan 02 will wire it to the filter pills row) -->
```typescript
interface DiscoverTabProps {
  personas: Persona[];
  savedSearchCounts?: Record<string, number>;
  keywords: string;
  isLoading: boolean;
  onNLSearch: (keywords: string) => void;
  onApplyFilters: (filters: Partial<PersonaFilters>) => void;
  onSubmitSearch: () => void;
  onSaveAsNewSearch: () => void;
  onSelectSavedSearch: (id: string) => void;
  onViewAllSaved?: () => void;
}
```

<!-- Design tokens used in this plan (from globals.css) -->
- --text-primary-ds: primary text on dark
- --text-secondary-ds: muted body text
- --text-tertiary: subdued labels
- --gold-primary: #d4af37 brand gold
- --bg-elevated: card surface
- --border-subtle, --border-default: card borders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NLSearchBar — elevated bg, prominent gold focus, remove Filters chip</name>
  <files>src/app/[orgId]/search/components/nl-search-bar.tsx</files>
  <action>
Edit `src/app/[orgId]/search/components/nl-search-bar.tsx`:

1. **Slim the props interface** — remove `onToggleFilters` and `filtersOpen` from `NLSearchBarProps`. Drop the `SlidersHorizontal` import (no longer used).

2. **Update the wrapper div styling** — change the outer pill container's inline `style` to:
   - `background: "rgba(255,255,255,0.03)"` (replaces `var(--bg-input)` for the slightly elevated feel)
   - `border: \`1px solid ${isFocused ? "rgba(212,175,55,0.55)" : "rgba(212,175,55,0.18)"}\`` (resting 0.18, focused 0.55 — much more prominent than current 0.15/0.30)
   - `boxShadow: isFocused ? "inset 0 0 0 1px rgba(212,175,55,0.25), 0 0 0 4px rgba(212,175,55,0.08)" : "none"` (stacks inner glow + outer ring for clear focus signal)
   - Keep `transition: "border-color 0.2s ease, box-shadow 0.2s ease"` and `rounded-[24px] overflow-hidden`

3. **Strip the Filters chip from the bottom toolbar** — In the `<div className="flex items-center justify-between px-3 pb-3">` toolbar:
   - Remove the entire left-side `<button>` that renders the SlidersHorizontal icon + "Filters" text
   - Replace `justify-between` with `justify-end` so Mic + Send remain right-aligned
   - Keep the right-side flex group `<div className="flex items-center gap-2">` (Mic + ArrowUp send button) intact, including all current styling

4. **Preserve every other behavior** — auto-resize logic (`handleAutoResize`), `handleChange`, `handleKeyDown` (Enter without shift submits), focus/blur state, send button gold-when-text styling, mic disabled placeholder. Do not touch the textarea element or its props.

Why: The user feedback says the search box is "too subtle" and the Filters chip needs to leave the bar (Plan 02 will replace it with an external pills row). Stacking inner inset + outer ring on focus is a luxury depth pattern — same trick used on premium SaaS inputs. Inline `style` is the right call here because we're using `rgba()` literals and stacked box-shadows that aren't in our design token set.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "nl-search-bar" || echo "OK: no type errors in nl-search-bar.tsx"</automated>
  </verify>
  <done>
- File compiles, props interface no longer contains `onToggleFilters` or `filtersOpen`
- Wrapper div uses `rgba(255,255,255,0.03)` background and the new focused border/box-shadow stack
- No `SlidersHorizontal` import, no "Filters" button in toolbar
- Toolbar uses `justify-end`; Mic + ArrowUp send button render unchanged on the right
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten DiscoverTab hero, add stats bar, remove inline AdvancedFiltersPanel</name>
  <files>src/app/[orgId]/search/components/discover-tab.tsx</files>
  <action>
Edit `src/app/[orgId]/search/components/discover-tab.tsx`:

1. **Drop the AdvancedFiltersPanel import and inline render** — remove `import { AdvancedFiltersPanel } from "./advanced-filters-panel";`. Delete the local `filtersOpen` state (`const [filtersOpen, setFiltersOpen] = useState(false);`). Remove the `{filtersOpen && (<div className="mt-3"><AdvancedFiltersPanel onApplyFilters={onApplyFilters} /></div>)}` block. **Keep `onApplyFilters` in the props destructure** — Plan 02 will wire it to the new filter pills row. Do not remove it from the props interface.

2. **Tighten the hero block** — On the `<div className="mb-8 text-center relative">` wrapper, change `mb-8` to `mb-4` (reduces dead space before the new stats bar).

3. **Scale down the h1** — change `className="font-serif text-[44px] sm:text-[56px] font-medium"` to `className="font-serif text-[32px] sm:text-[40px] font-medium"`. Keep the inline style (`letterSpacing: "-0.5px", color: "var(--text-primary-ds)"`).

4. **Tighten the subtext gap** — change the `<p className="mt-3 text-[13px] font-light"` to `<p className="mt-2 text-[13px] font-light"` (mt-3 → mt-2).

5. **Add the stats bar strip** — Immediately after the hero `<div className="mb-4 text-center relative">…</div>` closes, and BEFORE the `{/* Search area */}` div, insert:

```tsx
{/* Stats bar — subtle trust strip */}
<div className="mb-4 text-center">
  <p
    className="text-[11px] uppercase tracking-wider font-medium"
    style={{ color: "var(--text-tertiary)" }}
  >
    12,400+ prospects indexed <span style={{ opacity: 0.5 }}>·</span> Updated 2 hours ago
  </p>
</div>
```

6. **Update NLSearchBar invocation** — remove the `onToggleFilters={() => setFiltersOpen((p) => !p)}` and `filtersOpen={filtersOpen}` props from the `<NLSearchBar … />` JSX. Keep `key={prefillValue}`, `initialValue={prefillValue}`, `onSearch`, and `isLoading`.

7. **Preserve the rest** — radial glow div, ghost "Save this search" link, SavedSearchShortcutList, max-w-[680px] container, page-enter class. Do not touch.

Why: Heading scale-down recaptures vertical space; the stats bar slots into the freed space as a subtle trust signal. The inline AdvancedFiltersPanel removal clears the way for Plan 02's filter pills row. We are keeping `onApplyFilters` in the props because Plan 02 wires it to the new pills row in the same file (Wave 2 dependency).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -E "discover-tab" || echo "OK: no type errors in discover-tab.tsx"</automated>
  </verify>
  <done>
- File compiles
- h1 className uses `text-[32px] sm:text-[40px]`
- Stats bar div with "12,400+ prospects indexed" renders between hero and search area
- AdvancedFiltersPanel import + filtersOpen state + inline render are gone
- NLSearchBar invocation no longer passes onToggleFilters/filtersOpen
- onApplyFilters is still destructured from props (untouched, Plan 02 will use it)
- max-w-[680px], radial glow div, save-this-search ghost link all preserved
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` produces zero new errors in nl-search-bar.tsx and discover-tab.tsx
- `pnpm build --no-lint` succeeds
- Visual smoke check (manual after Plan 02): hero is visibly smaller, stats bar reads correctly, search bar has clear elevated feel and a strong gold focus ring
</verification>

<success_criteria>
- [ ] NLSearchBar no longer accepts `onToggleFilters` / `filtersOpen` props
- [ ] NLSearchBar background = `rgba(255,255,255,0.03)`, focused border = `rgba(212,175,55,0.55)`, focused box-shadow stacks inner inset + outer ring
- [ ] No "Filters" chip in NLSearchBar toolbar
- [ ] DiscoverTab hero h1 = `text-[32px] sm:text-[40px]`
- [ ] Stats bar div rendered between hero and search area
- [ ] AdvancedFiltersPanel inline render removed from discover-tab.tsx
- [ ] `pnpm tsc --noEmit` clean for both files
</success_criteria>

<output>
After completion, create `.planning/phases/32-discover-tab-v2-layout-tightening-search-depth-filter-pills-suggested-personas/32-01-SUMMARY.md`
</output>
