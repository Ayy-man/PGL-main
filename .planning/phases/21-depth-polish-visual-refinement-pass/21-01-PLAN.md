---
phase: 21-depth-polish-visual-refinement-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[orgId]/search/components/prospect-result-card.tsx
  - src/app/[orgId]/search/components/persona-card.tsx
  - src/app/[orgId]/lists/components/list-grid.tsx
  - src/app/[orgId]/exports/components/export-log-client.tsx
  - src/components/dashboard/activity-feed.tsx
  - src/components/charts/metrics-cards.tsx
autonomous: true
requirements:
  - VR-01
  - VR-02
  - VR-03
  - VR-04

must_haves:
  truths:
    - "Search result cards show gold glow on hover"
    - "Export log table rows animate in with stagger and lift on hover"
    - "Activity feed entries animate in with stagger"
    - "List grid cards have card-glow and press feedback"
    - "Dashboard stat cards display gold crown featured line"
    - "Persona cards in search have gold glow and press feedback"
  artifacts:
    - path: "src/app/[orgId]/search/components/prospect-result-card.tsx"
      provides: "Gold glow added to existing hover inline style"
      contains: "0 0 20px rgba(212, 175, 55, 0.06)"
    - path: "src/app/[orgId]/exports/components/export-log-client.tsx"
      provides: "row-hover-lift press-effect row-enter on data rows"
      contains: "row-hover-lift"
    - path: "src/components/dashboard/activity-feed.tsx"
      provides: "row-enter stagger on feed entries"
      contains: "row-enter"
    - path: "src/app/[orgId]/lists/components/list-grid.tsx"
      provides: "card-glow press-effect on list cards"
      contains: "card-glow"
    - path: "src/components/charts/metrics-cards.tsx"
      provides: "surface-card-featured on stat cards"
      contains: "surface-card-featured"
    - path: "src/app/[orgId]/search/components/persona-card.tsx"
      provides: "Gold glow added to persona card hover style"
      contains: "0 0 20px rgba(212, 175, 55, 0.06)"
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/[orgId]/exports/components/export-log-client.tsx"
      via: "row-hover-lift press-effect row-enter CSS classes"
      pattern: "row-hover-lift press-effect row-enter"
    - from: "src/app/globals.css"
      to: "src/components/dashboard/activity-feed.tsx"
      via: "row-enter CSS class"
      pattern: "row-enter"
---

<objective>
Wire existing depth/polish CSS utility classes to tenant-facing components that currently lack them. This plan covers the 6 highest-impact tenant-visible components: search result cards, persona cards, list grid, export log table, activity feed, and dashboard stat cards.

Purpose: Apply the CSS animation and depth classes (row-enter, card-glow, press-effect, row-hover-lift, surface-card-featured) that were defined in globals.css during quick task 260327-usu but only wired to 4 components. This plan extends coverage to the remaining tenant-facing UI.

Output: 6 modified component files with depth/polish CSS classes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-depth-polish-visual-refinement-pass/21-RESEARCH.md

@src/app/globals.css
@src/app/[orgId]/lists/components/list-member-table.tsx
@src/components/prospect/wealth-signals.tsx
</context>

<interfaces>
<!-- Established CSS utility classes from globals.css (lines 510-533) -->
<!-- These classes are already defined and working. Tasks only need to ADD them to className strings. -->

.row-hover-lift — transition: all 0.15s ease; on hover: translateY(-1px) + depth shadow
.press-effect:active — transform: scale(0.995)
.card-glow — transition on box-shadow/border-color; on hover: dual shadow + gold glow 0 0 20px rgba(212,175,55,0.06)
.row-enter — animation: fadeInUp 0.3s ease forwards; starts opacity:0
.surface-card-featured — ::after pseudo-element with gold crown gradient line at top
.badge-pulse — animation: pulse-subtle 2s ease-in-out infinite

<!-- Established wiring patterns (from list-member-table.tsx and wealth-signals.tsx): -->

Table row pattern: className="row-hover-lift press-effect row-enter" style={{ animationDelay: `${i * 30}ms` }}
Card pattern: className="card-glow press-effect row-enter" style={{ animationDelay: `${index * 60}ms` }}
Featured card: className="surface-card surface-card-featured rounded-[14px]"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire depth classes to search cards, persona cards, list grid, and activity feed</name>
  <files>
    src/app/[orgId]/search/components/prospect-result-card.tsx
    src/app/[orgId]/search/components/persona-card.tsx
    src/app/[orgId]/lists/components/list-grid.tsx
    src/components/dashboard/activity-feed.tsx
  </files>
  <read_first>
    src/app/[orgId]/search/components/prospect-result-card.tsx
    src/app/[orgId]/search/components/persona-card.tsx
    src/app/[orgId]/lists/components/list-grid.tsx
    src/components/dashboard/activity-feed.tsx
    src/app/globals.css
    src/app/[orgId]/lists/components/list-member-table.tsx
    src/components/prospect/wealth-signals.tsx
  </read_first>
  <action>
**prospect-result-card.tsx** — Add gold glow to the existing inline `boxShadow` hover state. This component uses `useState(isHovered)` + inline styles deliberately (manages selection + hover simultaneously). Do NOT migrate to CSS class approach. Change line ~77:
```
boxShadow: isHovered ? "var(--card-shadow-hover)" : "var(--card-shadow)",
```
to:
```
boxShadow: isHovered
  ? "var(--card-shadow-hover), 0 0 20px rgba(212, 175, 55, 0.06)"
  : "var(--card-shadow)",
```
Also add `press-effect` to the className string on the outer div (line ~65). The existing `hover:translate-y-[-1px]` Tailwind class already handles hover lift, so do NOT add `row-hover-lift` (would conflict with the inline transform). Just add `press-effect` after `cursor-pointer`:
```
className="rounded-[12px] p-6 px-7 flex items-start justify-between transition-all duration-[250ms] cursor-pointer press-effect hover:translate-y-[-1px]"
```

**persona-card.tsx (search variant)** — This card also uses `useState(isHovered)` + inline styles. Add gold glow to the `boxShadow` hover. Change line ~40:
```
boxShadow: isHovered ? "var(--card-shadow-hover)" : "var(--card-shadow)",
```
to:
```
boxShadow: isHovered
  ? "var(--card-shadow-hover), 0 0 20px rgba(212, 175, 55, 0.06)"
  : "var(--card-shadow)",
```
Add `press-effect` to the className on the outer `<button>` (line ~34). Keep existing `transition-all duration-200`. Result:
```
className="rounded-[14px] p-7 text-left transition-all duration-200 cursor-pointer press-effect overflow-hidden relative w-full"
```

**list-grid.tsx** — Add `card-glow press-effect` to each list card. The cards already use `animate-stagger-in` with delay, so do NOT add `row-enter` (would conflict — both set animation). Add `card-glow press-effect` to the className on line ~48. Change:
```
className="surface-card flex flex-col sm:flex-row sm:items-center sm:justify-between p-4 sm:p-6 sm:px-7 rounded-xl cursor-pointer group animate-stagger-in"
```
to:
```
className="surface-card card-glow press-effect flex flex-col sm:flex-row sm:items-center sm:justify-between p-4 sm:p-6 sm:px-7 rounded-xl cursor-pointer group animate-stagger-in"
```

**activity-feed.tsx** — Add `row-enter` with stagger to each feed entry. On the entry div (around line ~141-143), add `row-enter` to className. Pass `animationDelay` via the existing inline style object. The entries are mapped with an index available from `.map((entry) => ...)` — change to `.map((entry, index) => ...)` and add stagger:
```tsx
{entries.map((entry, index) => (
  <div
    key={entry.id}
    className="flex items-center gap-3 rounded-[8px] px-3 py-2.5 transition-colors row-enter"
    style={{ background: "transparent", animationDelay: `${Math.min(index * 30, 300)}ms` }}
    onMouseEnter={(e) => {
      e.currentTarget.style.background = "var(--bg-elevated)";
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.background = "transparent";
    }}
  >
```
The `Math.min(index * 30, 300)` caps stagger at 300ms for large feeds.
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && grep -c "0 0 20px rgba(212, 175, 55, 0.06)" src/app/[orgId]/search/components/prospect-result-card.tsx src/app/[orgId]/search/components/persona-card.tsx && grep -c "card-glow press-effect" src/app/[orgId]/lists/components/list-grid.tsx && grep -c "row-enter" src/components/dashboard/activity-feed.tsx && grep -c "press-effect" src/app/[orgId]/search/components/prospect-result-card.tsx src/app/[orgId]/search/components/persona-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - prospect-result-card.tsx contains the string `0 0 20px rgba(212, 175, 55, 0.06)` (gold glow in hover boxShadow)
    - prospect-result-card.tsx contains `press-effect` in a className attribute
    - persona-card.tsx (search variant) contains `0 0 20px rgba(212, 175, 55, 0.06)` (gold glow in hover boxShadow)
    - persona-card.tsx (search variant) contains `press-effect` in a className attribute
    - list-grid.tsx contains `card-glow press-effect` in the list card className
    - activity-feed.tsx contains `row-enter` in the entry div className
    - activity-feed.tsx contains `Math.min(index * 30, 300)` for capped stagger delay
  </acceptance_criteria>
  <done>Gold glow on search cards + persona cards, card-glow on list grid, row-enter on activity feed — all 4 components wired with depth classes</done>
</task>

<task type="auto">
  <name>Task 2: Wire depth classes to export log table and stat cards featured line</name>
  <files>
    src/app/[orgId]/exports/components/export-log-client.tsx
    src/components/charts/metrics-cards.tsx
  </files>
  <read_first>
    src/app/[orgId]/exports/components/export-log-client.tsx
    src/components/charts/metrics-cards.tsx
    src/app/[orgId]/lists/components/list-member-table.tsx
    src/components/prospect/profile-header.tsx
  </read_first>
  <action>
**export-log-client.tsx** — Add `row-hover-lift press-effect row-enter` to the data TableRow elements (not the header row). The desktop data rows are on line ~247 (`<TableRow key={entry.id}>`). Add the CSS classes and stagger delay. The `.map()` already provides an index parameter or can be added. Change:
```tsx
<TableRow key={entry.id}>
```
to:
```tsx
<TableRow key={entry.id} className="row-hover-lift press-effect row-enter" style={{ animationDelay: `${Math.min(index * 30, 300)}ms` }}>
```
Ensure the `.map()` callback has an index parameter. Check for `exports.map((entry)` or similar and add `, index` if missing. If the map callback already names the index parameter differently (like `i`), use that.

Do NOT modify the header `<TableRow>` (line ~225) — only data rows.

Also check for the mobile card variant rows. If there is a mobile card layout (hidden md:block pattern), add `row-enter` with stagger to mobile cards too.

**metrics-cards.tsx** — Add `surface-card-featured` alongside existing `surface-card` on the stat card divs. These are the primary dashboard metric cards and qualify as "hero" cards per the spec. On line ~61, change:
```
className="surface-card rounded-[14px] p-5"
```
to:
```
className="surface-card surface-card-featured rounded-[14px] p-5"
```
Also add `row-enter` with stagger for entrance animation. Use the `METRICS.map` index. The `METRICS.map` callback on line ~55 uses destructuring `({ key, label, icon })` — add an index parameter:
```tsx
{METRICS.map(({ key, label, icon }, index) => {
```
Then on the div:
```
className="surface-card surface-card-featured rounded-[14px] p-5 row-enter"
style={{ animationDelay: `${index * 60}ms` }}
```
Note: metrics-cards.tsx is a client component ("use client" at top). If it is NOT a client component, row-enter will cause a brief invisible flash on server render (per Pitfall 3 in RESEARCH.md). Check the top of the file — if it starts with "use client", proceed. If not, use `animate-stagger-in` instead of `row-enter`.
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && grep -c "row-hover-lift press-effect row-enter" src/app/[orgId]/exports/components/export-log-client.tsx && grep "surface-card-featured" src/components/charts/metrics-cards.tsx | head -3 && grep -c "row-enter" src/components/charts/metrics-cards.tsx</automated>
  </verify>
  <acceptance_criteria>
    - export-log-client.tsx data TableRow elements contain `row-hover-lift press-effect row-enter` in className
    - export-log-client.tsx data TableRow elements contain `animationDelay` with `Math.min` capping
    - export-log-client.tsx header TableRow does NOT contain `row-hover-lift`
    - metrics-cards.tsx stat card divs contain `surface-card surface-card-featured` in className
    - metrics-cards.tsx stat card divs contain `row-enter` (or `animate-stagger-in` if not client component) with stagger delay
    - `pnpm build --no-lint` exits 0
  </acceptance_criteria>
  <done>Export log rows have hover-lift + stagger entrance; dashboard stat cards display gold crown featured line with entrance animation</done>
</task>

</tasks>

<verification>
- `pnpm build --no-lint` exits 0 (no TypeScript compilation errors introduced)
- `grep -c 'row-hover-lift\|card-glow\|press-effect\|row-enter\|surface-card-featured' src/app/[orgId]/search/components/prospect-result-card.tsx src/app/[orgId]/search/components/persona-card.tsx src/app/[orgId]/lists/components/list-grid.tsx src/components/dashboard/activity-feed.tsx src/app/[orgId]/exports/components/export-log-client.tsx src/components/charts/metrics-cards.tsx` — all files show at least 1 match
- No new hardcoded `boxShadow` values outside the gold glow addition pattern
</verification>

<success_criteria>
- All 6 tenant-facing components have depth/polish CSS classes wired
- Gold glow visible on search result card hover and persona card hover
- Export log table rows animate in with stagger
- Activity feed entries animate in with stagger
- List grid cards have gold glow on hover and press feedback
- Dashboard stat cards display gold crown featured line
- Build passes clean (pnpm build --no-lint exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/21-depth-polish-visual-refinement-pass/21-01-SUMMARY.md`
</output>
