---
phase: 21-depth-polish-visual-refinement-pass
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified: []
autonomous: true
requirements:
  - VR-07

must_haves:
  truths:
    - "Build compiles with zero TypeScript errors"
    - "No new hardcoded box-shadow rgba values in component TSX files"
    - "All depth CSS classes are wired to their target components"
  artifacts: []
  key_links: []
---

<objective>
Build verification and box-shadow consistency audit for Phase 21. Confirm the build passes, audit for inconsistent hardcoded box-shadow values in component files, and verify all depth CSS classes were successfully wired.

Purpose: Final gate ensuring all visual polish changes compile cleanly and no rogue box-shadow values were introduced. The grep audit catches any hardcoded rgba() shadow values in component TSX files that should use CSS variables or utility classes instead.

Output: Verified build, audit results, STATE.md and ROADMAP.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-depth-polish-visual-refinement-pass/21-RESEARCH.md
@.planning/phases/21-depth-polish-visual-refinement-pass/21-01-SUMMARY.md
@.planning/phases/21-depth-polish-visual-refinement-pass/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification + box-shadow audit + depth class coverage check</name>
  <files></files>
  <read_first>
    .planning/phases/21-depth-polish-visual-refinement-pass/21-RESEARCH.md
    .planning/phases/21-depth-polish-visual-refinement-pass/21-01-SUMMARY.md
    .planning/phases/21-depth-polish-visual-refinement-pass/21-02-SUMMARY.md
  </read_first>
  <action>
Run three verification checks in sequence:

**1. Build verification:**
```bash
pnpm build --no-lint
```
Must exit 0. If it fails, diagnose and fix any TypeScript errors introduced by Plans 01-02.

**2. Box-shadow consistency audit:**
```bash
grep -rn "box-shadow\|boxShadow" src/ --include="*.tsx" | grep "rgba\|rgb(" | grep -v "globals.css" | grep -v "node_modules"
```
Review the output. The following patterns are ACCEPTABLE (pre-existing from prior phases, not introduced by Phase 21):
- `getAvatarGradient` or avatar background gradients
- `boxShadow: "var(--card-shadow-hover), 0 0 20px rgba(212, 175, 55, 0.06)"` — this is the gold glow pattern added by Plan 01 and is correct
- Any `boxShadow` that references `var(--card-shadow)` or `var(--card-shadow-hover)` as the primary value

Flag anything that is a raw hardcoded `box-shadow: X Y Z rgba(...)` NOT referencing CSS variables. Log these as pre-existing violations (not Phase 21 regressions) if they existed before.

**3. Depth class coverage check:**
```bash
grep -l "row-hover-lift\|card-glow\|press-effect\|row-enter\|surface-card-featured" src/app/[orgId]/search/components/prospect-result-card.tsx src/app/[orgId]/search/components/persona-card.tsx src/app/[orgId]/lists/components/list-grid.tsx src/components/dashboard/activity-feed.tsx src/app/[orgId]/exports/components/export-log-client.tsx src/components/charts/metrics-cards.tsx src/components/admin/automation-runs-table.tsx
```
All 7 files must appear in the output (each has at least one depth class). If any file is missing, report which depth classes are absent.

Also verify:
```bash
grep "backdrop-blur-sm" src/components/ui/sheet.tsx src/components/ui/dropdown-menu.tsx
```
Both files must show a match.

**4. Update STATE.md and ROADMAP.md:**
- In STATE.md: Update "Current Position" to Phase 21, update "Last activity" to current date, update progress
- In ROADMAP.md: Update Phase 21 status to COMPLETE, mark plans as done, update goal from `[To be planned]`
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && pnpm build --no-lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build --no-lint` exits 0
    - grep depth class coverage check returns all 7 component files
    - grep backdrop-blur-sm check returns both sheet.tsx and dropdown-menu.tsx
    - Box-shadow audit shows no NEW hardcoded rgba() shadows introduced by Phase 21 (pre-existing violations acceptable)
    - STATE.md updated with Phase 21 status
    - ROADMAP.md Phase 21 updated with goal, plan list, and COMPLETE status
  </acceptance_criteria>
  <done>Build passes, box-shadow audit clean (no regressions), all depth classes wired to all target components, planning docs updated</done>
</task>

</tasks>

<verification>
- `pnpm build --no-lint` exits 0
- All 7 target component files contain at least one depth CSS class
- Both shared UI files (sheet.tsx, dropdown-menu.tsx) contain backdrop-blur-sm
- STATE.md and ROADMAP.md are updated
</verification>

<success_criteria>
- Build compiles cleanly with zero new errors
- Box-shadow audit confirms no regressions
- All 9 files modified across Plans 01-03 verified
- Phase 21 marked complete in planning documentation
</success_criteria>

<output>
After completion, create `.planning/phases/21-depth-polish-visual-refinement-pass/21-03-SUMMARY.md`
</output>
