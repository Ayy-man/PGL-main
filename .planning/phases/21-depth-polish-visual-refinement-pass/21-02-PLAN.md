---
phase: 21-depth-polish-visual-refinement-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/admin/automation-runs-table.tsx
autonomous: true
requirements:
  - VR-05
  - VR-06

must_haves:
  truths:
    - "Sheet overlay shows backdrop blur behind the dimmed area"
    - "Dropdown menu content shows subtle backdrop blur"
    - "Admin automation runs table rows animate in with stagger and lift on hover"
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "Backdrop blur on SheetOverlay"
      contains: "backdrop-blur-sm"
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "Backdrop blur on DropdownMenuContent"
      contains: "backdrop-blur-sm"
    - path: "src/components/admin/automation-runs-table.tsx"
      provides: "row-hover-lift press-effect row-enter on data rows"
      contains: "row-hover-lift"
  key_links:
    - from: "src/components/ui/sheet.tsx"
      to: "All Sheet consumers (tenant drawer, mobile nav, prospect slide-over)"
      via: "SheetOverlay component"
      pattern: "backdrop-blur-sm"
    - from: "src/components/ui/dropdown-menu.tsx"
      to: "All DropdownMenu consumers (TopBar, action menus)"
      via: "DropdownMenuContent component"
      pattern: "backdrop-blur-sm"
---

<objective>
Add backdrop blur to elevated UI surfaces (Sheet overlay, DropdownMenu content) and wire depth classes to admin automation runs table.

Purpose: Backdrop blur on sheets and dropdowns adds physical depth perception (the underlying content blurs, reinforcing the z-layer). Admin automation table rows get the same hover-lift and stagger entrance treatment as tenant-facing tables. These are the remaining gaps from the research that target shared UI primitives and admin components.

Output: 3 modified component files with backdrop blur and depth classes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-depth-polish-visual-refinement-pass/21-RESEARCH.md

@src/components/ui/dialog.tsx
</context>

<interfaces>
<!-- dialog.tsx already has backdrop-blur-sm on its overlay (line 24): -->
<!-- "fixed inset-0 z-50 bg-black/80 backdrop-blur-sm ..." -->
<!-- Sheet and DropdownMenu should follow this same pattern. -->

<!-- From sheet.tsx current SheetOverlay (line 24): -->
<!-- "fixed inset-0 z-50 bg-black/80 data-[state=open]:animate-in ..." -->
<!-- Missing: backdrop-blur-sm -->

<!-- From dropdown-menu.tsx current DropdownMenuContent (line 68): -->
<!-- "z-50 ... rounded-md border bg-popover p-1 text-popover-foreground shadow-md ..." -->
<!-- Missing: backdrop-blur-sm -->

<!-- Established table row depth pattern (from list-member-table.tsx line 173): -->
<!-- className="row-hover-lift press-effect row-enter" style={{ animationDelay: `${i * 30}ms` }} -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add backdrop blur to Sheet overlay and DropdownMenu content</name>
  <files>
    src/components/ui/sheet.tsx
    src/components/ui/dropdown-menu.tsx
  </files>
  <read_first>
    src/components/ui/sheet.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/dialog.tsx
  </read_first>
  <action>
**sheet.tsx** — Add `backdrop-blur-sm` to SheetOverlay className and reduce opacity from `bg-black/80` to `bg-black/60` so the blur is perceptible. On line 24, change:
```
"fixed inset-0 z-50 bg-black/80  data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
```
to:
```
"fixed inset-0 z-50 bg-black/60 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0",
```
Note the double space before `data-` on the current line — clean that up to single space.

**dropdown-menu.tsx** — Add `backdrop-blur-sm` to DropdownMenuContent className. On line 68, add `backdrop-blur-sm` after `shadow-md`. Change:
```
"z-50 max-h-[var(--radix-dropdown-menu-content-available-height)] min-w-[8rem] overflow-y-auto overflow-x-hidden rounded-md border bg-popover p-1 text-popover-foreground shadow-md",
```
to:
```
"z-50 max-h-[var(--radix-dropdown-menu-content-available-height)] min-w-[8rem] overflow-y-auto overflow-x-hidden rounded-md border bg-popover p-1 text-popover-foreground shadow-md backdrop-blur-sm",
```
Keep `bg-popover` as-is — shadcn expects it for theming. The blur adds subtle depth behind the dropdown even with the dark popover background. Do NOT change the DropdownMenuSubContent (less visible, lower priority per research recommendation).
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && grep "backdrop-blur-sm" src/components/ui/sheet.tsx src/components/ui/dropdown-menu.tsx && grep "bg-black/60" src/components/ui/sheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - sheet.tsx SheetOverlay className contains `backdrop-blur-sm`
    - sheet.tsx SheetOverlay className contains `bg-black/60` (reduced from bg-black/80)
    - sheet.tsx does NOT have a double-space before `data-[state=open]`
    - dropdown-menu.tsx DropdownMenuContent className contains `backdrop-blur-sm`
    - dropdown-menu.tsx DropdownMenuContent still contains `bg-popover` (not removed)
    - dropdown-menu.tsx DropdownMenuSubContent does NOT have `backdrop-blur-sm` added
  </acceptance_criteria>
  <done>Sheet overlay and dropdown menu content both have backdrop-blur-sm for physical depth perception on elevated surfaces</done>
</task>

<task type="auto">
  <name>Task 2: Wire depth classes to admin automation runs table</name>
  <files>
    src/components/admin/automation-runs-table.tsx
  </files>
  <read_first>
    src/components/admin/automation-runs-table.tsx
    src/app/[orgId]/lists/components/list-member-table.tsx
  </read_first>
  <action>
**automation-runs-table.tsx** — Add `row-hover-lift press-effect row-enter` to data table rows. Read the file to find the data row mapping pattern. Look for a `.map()` over runs that renders `<TableRow>` or `<tr>` elements. Add the CSS classes and stagger delay following the list-member-table.tsx pattern:

```tsx
className="row-hover-lift press-effect row-enter"
style={{ animationDelay: `${Math.min(index * 30, 300)}ms` }}
```

Apply ONLY to data rows, NOT to header rows or empty-state rows.

If the component uses a mobile card variant (hidden md:block pattern from Phase 17), add `row-enter` with stagger to the mobile cards too.

Note: Admin components use `admin-row-hover` for their own hover system (per RESEARCH.md scope boundary). However, the research identified automation-runs-table as NOT having any hover/entrance effects. Since this is not using `admin-row-hover` currently, adding `row-hover-lift` is correct. If it already uses `admin-row-hover`, do NOT add `row-hover-lift` (would conflict) — instead, just add `row-enter` and `press-effect`.

Check the tenant-heatmap.tsx — it already has `row-hover` (class="row-hover" at line 91), which is part of the admin hover system. Do NOT modify tenant-heatmap.tsx.
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library.noSync/PGL-main" && grep -c "row-hover-lift\|row-enter\|press-effect" src/components/admin/automation-runs-table.tsx && grep -c "row-hover-lift\|row-enter" src/components/admin/tenant-heatmap.tsx</automated>
  </verify>
  <acceptance_criteria>
    - automation-runs-table.tsx data rows contain `row-enter` and `press-effect` in className
    - automation-runs-table.tsx data rows contain `animationDelay` with `Math.min` capping
    - automation-runs-table.tsx header rows do NOT contain `row-hover-lift` or `row-enter`
    - tenant-heatmap.tsx is NOT modified (still has only `row-hover`, no new classes added)
    - `pnpm build --no-lint` exits 0
  </acceptance_criteria>
  <done>Admin automation runs table rows have entrance animation and hover feedback; tenant heatmap untouched (uses its own admin hover system)</done>
</task>

</tasks>

<verification>
- `pnpm build --no-lint` exits 0 (no TypeScript compilation errors)
- `grep -c 'backdrop-blur-sm' src/components/ui/sheet.tsx src/components/ui/dropdown-menu.tsx` — both show 1
- `grep -c 'row-enter' src/components/admin/automation-runs-table.tsx` — shows at least 1
- Sheet overlay uses bg-black/60 (not /80)
</verification>

<success_criteria>
- Sheet overlay and DropdownMenu content have backdrop blur for depth perception
- Admin automation runs table rows animate in with stagger and lift on hover
- Tenant heatmap is NOT modified (scope boundary respected)
- Build passes clean (pnpm build --no-lint exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/21-depth-polish-visual-refinement-pass/21-02-SUMMARY.md`
</output>
