---
phase: 25-exa-research-scrapbook
plan: 06
type: execute
wave: 4
depends_on: ["25-05"]
files_modified: []
autonomous: true
requirements:
  - RES-19

must_haves:
  truths:
    - "pnpm build --no-lint exits 0"
    - "All new files use CSS variables from design system (no raw hex colors)"
    - "No TypeScript errors in new files"
  artifacts: []
  key_links: []
---

<objective>
Build verification and design system compliance audit for the Exa Research Scrapbook feature.

Purpose: Confirm the entire feature builds cleanly, all components follow the design system, and no regressions were introduced.
Output: Build verification results, design system audit report, STATE.md and ROADMAP.md updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-exa-research-scrapbook/25-CONTEXT.md
@.planning/phases/25-exa-research-scrapbook/25-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification + TypeScript check</name>
  <read_first>
    - package.json
  </read_first>
  <files></files>
  <action>
    Run the full build to verify no errors:

    ```bash
    pnpm build --no-lint
    ```

    If build fails:
    1. Read the error output carefully
    2. Fix TypeScript errors in the files created/modified by this phase
    3. Common issues to check:
       - AI SDK v6 import paths (`createUIMessageStream` from `"ai"`, `useChat` from `"@ai-sdk/react"`)
       - shadcn AI component imports (verify exact export names match)
       - Type mismatches between ScrapbookCard and what API routes return
       - Missing props on ResearchPanel or DossierResearchToggle
    4. Re-run build until it passes

    If build succeeds, also verify key type correctness:
    ```bash
    npx tsc --noEmit --pretty 2>&1 | head -50
    ```
  </action>
  <verify>
    <automated>pnpm build --no-lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build --no-lint` exits with code 0
    - No TypeScript errors in phase 25 files
    - Build output shows all pages compiled successfully
  </acceptance_criteria>
  <done>Build passes clean with no errors from phase 25 files.</done>
</task>

<task type="auto">
  <name>Task 2: Design system compliance audit + ROADMAP/STATE update</name>
  <read_first>
    - src/components/prospect/dossier-research-toggle.tsx
    - src/components/prospect/research-panel.tsx
    - src/components/prospect/research-result-card.tsx
    - src/components/prospect/research-pin-dropdown.tsx
    - src/components/prospect/profile-view.tsx
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <files></files>
  <action>
    **Design system audit:**

    Check all new/modified component files for raw color violations. Every color should use CSS variables, not hardcoded hex/rgb (except rgba with opacity for shadows/overlays which are acceptable per Phase 05 decision).

    Run for each phase 25 component file:
    ```bash
    grep -n '#[0-9a-fA-F]\{3,8\}' src/components/prospect/dossier-research-toggle.tsx src/components/prospect/research-panel.tsx src/components/prospect/research-result-card.tsx src/components/prospect/research-pin-dropdown.tsx 2>/dev/null
    ```

    **Acceptable patterns** (not violations):
    - `rgba(...)` for shadows, overlays, gradients with opacity
    - Colors inside CATEGORY_COLORS constant (mapping to specific signal categories)
    - `#000` in gold gradient button (black text on gold background)

    **Violations to fix:**
    - Raw hex colors used for text, backgrounds, borders outside of the above exceptions
    - Replace with CSS variable equivalents

    Also check:
    - `font-sans` used for message text (not system default)
    - `font-mono` used for timestamps/metadata
    - CSS variable usage for `--bg-card`, `--border-subtle`, `--text-primary`, `--gold-primary`

    **ROADMAP.md update:**

    Update Phase 25 entry:
    ```markdown
    ### Phase 25: Exa Research Scrapbook

    **Goal:** Build a first-class research workbench in every prospect profile. Agent types freeform questions, Exa searches the web, LLM digests results into rich cards the agent can edit and pin to the prospect's Intelligence Dossier or Signal Timeline.
    **Requirements**: RES-01 through RES-19
    **Depends on:** Phase 24
    **Status:** COMPLETE -- All 6 plans executed. Build verified (pnpm build --no-lint exit 0). Design system compliance audit: [N/N files pass].

    **Plans:** 6/6 plans complete

    Plans:
    - [x] 25-01-PLAN.md -- Foundation: packages, shadcn AI components, DB migration, TypeScript types
    - [x] 25-02-PLAN.md -- Lib functions: exa-search, research-digest, rate-limit
    - [x] 25-03-PLAN.md -- API routes: streaming research, pin, sessions, suggestions
    - [x] 25-04-PLAN.md -- UI components: toggle, research panel, result card, pin dropdown
    - [x] 25-05-PLAN.md -- Profile integration: wire into profile-view, signal timeline updates
    - [x] 25-06-PLAN.md -- Build verification + design system compliance audit
    ```

    **STATE.md update:**

    Update current position to Phase 25 complete:
    - Phase: 25
    - Status: Complete
    - Last activity: today's date
  </action>
  <verify>
    <automated>grep "Phase 25" .planning/ROADMAP.md && grep "COMPLETE" .planning/ROADMAP.md | grep -i "25"</automated>
  </verify>
  <acceptance_criteria>
    - All phase 25 component files pass design system audit (no raw hex violations outside acceptable patterns)
    - ROADMAP.md Phase 25 entry updated with Goal, Requirements, Status: COMPLETE, all 6 plans listed as [x]
    - STATE.md updated with Phase 25 complete
    - Build still passes after any audit-driven fixes
  </acceptance_criteria>
  <done>Design system audit complete, ROADMAP and STATE updated to reflect Phase 25 completion.</done>
</task>

</tasks>

<verification>
- `pnpm build --no-lint` exits 0
- `grep -c 'raw hex' ...` returns 0 violations (or only acceptable patterns)
- ROADMAP.md shows Phase 25 COMPLETE
- STATE.md shows Phase 25 complete
</verification>

<success_criteria>
- Clean build with no errors
- Design system compliance verified
- Planning documents updated
</success_criteria>

<output>
After completion, create `.planning/phases/25-exa-research-scrapbook/25-06-SUMMARY.md`
</output>
