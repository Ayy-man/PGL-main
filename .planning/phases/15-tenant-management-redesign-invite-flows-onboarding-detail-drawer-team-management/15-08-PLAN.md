# Plan 15-08: Build Verification + Design System Compliance Audit

## Goal
Verify the entire Phase 15 implementation compiles, follows design system conventions, and all flows work end-to-end.

## Tasks

### Task 1: Build verification
Run `pnpm build` and confirm exit 0:
- Fix any TypeScript errors
- Fix any ESLint errors
- Confirm all new pages are generated in build output
- Expected new pages: `/onboarding/confirm-tenant`, `/onboarding/set-password`, `/[orgId]/team`

### Task 2: Design system compliance audit
Check all new/modified files against design system rules:

1. **No raw Tailwind colors** — all color values via CSS variables (--gold-primary, --text-primary-ds, etc.)
2. **No Material Symbols / non-Lucide icons** — only lucide-react
3. **surface-admin-card** on all card containers in admin context
4. **rounded-[14px]** on all card containers
5. **onMouseEnter/onMouseLeave** for CSS variable hover states (Tailwind hover: cannot use CSS vars)
6. **font-serif** on headings per design system
7. **Gold CTA pattern** for primary action buttons: bg-[var(--gold-bg-strong)] border-[var(--border-gold)] text-[var(--gold-primary)]
8. **Skeleton loading states** for async data in drawer and pages
9. **Admin table classes** (admin-thead, admin-row-hover) on all admin tables

Scope: All files created/modified in Plans 15-01 through 15-07.

### Task 3: Flow verification
Verify logical correctness of all flows:

1. **Create tenant + invite admin:**
   - createTenant action accepts admin_email
   - inviteUserByEmail called with correct redirectTo
   - app_metadata set with onboarding_completed: false

2. **Admin onboarding:**
   - Auth callback detects onboarding flag
   - Middleware redirects to /onboarding/confirm-tenant
   - Onboarding page fetches and displays tenant data
   - Submit updates tenant, password, and clears flag

3. **Tenant detail drawer:**
   - Row click opens drawer
   - All 5 API endpoints return correct data
   - All cards render with data
   - Activity tabs switch and cache
   - Expand modal shows full activity log

4. **Team management:**
   - Tenant admin can access team page
   - Invite dialog sends email invite
   - Invited user onboarding flow works
   - Nav item visible only for tenant_admin

### Task 4: Update STATE.md and ROADMAP.md
- Mark Phase 15 plans with completion status
- Update current position in STATE.md
- Log any decisions made during implementation

## Files Modified
- Various (bug fixes from build/audit)
- `.planning/STATE.md`
- `.planning/ROADMAP.md`

## Verification
- `pnpm build` exits 0
- All compliance checks pass
- All flows logically verified
- STATE.md and ROADMAP.md updated
