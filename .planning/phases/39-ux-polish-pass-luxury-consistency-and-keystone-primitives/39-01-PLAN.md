---
phase: 39-ux-polish-pass-luxury-consistency-and-keystone-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-toast.ts
  - src/app/admin/layout.tsx
  - src/app/layout.tsx
  - src/components/ui/button.tsx
  - src/components/ui/toast.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/select.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/enrichment-status-dots.tsx
  - src/app/[orgId]/search/components/prospect-results-table.tsx
  - src/app/[orgId]/lists/components/list-member-table.tsx
  - src/app/not-found.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/table.tsx
autonomous: false
requirements:
  - PHASE-39-K1
  - PHASE-39-K2
  - PHASE-39-K3
  - PHASE-39-K4
  - PHASE-39-K5
  - PHASE-39-K6
  - PHASE-39-K7
  - PHASE-39-K8
  - PHASE-39-K9
  - PHASE-39-K10

must_haves:
  truths:
    - "Toasts auto-dismiss within 5 seconds instead of lingering for 16 minutes"
    - "Up to 3 toasts stack concurrently (not just 1)"
    - "Admin routes show toast notifications (Toaster mounted in admin layout)"
    - "Tooltips appear globally with 250ms delay without per-consumer TooltipProvider"
    - "Primary CTAs (submit buttons without explicit variant) render gold, not tan"
    - "Toast, Tooltip, DropdownMenu all use --bg-floating-elevated (not bg-primary neutral)"
    - "Toast titles use serif (Cormorant Garamond)"
    - "Input/Textarea/Select focus shadow uses var(--gold-bg-strong), not raw rgba"
    - "Badge/Button gold variants use var(--gold-*) CSS vars, not hard-coded #d4af37 hex"
    - "EnrichmentStatusDots in_progress state pulses; search + list tables use the primitive"
    - "Bad tenant URLs show branded 404 page (not Next.js default)"
    - "Checkbox checked state is gold; selected table rows have bg-gold-bg"
  artifacts:
    - path: "src/hooks/use-toast.ts"
      provides: "TOAST_REMOVE_DELAY = 5_000; TOAST_LIMIT = 3"
      contains: "TOAST_REMOVE_DELAY = 5_000"
    - path: "src/app/admin/layout.tsx"
      provides: "Toaster mounted in admin shell"
      contains: 'import { Toaster }'
    - path: "src/app/layout.tsx"
      provides: "TooltipProvider at root with delayDuration=250"
      contains: "TooltipProvider"
    - path: "src/components/ui/button.tsx"
      provides: "defaultVariants.variant = 'gold-solid'"
      contains: "variant: \"gold-solid\""
    - path: "src/components/ui/toast.tsx"
      provides: "floating-elevated bg, serif title"
      contains: "--bg-floating-elevated"
    - path: "src/components/ui/tooltip.tsx"
      provides: "floating-elevated bg"
      contains: "--bg-floating-elevated"
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "floating-elevated bg, gold focus"
      contains: "--bg-floating"
    - path: "src/components/ui/input.tsx"
      provides: "focus shadow uses var(--gold-bg-strong)"
      contains: "var(--gold-bg-strong)"
    - path: "src/components/ui/textarea.tsx"
      provides: "focus shadow uses var(--gold-bg-strong)"
      contains: "var(--gold-bg-strong)"
    - path: "src/components/ui/select.tsx"
      provides: "focus shadow uses var(--gold-bg-strong)"
      contains: "var(--gold-bg-strong)"
    - path: "src/components/ui/badge.tsx"
      provides: "gold variants use var(--gold-*) tokens"
      contains: "var(--gold-primary)"
    - path: "src/components/ui/enrichment-status-dots.tsx"
      provides: "animate-pulse on in_progress"
      contains: "animate-pulse"
    - path: "src/app/not-found.tsx"
      provides: "branded 404 page matching global-error chrome"
      contains: "404"
    - path: "src/components/ui/checkbox.tsx"
      provides: "checked state = gold"
      contains: "gold-primary"
    - path: "src/components/ui/table.tsx"
      provides: "selected row state = bg-gold-bg"
      contains: "data-[state=selected]:bg-[var(--gold-bg)]"
  key_links:
    - from: "src/app/[orgId]/search/components/prospect-results-table.tsx"
      to: "src/components/ui/enrichment-status-dots.tsx"
      via: "import EnrichmentStatusDots and render for enrichment column"
      pattern: "EnrichmentStatusDots"
    - from: "src/app/[orgId]/lists/components/list-member-table.tsx"
      to: "src/components/ui/enrichment-status-dots.tsx"
      via: "import EnrichmentStatusDots, replace local EnrichmentDot"
      pattern: "EnrichmentStatusDots"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/tooltip.tsx"
      via: "TooltipProvider wrapping children"
      pattern: "<TooltipProvider"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/ui/toaster.tsx"
      via: "Toaster mounted once in admin shell"
      pattern: "<Toaster"
---

<objective>
Ship the 10 keystone primitive fixes (K1-K10) from the UX polish audit in one plan. Each is a single-file, atomic edit. Collectively they close ~60 of the 242 findings because downstream screens inherit the fix automatically. This PR MUST land first so subsequent plans operate on the new baseline.

Purpose: Fix the 10 foundational primitives/mounts that cascade across every screen. Correct toast timing, add global Toaster (admin) + TooltipProvider (root), make gold the default button variant, reskin floating surfaces to `--bg-floating-elevated`, tokenize gold usage in focus rings + badge/button gold variants, animate enrichment dots + roll primitive into search/list tables, create branded 404 page, make checkbox/table-selected state gold.

Output: 17 files modified, 1 file created. After merge, ~60 findings are closed without per-screen work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-CONTEXT.md
@.planning/audits/ux-polish-2026-04-14/README.md
@.planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md
@.planning/audits/ux-polish-2026-04-14/05-design-system.md
@tailwind.config.ts
@src/app/globals.css
@src/lib/tenant-theme.ts

<interfaces>
Existing primitives consumed by this plan (no new primitives are created):

From `src/components/ui/toaster.tsx`:
```typescript
export function Toaster(): JSX.Element
```

From `src/components/ui/tooltip.tsx`:
```typescript
export const TooltipProvider: React.FC<TooltipProviderProps>
// Accepts delayDuration, skipDelayDuration
```

From `src/components/ui/enrichment-status-dots.tsx`:
```typescript
// Current signature (from audit 03 line 64, 05 line 212):
export function EnrichmentStatusDots({ sourceStatus }: { sourceStatus: EnrichmentSourceStatus | null }): JSX.Element | null
// Returns null when sourceStatus is null (consumers can render fallback text)
```

From `src/hooks/use-toast.ts` (current values at lines 11-12):
```typescript
const TOAST_LIMIT = 1            // CHANGE TO: 3
const TOAST_REMOVE_DELAY = 1000000  // CHANGE TO: 5_000
```

From `src/components/ui/button.tsx` (current at lines 35-38):
```typescript
defaultVariants: {
  variant: "default",  // CHANGE TO: "gold-solid"
  size: "default",
}
```

Available gold tokens (from globals.css + tailwind.config.ts — DO NOT invent new):
- `--gold-primary` (#d4af37)
- `--gold-bright` (#f0d060)
- `--gold-text` (rgba(212,175,55,0.7))
- `--gold-bg` (rgba(212,175,55,0.08))
- `--gold-bg-strong` (rgba(212,175,55,0.15))
- `--border-gold` (rgba(212,175,55,0.25))
- `--bg-floating` (#141416)
- `--bg-floating-elevated` (#1a1a1e per audit)

Tailwind utility aliases for the gold tokens (per `tailwind.config.ts`):
- `bg-gold-bg`, `bg-gold-bg-strong`, `text-gold-primary`, `text-gold-bright`, `text-gold-text`, `border-border-gold`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (K1): Fix TOAST_REMOVE_DELAY and TOAST_LIMIT</name>
  <read_first>
    - src/hooks/use-toast.ts
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (finding at lines 219-224 — TOAST_REMOVE_DELAY=1_000_000ms; TOAST_LIMIT=1)
  </read_first>
  <files>src/hooks/use-toast.ts</files>
  <action>
    In `src/hooks/use-toast.ts` at lines 11-12:
    - Change `const TOAST_LIMIT = 1` to `const TOAST_LIMIT = 3`
    - Change `const TOAST_REMOVE_DELAY = 1000000` to `const TOAST_REMOVE_DELAY = 5_000`

    Do NOT touch any other logic in this file. Keep identifier names and file structure otherwise unchanged.
  </action>
  <verify>
    <automated>grep -E "TOAST_REMOVE_DELAY = 5_000" src/hooks/use-toast.ts && grep -E "TOAST_LIMIT = 3" src/hooks/use-toast.ts</automated>
  </verify>
  <done>
    - `grep` for `TOAST_REMOVE_DELAY = 5_000` returns a match in `src/hooks/use-toast.ts`.
    - `grep` for `TOAST_LIMIT = 3` returns a match in `src/hooks/use-toast.ts`.
    - `grep` for `TOAST_REMOVE_DELAY = 1000000` returns 0 matches.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (K2): Mount Toaster in admin shell layout</name>
  <read_first>
    - src/app/admin/layout.tsx
    - src/components/ui/toaster.tsx
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (Layout section, lines 9-14 — no Toaster mounted)
    - .planning/audits/ux-polish-2026-04-14/README.md (K2 description)
  </read_first>
  <files>src/app/admin/layout.tsx</files>
  <action>
    In `src/app/admin/layout.tsx`:
    - Add `import { Toaster } from "@/components/ui/toaster";` to imports (alongside the existing admin layout imports).
    - Inside the returned JSX tree (currently around lines 30-39), mount `<Toaster />` as the last child of the outermost `<div>` (or inside `<main>` — whichever wraps the content root). The Toaster must render once and at the root of admin layout content so it's present for every admin route.

    Do NOT remove or modify any existing inline `setSuccess/setError` banners in admin child pages — that migration belongs to plan 39-05. This plan only mounts the Toaster so downstream banners can migrate.
  </action>
  <verify>
    <automated>grep -E "import \{ Toaster \}" src/app/admin/layout.tsx && grep -E "<Toaster" src/app/admin/layout.tsx</automated>
  </verify>
  <done>
    - `grep` for `import { Toaster }` in `src/app/admin/layout.tsx` returns a match.
    - `grep` for `<Toaster` in `src/app/admin/layout.tsx` returns a match.
    - `pnpm build` exits 0 (no TS import errors introduced).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (K3): Mount TooltipProvider at root layout with delayDuration=250</name>
  <read_first>
    - src/app/layout.tsx
    - src/components/ui/tooltip.tsx
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (finding at lines 284-289 — no TooltipProvider at root)
  </read_first>
  <files>src/app/layout.tsx</files>
  <action>
    In `src/app/layout.tsx`:
    - Add `import { TooltipProvider } from "@/components/ui/tooltip";` alongside existing imports.
    - Wrap `{children}` inside the body with `<TooltipProvider delayDuration={250} skipDelayDuration={500}>{children}</TooltipProvider>`.
    - If there are already other providers (e.g. theme providers) wrapping `{children}`, place `<TooltipProvider>` inside them (closest to `{children}`) so tooltip config is not blocked by outer context.

    Rationale: 250ms is the luxury-feel sweet spot per audit (Radix default is 700ms — too slow; 0ms — too trigger-happy).
  </action>
  <verify>
    <automated>grep -E "TooltipProvider" src/app/layout.tsx && grep -E "delayDuration=\{?250" src/app/layout.tsx</automated>
  </verify>
  <done>
    - `grep` for `TooltipProvider` in `src/app/layout.tsx` returns a match.
    - `grep` for `delayDuration=\{?250\}?` matches (Next.js JSX accepts both quoted/braced).
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (K4): Change Button defaultVariants.variant to 'gold-solid'</name>
  <read_first>
    - src/components/ui/button.tsx
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (finding A-ui/button.tsx at lines 56-61)
    - .planning/audits/ux-polish-2026-04-14/README.md (K4 description — re-colors ~9 primary CTAs)
  </read_first>
  <files>src/components/ui/button.tsx</files>
  <action>
    In `src/components/ui/button.tsx` at lines 35-38:
    - Change `defaultVariants.variant` from `"default"` to `"gold-solid"`.
    - The final object should read:
      ```ts
      defaultVariants: {
        variant: "gold-solid",
        size: "default",
      }
      ```
    - Do NOT rename or remove the `default` variant — existing consumers explicitly using `variant="default"` must still work (they'll opt in to tan). This change only affects buttons that omit `variant`.
    - Do NOT change any other variant definitions in this task (gold/gold-solid/ghost/destructive token-ization is task 7 / K7).
  </action>
  <verify>
    <automated>grep -E 'variant:\s*"gold-solid"' src/components/ui/button.tsx</automated>
  </verify>
  <done>
    - `grep` for `variant: "gold-solid"` inside `defaultVariants` block in `src/components/ui/button.tsx` matches.
    - `pnpm build` exits 0.
    - All 9 submit buttons listed in audit 05 C-1 (create-list-dialog, persona-form-dialog, tenants/new, users/new, settings pages, confirm-tenant, set-password, invite-dialog) — when visually inspected — now render gold without those files being edited.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5 (K5): Restyle Toast + Tooltip + DropdownMenu Content to --bg-floating-elevated; serif toast title</name>
  <read_first>
    - src/components/ui/toast.tsx
    - src/components/ui/tooltip.tsx
    - src/components/ui/dropdown-menu.tsx
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (Toasts section lines 196-231; Tooltip lines 272-283; DropdownMenu lines 293-318)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/toast.tsx, ui/tooltip.tsx, ui/dropdown-menu.tsx sections)
  </read_first>
  <files>src/components/ui/toast.tsx, src/components/ui/tooltip.tsx, src/components/ui/dropdown-menu.tsx</files>
  <action>
    Three coordinated changes across floating-surface primitives:

    **A. `src/components/ui/toast.tsx`:**
    - At lines 27-41 (variants), update `default` variant from `"border bg-background text-foreground"` to include `--bg-floating-elevated` and `--border-default`:
      ```
      default: "border bg-[var(--bg-floating-elevated)] border-[var(--border-default)] text-[var(--text-primary-ds)] shadow-[var(--shadow-lg,0_4px_16px_rgba(0,0,0,0.4))]"
      ```
      If `--shadow-lg` is not defined, fall back to `shadow-2xl`.
    - At lines 91-100 (`ToastTitle`), prepend `font-serif font-semibold tracking-[0.015em]` to the className so toast titles use Cormorant Garamond.
    - Do NOT remove the `destructive` variant; leave it as-is for now.
    - Drop any `bg-primary` references in the default variant.

    **B. `src/components/ui/tooltip.tsx`:**
    - At lines 22-24, replace `"... rounded-md bg-primary px-3 py-1.5 text-xs text-primary-foreground ..."` with:
      ```
      "z-50 overflow-hidden rounded-[8px] bg-[var(--bg-floating-elevated)] border border-[var(--border-default)] px-3 py-1.5 text-xs text-[var(--text-primary-ds)] shadow-[var(--shadow-md,0_2px_8px_rgba(0,0,0,0.3))] animate-in fade-in-0 zoom-in-95 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2"
      ```
      Preserve existing animation classes. Drop `bg-primary` and `text-primary-foreground`.

    **C. `src/components/ui/dropdown-menu.tsx`:**
    - At lines 67-72 (DropdownMenuContent), replace `bg-popover p-1 text-popover-foreground shadow-md` with `bg-[var(--bg-floating)] text-[var(--text-primary-ds)] border-[var(--border-default)] shadow-[var(--shadow-lg,0_4px_16px_rgba(0,0,0,0.4))]`. Change `rounded-md` to `rounded-[8px]` (per audit note that this project's `rounded-md` is 14px, not 8px).
    - At line 50 (`DropdownMenuSubContent`), apply same swap: `bg-[var(--bg-floating)]`, `rounded-[8px]`, explicit border + shadow.
    - At lines 87, 103, 127 (DropdownMenuItem, CheckboxItem, RadioItem), replace `focus:bg-accent focus:text-accent-foreground` with `focus:bg-[var(--gold-bg)] focus:text-[var(--gold-primary)] hover:bg-[var(--gold-bg)] hover:text-[var(--gold-primary)]`.
  </action>
  <verify>
    <automated>grep -E "var\(--bg-floating-elevated\)" src/components/ui/toast.tsx && grep -E "font-serif" src/components/ui/toast.tsx && grep -E "var\(--bg-floating-elevated\)" src/components/ui/tooltip.tsx && grep -E "var\(--bg-floating\)" src/components/ui/dropdown-menu.tsx && grep -E "var\(--gold-bg\)" src/components/ui/dropdown-menu.tsx</automated>
  </verify>
  <done>
    - `grep -E "var\(--bg-floating-elevated\)"` matches in both `toast.tsx` and `tooltip.tsx`.
    - `grep -E "var\(--bg-floating\)"` matches in `dropdown-menu.tsx`.
    - `grep -E "var\(--gold-bg\)"` matches in `dropdown-menu.tsx` (at least 3 times, one per Item/CheckboxItem/RadioItem).
    - `grep -E "font-serif"` in `toast.tsx` matches (on ToastTitle).
    - `grep -E "bg-primary"` in `tooltip.tsx` returns 0 matches.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6 (K6): Input/Textarea/Select focus shadow -> var(--gold-bg-strong)</name>
  <read_first>
    - src/components/ui/input.tsx
    - src/components/ui/textarea.tsx
    - src/components/ui/select.tsx
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/input.tsx lines 93-98, ui/textarea.tsx lines 109-114, ui/select.tsx lines 124-129)
  </read_first>
  <files>src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/select.tsx</files>
  <action>
    Three primitives share the same hard-coded rgba focus shadow. Replace the raw rgba in each:

    **A. `src/components/ui/input.tsx` at line 11:**
    - Replace `focus-visible:shadow-[0_0_0_3px_rgba(212,175,55,0.15)]` with `focus-visible:shadow-[0_0_0_3px_var(--gold-bg-strong)]`.

    **B. `src/components/ui/textarea.tsx` at line 12:**
    - Replace `focus-visible:shadow-[0_0_0_3px_rgba(212,175,55,0.15)]` with `focus-visible:shadow-[0_0_0_3px_var(--gold-bg-strong)]`.

    **C. `src/components/ui/select.tsx` at line 22 (SelectTrigger):**
    - Replace `focus:shadow-[0_0_0_3px_rgba(212,175,55,0.15)]` with `focus:shadow-[0_0_0_3px_var(--gold-bg-strong)]`.

    Do NOT introduce new background colors, borders, or hover states in this task — that's scope for plan 39-04. This task is a strict find-and-replace of the focus shadow value.
  </action>
  <verify>
    <automated>grep -E "var\(--gold-bg-strong\)" src/components/ui/input.tsx && grep -E "var\(--gold-bg-strong\)" src/components/ui/textarea.tsx && grep -E "var\(--gold-bg-strong\)" src/components/ui/select.tsx && ! grep -E "rgba\(212,175,55,0.15\)" src/components/ui/input.tsx src/components/ui/textarea.tsx src/components/ui/select.tsx</automated>
  </verify>
  <done>
    - `grep -E "var\(--gold-bg-strong\)"` matches in all three files.
    - `grep -E "rgba\(212,175,55,0.15\)"` returns 0 matches in all three files.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7 (K7): Badge + Button gold variants -> var(--gold-*) tokens</name>
  <read_first>
    - src/components/ui/badge.tsx
    - src/components/ui/button.tsx
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/button.tsx gold/gold-solid findings lines 77-82; ui/badge.tsx gold/gold-solid findings lines 187-191)
  </read_first>
  <files>src/components/ui/button.tsx, src/components/ui/badge.tsx</files>
  <action>
    Detokenize both `gold` and `gold-solid` variants in Button and Badge so tenant-theme override flows through.

    **A. `src/components/ui/button.tsx` at lines 23-26 (gold/gold-solid variants):**
    - Replace hard-coded `bg-[rgba(212,175,55,0.1)]`, `border-[rgba(212,175,55,0.3)]`, `text-[#d4af37]`, `hover:bg-[rgba(212,175,55,0.15)]`, and the gold-solid `bg-[#d4af37]`, `text-[#0a0a0a]`, `shadow-[0_0_20px_rgba(212,175,55,0.2)]` with CSS-var equivalents:
      ```
      gold: "bg-[var(--gold-bg)] border border-[var(--border-gold)] text-[var(--gold-primary)] font-semibold rounded-[8px] hover:bg-[var(--gold-bg-strong)]"
      "gold-solid": "bg-[var(--gold-primary)] text-[var(--gold-foreground,#0a0a0a)] font-bold rounded-[8px] shadow-[0_0_20px_var(--gold-bg-strong)] hover:bg-[var(--gold-bright)]"
      ```
      If `--gold-foreground` is not defined in globals.css (check before replacement), keep a fallback literal `#0a0a0a` as shown via the CSS var fallback syntax.

    **B. `src/components/ui/badge.tsx` at lines 24-27 (gold/gold-solid variants):**
    - Replace the same rgba/hex literals with:
      ```
      gold: "bg-[var(--gold-bg)] text-[var(--gold-primary)] border-[var(--border-gold)]"
      "gold-solid": "border-transparent bg-[var(--gold-primary)] text-[var(--gold-foreground,#0a0a0a)] font-bold shadow-[0_0_10px_var(--gold-bg-strong)]"
      ```

    Do NOT touch `success/warning/info/destructive` variants — those are scope for plan 39-04 or 39-09.
  </action>
  <verify>
    <automated>grep -E "rgba\(212,175,55" src/components/ui/button.tsx src/components/ui/badge.tsx; test $? -ne 0 && grep -E "var\(--gold-primary\)" src/components/ui/button.tsx src/components/ui/badge.tsx</automated>
  </verify>
  <done>
    - `grep -RE "rgba\(212,175,55" src/components/ui/button.tsx src/components/ui/badge.tsx` returns 0 matches.
    - `grep -RE "#d4af37" src/components/ui/button.tsx src/components/ui/badge.tsx` returns 0 matches.
    - `grep -E "var\(--gold-primary\)"` matches in both files.
    - `pnpm build` exits 0.
    - Visual spot-check: primary CTAs still render gold (unchanged appearance, but now tenant-theme aware).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8 (K8): EnrichmentStatusDots animate-pulse + rollout into search + list-member tables</name>
  <read_first>
    - src/components/ui/enrichment-status-dots.tsx
    - src/app/[orgId]/search/components/prospect-results-table.tsx
    - src/app/[orgId]/lists/components/list-member-table.tsx
    - .planning/audits/ux-polish-2026-04-14/03-agent-workflow.md (findings at lines 60-75 and lines 290-295)
  </read_first>
  <files>src/components/ui/enrichment-status-dots.tsx, src/app/[orgId]/search/components/prospect-results-table.tsx, src/app/[orgId]/lists/components/list-member-table.tsx</files>
  <action>
    Three coordinated changes:

    **A. `src/components/ui/enrichment-status-dots.tsx` at lines 20-33:**
    - In the `getDotStyle` or equivalent branching for `status === "in_progress"`, add `animate-pulse` to the className of the outer wrapper span. The colored dot's boxShadow keeps working because `animate-pulse` only modulates opacity.
    - Also add a slower breathe effect for `status === "pending"`: apply `animate-pulse` with lower intensity is acceptable since no custom `animate-breathe` exists today (do NOT add new keyframes — that's out of scope).
    - Implementation pattern (applied inline where the dot is rendered):
      ```tsx
      const dotClass = cn("h-2.5 w-2.5 rounded-full transition-shadow", status === "in_progress" && "animate-pulse", status === "pending" && "animate-pulse opacity-60")
      ```
    - Preserve the existing `return null` behavior when `sourceStatus === null` so consumers can render fallback text.

    **B. `src/app/[orgId]/search/components/prospect-results-table.tsx` at lines 289-310:**
    - Replace the block that renders three static `rgba(255,255,255,0.15)` circles + "Not enriched" text with:
      ```tsx
      import { EnrichmentStatusDots } from "@/components/ui/enrichment-status-dots";
      // ...inside the cell render:
      const status = prospect.enrichment_source_status ?? null;
      return status
        ? <EnrichmentStatusDots sourceStatus={status} />
        : <span className="text-sm" style={{ color: "var(--text-ghost)" }}>Not enriched</span>;
      ```
    - Verify the ApolloPerson shape actually has `enrichment_source_status`. If not, use the existing `prospect._savedSearchMeta?.enrichment_source_status` or whichever field the saved-search path already populates (grep the file for the existing dots rendering to find the correct prop path — do NOT invent new fields).

    **C. `src/app/[orgId]/lists/components/list-member-table.tsx` at lines 65-102:**
    - Remove the local `EnrichmentDot` component.
    - Replace its usage at line 65 with:
      ```tsx
      import { EnrichmentStatusDots } from "@/components/ui/enrichment-status-dots";
      // ...
      <EnrichmentStatusDots sourceStatus={member.prospect?.enrichment_source_status ?? null} />
      ```
    - If `enrichment_source_status` is not currently fetched by the lists query, fall back to `null` — the "Replace setTimeout spinner" finding (audit 03 line 297) is its own fix in plan 39-07.

    Do NOT modify the query layer or add new fields to Supabase tables — polish only.
  </action>
  <verify>
    <automated>grep -E "animate-pulse" src/components/ui/enrichment-status-dots.tsx && grep -E "EnrichmentStatusDots" src/app/[orgId]/search/components/prospect-results-table.tsx && grep -E "EnrichmentStatusDots" src/app/[orgId]/lists/components/list-member-table.tsx</automated>
  </verify>
  <done>
    - `grep` for `animate-pulse` matches in `enrichment-status-dots.tsx`.
    - `grep` for `EnrichmentStatusDots` matches in both `prospect-results-table.tsx` AND `list-member-table.tsx`.
    - Local `EnrichmentDot` function is removed from `list-member-table.tsx` (not just renamed): `grep -n "function EnrichmentDot" src/app/[orgId]/lists/components/list-member-table.tsx` returns 0 matches.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9 (K9): Create src/app/not-found.tsx matching global-error chrome</name>
  <read_first>
    - src/app/global-error.tsx
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (finding at lines 404-409 — No not-found.tsx anywhere)
  </read_first>
  <files>src/app/not-found.tsx</files>
  <action>
    Create a new file `src/app/not-found.tsx`. It must:
    - Be a React Server Component (no `"use client"`).
    - Mirror the layout/chrome of `src/app/global-error.tsx` (serif heading, centered card, gold CTA).
    - Use `font-serif` (Cormorant Garamond) with `font-semibold` (NOT `font-bold` — per audit 04 line 401 the rest of the app uses `font-semibold` on serif headings).
    - Render:
      - Serif H1 "404" (e.g. `<h1 className="font-serif text-6xl font-semibold text-[var(--text-primary-ds)]">404</h1>`).
      - A subdued body paragraph (use `text-muted-foreground` or `var(--text-secondary-ds)`): "This page doesn't exist or has moved."
      - A gold CTA link to `/`: `<Link href="/" className="inline-flex items-center rounded-[8px] bg-[var(--gold-primary)] px-4 py-2 text-sm font-bold text-[var(--gold-foreground,#0a0a0a)] hover:bg-[var(--gold-bright)] transition-colors">Return home</Link>`. Alternatively use `<Button variant="gold-solid" asChild><Link href="/">Return home</Link></Button>`.
    - Use the same outer wrapper classes as `global-error.tsx` (flex + min-h-screen + centered) so the background matches.

    Do NOT create `src/app/[orgId]/not-found.tsx` in this task — scope is the root-level fix only (covers every unmatched route, including tenant bad-URL).
  </action>
  <verify>
    <automated>test -f src/app/not-found.tsx && grep -E "404" src/app/not-found.tsx && grep -E "font-serif" src/app/not-found.tsx && grep -E "Return home" src/app/not-found.tsx</automated>
  </verify>
  <done>
    - File `src/app/not-found.tsx` exists.
    - Contains `404` text, `font-serif` class, and a CTA linking to `/`.
    - No `font-bold` on the H1 (uses `font-semibold` per audit).
    - `pnpm build` exits 0.
    - Manual verify: navigate to `/nonexistent-route-xyz` → see branded 404 page (not Next.js dev default).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 10 (K10): Checkbox checked state -> gold; table selected row -> bg-gold-bg</name>
  <read_first>
    - src/components/ui/checkbox.tsx
    - src/components/ui/table.tsx
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/checkbox.tsx lines 146-152; ui/table.tsx line 61 selected-row state C-2 at lines 480-481)
  </read_first>
  <files>src/components/ui/checkbox.tsx, src/components/ui/table.tsx</files>
  <action>
    **A. `src/components/ui/checkbox.tsx` at line 16 (base className):**
    - Replace `border-primary ... data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground` with:
      ```
      border-[var(--border-subtle)] hover:border-[var(--gold-primary)]/60 data-[state=checked]:bg-[var(--gold-primary)] data-[state=checked]:border-[var(--gold-primary)] data-[state=checked]:text-[var(--gold-foreground,#0a0a0a)]
      ```
    - Keep all other classes (focus ring, size, transition) intact.
    - If `--gold-foreground` is not defined in globals.css, use the fallback literal via CSS var fallback syntax.

    **B. `src/components/ui/table.tsx` at line 61 (TableRow base):**
    - The current class likely contains `data-[state=selected]:bg-muted` (per audit line 480). Replace with `data-[state=selected]:bg-[var(--gold-bg)]`.
    - Preserve existing hover-gold class (row-hover-gold or equivalent). Just the selected-row swap.
  </action>
  <verify>
    <automated>grep -E "data-\[state=checked\]:bg-\[var\(--gold-primary\)\]" src/components/ui/checkbox.tsx && grep -E "data-\[state=selected\]:bg-\[var\(--gold-bg\)\]" src/components/ui/table.tsx</automated>
  </verify>
  <done>
    - `grep` for `data-[state=checked]:bg-[var(--gold-primary)]` matches in `checkbox.tsx`.
    - `grep` for `data-[state=selected]:bg-[var(--gold-bg)]` matches in `table.tsx`.
    - `grep -E "data-\[state=checked\]:bg-primary" src/components/ui/checkbox.tsx` returns 0 matches.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11: Manual verification — keystone cascade</name>
  <what-built>
    All 10 keystones (K1-K10) merged. Expected cascade:
    - Every toast auto-dismisses in ~5s, up to 3 stack.
    - Admin screens can now fire toasts (Toaster mounted).
    - Tooltips appear globally after 250ms delay.
    - Primary CTAs (Create List, Save Search, Invite User, every auth/onboarding submit) now render gold.
    - Toasts, Tooltips, Dropdown menus all read on `--bg-floating-elevated` (darker, premium feel).
    - Input/Textarea/Select focus rings respect tenant theme (test with non-gold tenant to confirm).
    - Gold button/badge variants respect tenant theme.
    - Enrichment dots pulse while in_progress. Search table + list-member table render EnrichmentStatusDots primitive (not static stubs).
    - Navigating to `/nonexistent-route` shows branded 404, not Next.js default.
    - Checkbox checked state renders gold. Selected table rows render with `bg-gold-bg` tint.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and execute the following spot-checks in the browser:

    1. **Toasts (K1, K2, K5):**
       - Visit any tenant page that fires a toast (e.g. `/[orgId]/lists` → create a list). Confirm toast auto-dismisses in ~5s.
       - Trigger multiple toasts quickly (e.g. enrich multiple prospects). Up to 3 should stack.
       - Visit `/admin/tenants` and toggle a tenant's status. Even if the admin page hasn't been migrated to toast yet (plan 39-05), the Toaster DOM outlet should be present (inspect element — look for `[role="region"]` toast viewport).
       - Toast title should render in Cormorant Garamond (serif) not DM Sans.

    2. **TooltipProvider (K3):**
       - Hover over any `<Tooltip>`-wrapped element (e.g. admin sidebar collapse toggle, any existing tooltip in the app). Tooltip should appear after 250ms.

    3. **Button default variant (K4):**
       - Open `/[orgId]/lists` → "Create List" dialog. The submit button should render GOLD (not tan).
       - Open `/[orgId]/settings`. "Save changes" should render gold.
       - Visit `/login`. Sign-in button should render gold.
       - Visit `/admin/tenants/new`. Create button should render gold.

    4. **Floating surface restyle (K5):**
       - Any tooltip/toast/dropdown-menu should appear on `var(--bg-floating-elevated)` surface. Compare against `bg-primary` which is tan/beige — if you see tan, the fix didn't land.

    5. **Focus shadow tokenization (K6) + gold tokenization (K7) — verify tenant-theme safe:**
       - Set a test tenant to sapphire theme in the theme picker. Focus on any Input/Textarea/Select — the focus ring should reflect sapphire tint (not gold). If it stays gold, the rgba literal survived somewhere.
       - The gold variant button should also switch to sapphire-tinted for that tenant.

    6. **EnrichmentStatusDots (K8):**
       - Visit `/[orgId]/search`, trigger a bulk enrich. Rows in progress should show pulsing dots (not static).
       - Visit `/[orgId]/lists/[id]`. The enrichment column should use the primitive, showing per-source breakdown for enriched rows.

    7. **404 page (K9):**
       - Navigate to `/does-not-exist-xyz`. Expect branded page with serif "404" heading and gold "Return home" CTA. NOT the Next.js dev-default.

    8. **Checkbox + table selected (K10):**
       - Visit `/[orgId]/search`, select a prospect checkbox. Checkbox should fill GOLD. Entire row should get `bg-gold-bg` tint.

    Leave notes on any regressions found. Common regression: Button defaultVariants change (K4) may cause buttons that were implicitly tan (i.e. `variant` unset but needed to be neutral) to now read gold. Flag those — they need to opt in to `variant="default"` or the button should be reconsidered.
  </how-to-verify>
  <resume-signal>Type "approved" (or list specific regressions if the cascade revealed any buttons that should have been `variant="default"` / `variant="ghost"` / `variant="outline"` but were untyped)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All edits are UI-layer primitive refactors — no new endpoints, no new auth paths, no new user input sinks, no new external calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | Toast duration reduction (5s from 16min) | accept | No security impact — toast content is client-generated UI feedback, not persisted. Shorter display window is strictly better for UX and has no auth/authz implication. |
| T-39-01-02 | Information Disclosure | 404 page (K9) | accept | Page content is static serif text + CTA to `/`. No route enumeration risk (Next.js already handles this — custom page only replaces the chrome). |
| T-39-01-03 | Denial of Service | Toast limit increased 1->3 | accept | Max 3 concurrent DOM nodes. No amplification vector. |
| T-39-01-04 | Spoofing | Branded 404 page mimics real layout | accept | Users hitting a 404 see branded chrome — this is intentional UX. No phishing vector since the page is served from our domain. |

No new attack surface. UI-only polish reusing existing primitives.
</threat_model>

<verification>
## Phase-level verification

Run at end of plan execution (executor):

1. **Build passes:** `pnpm build` exits 0.
2. **Grep audit:**
   ```
   grep -RE "TOAST_REMOVE_DELAY = 5_000" src/hooks/use-toast.ts       # 1 match
   grep -RE "TOAST_LIMIT = 3" src/hooks/use-toast.ts                  # 1 match
   grep -RE "<Toaster" src/app/admin/layout.tsx                       # >=1 match
   grep -RE "TooltipProvider" src/app/layout.tsx                      # >=1 match
   grep -RE 'variant: "gold-solid"' src/components/ui/button.tsx      # 1 match
   grep -RE "var\(--bg-floating-elevated\)" src/components/ui/toast.tsx src/components/ui/tooltip.tsx  # >=1 each
   grep -RE "var\(--gold-bg-strong\)" src/components/ui/input.tsx src/components/ui/textarea.tsx src/components/ui/select.tsx  # 1 each
   grep -RE "rgba\(212,175,55" src/components/ui/button.tsx src/components/ui/badge.tsx                 # 0 matches
   grep -RE "animate-pulse" src/components/ui/enrichment-status-dots.tsx                                 # >=1 match
   test -f src/app/not-found.tsx                                                                          # exists
   grep -RE "data-\[state=checked\]:bg-\[var\(--gold-primary\)\]" src/components/ui/checkbox.tsx       # 1 match
   grep -RE "data-\[state=selected\]:bg-\[var\(--gold-bg\)\]" src/components/ui/table.tsx             # 1 match
   ```
3. **Manual browser verification** per Task 11 above.
</verification>

<success_criteria>
- [ ] All 10 keystone edits (K1-K10) applied atomically per their dedicated task.
- [ ] `pnpm build` exits 0.
- [ ] All grep checks in `<verification>` return expected matches.
- [ ] Manual browser verification (Task 11) passes all 8 spot-check points.
- [ ] No regressions: buttons that should be neutral/ghost/outline still render correctly (flag any revealed during Task 11).
- [ ] Tenant theme switch (sapphire/emerald) correctly cascades to input focus, gold button/badge (K6/K7 verified).
</success_criteria>

<output>
After completion, create `.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-01-SUMMARY.md` summarizing:
- All 10 keystones shipped (list with brief before/after).
- Any regressions discovered in Task 11 (e.g. buttons that should revert to `variant="default"`).
- Closed finding count (target: ~60 of 242 — manual count against audit).
- Screenshots / notes from the manual verification step if available.
- Any findings that surfaced during implementation that belong in later plans (e.g. "foo button was gold-styled inline, now redundant — flagged for 39-04 cleanup").
</output>
