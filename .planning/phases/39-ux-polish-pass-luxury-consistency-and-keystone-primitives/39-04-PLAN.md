---
phase: 39-ux-polish-pass-luxury-consistency-and-keystone-primitives
plan: 04
type: execute
wave: 4
depends_on:
  - 39-01
files_modified:
  - src/components/ui/wealth-tier-badge.tsx
  - src/components/ui/tag-input.tsx
  - src/components/ui/loader.tsx
  - src/components/ui/confirmation.tsx
  - src/components/ui/enrichment-status-dots.tsx
  - src/components/ui/table.tsx
  - src/components/ui/shimmer.tsx
  - src/components/ui/logo-upload.tsx
autonomous: false
requirements:
  - PHASE-39-T3-THEME-LEAK

must_haves:
  truths:
    - "Setting a tenant to sapphire theme causes all gold-tinted primitives to switch to sapphire-tinted"
    - "No rgba(212,175,55,*) literals remain in src/components/ui/ (primitives)"
    - "No #d4af37 literals remain in src/components/ui/ (primitives)"
    - "WealthTierBadge tiers 1-2 use var(--gold-*) tokens (tiers 3-4 can keep bespoke darker-gold hex per audit)"
    - "TagInput chips and dropdown background use CSS vars, not hex"
    - "Loader spinner border uses var(--gold-text)"
    - "EnrichmentStatusDots glows use utility classes (.dot-glow-*) or CSS vars"
    - "Confirmation border/bg use --destructive token (not raw rgba red)"
    - "Table header gold text uses text-gold-text class"
  artifacts:
    - path: "src/components/ui/wealth-tier-badge.tsx"
      provides: "tier-1 and tier-2 use var(--gold-*) tokens"
      contains: "var(--gold"
    - path: "src/components/ui/tag-input.tsx"
      provides: "chip + dropdown use CSS vars"
      contains: "var(--gold"
    - path: "src/components/ui/loader.tsx"
      provides: "spinner uses var(--gold-text)"
      contains: "var(--gold-text)"
    - path: "src/components/ui/confirmation.tsx"
      provides: "destructive styles use token (border-destructive/20, bg-destructive/*)"
      contains: "border-destructive"
    - path: "src/components/ui/enrichment-status-dots.tsx"
      provides: "uses .dot-glow-* utilities or CSS vars"
      contains: "dot-glow-"
    - path: "src/components/ui/table.tsx"
      provides: "gold text uses text-gold-text token"
      contains: "text-gold-text"
    - path: "src/components/ui/shimmer.tsx"
      provides: "ShimmerLine/Block/Card use bg tokens (bg-bg-elevated or bg-border-subtle)"
      contains: "bg-bg-elevated"
    - path: "src/components/ui/logo-upload.tsx"
      provides: "error text uses text-destructive"
      contains: "text-destructive"
  key_links:
    - from: "src/components/ui/* primitives"
      to: "src/lib/tenant-theme.ts (per-tenant --gold-* overrides)"
      via: "CSS cascade: when tenant layout injects --gold-primary override, primitives consume the overridden variable"
      pattern: "var\\(--gold-"
    - from: "final verification grep"
      to: "entire src/components/ui/ tree"
      via: "grep must return 0 matches for rgba(212,175,55) and #d4af37"
      pattern: "rgba\\(212,175,55|#d4af37"
---

<objective>
Close theme T3 from the audit: eliminate every hard-coded `rgba(212,175,55,*)` and `#d4af37` literal from `src/components/ui/` primitives. Replace with `var(--gold-*)` CSS variables so tenant-theme.ts's per-tenant gold override (sapphire, emerald, rose, amber, slate, violet, coral) flows through correctly.

Context: `src/lib/tenant-theme.ts:52-57` rewrites `--gold-*` CSS variables per tenant at the layout level. Any primitive that uses hard-coded hex bypasses this override, meaning non-gold tenants still get gold buttons/badges/focus rings — a luxury-consistency break.

Note: Plan 39-01 (K4, K6, K7) already detokenized `button.tsx`, `input.tsx`, `textarea.tsx`, `select.tsx`, `badge.tsx`, `checkbox.tsx`, `enrichment-status-dots.tsx`'s in-progress state. This plan mops up the REMAINING primitives per audit 05 section A.

Output: 8 primitives modified. After merge, grep `rgba(212,175,55|#d4af37` in `src/components/ui/` returns 0 matches. Tenant theme switch verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-CONTEXT.md
@.planning/audits/ux-polish-2026-04-14/05-design-system.md
@src/lib/tenant-theme.ts
@src/app/globals.css
@tailwind.config.ts

<interfaces>
Gold CSS variables (dark mode — from globals.css):
- `--gold-primary` = `#d4af37`
- `--gold-bright` = `#f0d060`
- `--gold-text` = `rgba(212,175,55,0.7)`
- `--gold-bg` = `rgba(212,175,55,0.08)`
- `--gold-bg-strong` = `rgba(212,175,55,0.15)`
- `--border-gold` = `rgba(212,175,55,0.25)`

Tailwind utility aliases (from `tailwind.config.ts`):
- `bg-gold-bg`, `bg-gold-bg-strong`
- `text-gold-primary`, `text-gold-bright`, `text-gold-text`
- `border-border-gold`

Dot-glow utilities (from globals.css:336-347):
- `.dot-glow-green`, `.dot-glow-blue`, `.dot-glow-red`, `.dot-glow-amber`

Destructive tokens (from globals.css — existing):
- `--destructive` (oklch)
- Tailwind aliases: `bg-destructive`, `text-destructive`, `border-destructive` (can do `/10`, `/20`, `/5` opacity)

Success/warning/info tokens (existing, Tailwind aliased):
- `bg-success-muted`, `text-success`, `border-success/30`
- `bg-warning-muted`, `text-warning`, `border-warning/30`
- `bg-info-muted`, `text-info`, `border-info/30`

Per-tenant override mechanism (from `src/lib/tenant-theme.ts` lines 52-57):
```typescript
// Injected into [orgId]/layout.tsx as a <style> tag:
// :root { --gold-primary: <tenant-color>; --gold-bright: ...; --gold-bg: ...; ... }
// So any primitive using var(--gold-*) gets the tenant's color at render time.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WealthTierBadge — tokenize tier-1 and tier-2, keep tier-3/tier-4 bespoke (document)</name>
  <read_first>
    - src/components/ui/wealth-tier-badge.tsx (focus lines 10-37)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/wealth-tier-badge.tsx finding at lines 202-208)
  </read_first>
  <files>src/components/ui/wealth-tier-badge.tsx</files>
  <action>
    Per audit lines 207-208, the four tiers use gold-family hues. Replace hard-coded `rgba(212,175,55,*)` and primary `#f0d060`/`#d4af37` for tier-1 and tier-2 with CSS vars. Tiers 3 and 4 use `#c4a030` / `#a08828` (darker gold shades) — these do NOT exist as CSS variables and ARE intentional value-tier signals. Keep those hex literals but add an inline comment flagging them as "tier-3/tier-4 bespoke shades — not theme-aware by design".

    Implementation (for the tier style map):
    ```ts
    // Tier 1 ($500M+):
    { bg: "var(--gold-bg-strong)", border: "rgba(212,175,55,0.6)", text: "var(--gold-bright)" }
    // Tier 2 ($100M+):
    { bg: "var(--gold-bg-strong)", border: "var(--border-gold)", text: "var(--gold-primary)" }
    // Tier 3 ($50M+) — bespoke tier shade, intentional (NOT theme-aware):
    { bg: "var(--gold-bg)", border: "var(--border-gold)", text: "#c4a030" /* tier-3 value signal */ }
    // Tier 4 ($30M+) — bespoke tier shade, intentional (NOT theme-aware):
    { bg: "var(--gold-bg)", border: "var(--border-gold)", text: "#a08828" /* tier-4 value signal */ }
    ```

    The border for tier-1 keeps an `rgba(212,175,55,0.6)` literal because there's no `--border-gold-strong` token and 0.6 opacity is between the existing 0.25 and 1.0 gold values. Add a comment: `// tier-1: intensified border, no token for 0.6 opacity exists`.

    Alternative acceptable: change tier-1 border to `var(--gold-primary)` (fully opaque gold) — that IS theme-aware. Use this if the visual difference is acceptable.
  </action>
  <verify>
    <automated>grep -E "var\(--gold-bg-strong\)" src/components/ui/wealth-tier-badge.tsx && grep -E "var\(--gold-bright\)" src/components/ui/wealth-tier-badge.tsx</automated>
  </verify>
  <done>
    - Tier-1 and tier-2 styles use `var(--gold-*)` tokens.
    - Tier-3 and tier-4 keep `#c4a030` / `#a08828` with a comment flagging intentionality.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TagInput — tokenize chip + dropdown + suggestion hover</name>
  <read_first>
    - src/components/ui/tag-input.tsx (focus lines 72-77 chip, 117 dropdown, 131-136 suggestion)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/tag-input.tsx findings at lines 342-355)
  </read_first>
  <files>src/components/ui/tag-input.tsx</files>
  <action>
    **A. Chip style (lines 72-77):**
    - Replace inline `style={{ background: "rgba(212,175,55,0.15)", color: "#d4af37", border: "1px solid rgba(212,175,55,0.3)" }}` with className-based:
      ```tsx
      className="inline-flex items-center gap-1 rounded-md px-2 py-0.5 text-xs font-medium bg-[var(--gold-bg-strong)] text-[var(--gold-primary)] border border-[var(--border-gold)]"
      ```

    **B. Dropdown background (line 117):**
    - Replace `style={{ background: "#1a1a1a" }}` with:
      ```tsx
      className="... bg-[var(--bg-floating-elevated)] border border-[var(--border-default)]"
      ```

    **C. Suggestion button hover (lines 131-136):**
    - Remove `onMouseEnter/Leave` handlers that set inline background.
    - Replace with CSS classes: `className="w-full text-left px-3 py-2 text-sm transition-colors hover:bg-[var(--gold-bg)] hover:text-[var(--gold-primary)]"`.

    Preserve all other component behavior (tag-add on Enter, remove on X click, etc.).
  </action>
  <verify>
    <automated>! grep -E "rgba\(212,175,55|#d4af37|#1a1a1a" src/components/ui/tag-input.tsx && grep -E "var\(--gold-bg-strong\)" src/components/ui/tag-input.tsx</automated>
  </verify>
  <done>
    - `grep -E "rgba\(212,175,55|#d4af37|#1a1a1a"` returns 0 matches in `tag-input.tsx`.
    - `grep -E "var\(--gold-bg-strong\)"` matches.
    - Hover handlers (onMouseEnter/Leave) for suggestions are removed — replaced by CSS hover.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Loader — replace rgba gold literals with var(--gold-text)</name>
  <read_first>
    - src/components/ui/loader.tsx (focus lines 33, 75)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/loader.tsx finding at lines 276-281)
  </read_first>
  <files>src/components/ui/loader.tsx</files>
  <action>
    **A. Spinner border-top (line 33):**
    - Replace `border-t-[rgba(212,175,55,0.7)]` with `border-t-[var(--gold-text)]` (the CSS var `--gold-text` equals `rgba(212,175,55,0.7)` per globals.css — identical visual, theme-aware).

    **B. Pulse variant (line 75):**
    - Replace `bg-[rgba(212,175,55,0.6)]` with `bg-[var(--gold-text)]` (closest match; 0.7 vs 0.6 is negligible but gains theme-awareness). If the visual regression is noticeable, alternative: use `bg-[var(--gold-primary)]/60` (Tailwind opacity modifier).

    Dots variant (other part of Loader) uses `bg-foreground/40` per audit — leave it alone.
  </action>
  <verify>
    <automated>! grep -E "rgba\(212,175,55" src/components/ui/loader.tsx && grep -E "var\(--gold-text\)|var\(--gold-primary\)" src/components/ui/loader.tsx</automated>
  </verify>
  <done>
    - `grep -E "rgba\(212,175,55"` returns 0 matches in `loader.tsx`.
    - Spinner + pulse use CSS vars.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Confirmation — tokenize border/bg/icon colors</name>
  <read_first>
    - src/components/ui/confirmation.tsx (focus lines 38-41, 86-90)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/confirmation.tsx findings at lines 311-330)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (Confirmation section at lines 332-344)
  </read_first>
  <files>src/components/ui/confirmation.tsx</files>
  <action>
    **A. Border + background (lines 38-41):**
    - Replace:
      - `border-[rgba(255,255,255,0.1)] bg-[rgba(255,255,255,0.03)]` → `border-border-subtle bg-bg-elevated`
      - `isDestructive && "border-[rgba(239,68,68,0.2)] bg-[rgba(239,68,68,0.03)]"` → `isDestructive && "border-destructive/20 bg-destructive/5"`

    **B. ConfirmationIcon variants (lines 86-90):**
    - Replace:
      - warning: `"bg-[rgba(234,179,8,0.12)] text-yellow-400"` → `"bg-warning-muted text-warning"`
      - info: `"bg-[rgba(59,130,246,0.12)] text-blue-400"` → `"bg-info-muted text-info"`
      - destructive: `"bg-[rgba(239,68,68,0.12)] text-red-400"` → `"bg-destructive/15 text-destructive"`

    Note: The audit also recommends bumping ConfirmationTitle to `font-serif text-base` but flags as "don't auto-fix" (line 444). SKIP the typography change — keep as `text-sm font-semibold`.
  </action>
  <verify>
    <automated>! grep -E "rgba\(239,68,68|rgba\(234,179,8|rgba\(59,130,246|text-yellow-400|text-blue-400|text-red-400" src/components/ui/confirmation.tsx && grep -E "text-destructive|text-warning|text-info" src/components/ui/confirmation.tsx</automated>
  </verify>
  <done>
    - No hardcoded rgba() red/yellow/blue in `confirmation.tsx`.
    - No `text-yellow-400` / `text-blue-400` / `text-red-400` Tailwind defaults.
    - Uses semantic tokens (`text-destructive`, `text-warning`, `text-info`).
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: EnrichmentStatusDots — use .dot-glow-* utilities or CSS vars for glows</name>
  <read_first>
    - src/components/ui/enrichment-status-dots.tsx (focus lines 20-32)
    - src/app/globals.css (find .dot-glow-green / .dot-glow-blue / .dot-glow-red / .dot-glow-amber definitions at lines 336-347)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/enrichment-status-dots.tsx finding at lines 212-217)
  </read_first>
  <files>src/components/ui/enrichment-status-dots.tsx</files>
  <action>
    Note: Plan 39-01 Task 8 already added `animate-pulse` to `in_progress` in this file. This task adds the glow-utility substitution (distinct concern).

    Verify `.dot-glow-green`, `.dot-glow-blue`, `.dot-glow-red`, `.dot-glow-amber` exist in `src/app/globals.css` around lines 336-347. If they do, replace the inline `style={{ boxShadow: "0 0 6px rgba(34,197,94,0.6)" }}` (and equivalents for blue/red/amber) with `className={cn(..., glowUtility)}` where `glowUtility` maps status → class name.

    Implementation pattern:
    ```tsx
    const glowClass = {
      complete: "dot-glow-green",
      in_progress: "dot-glow-blue",
      failed: "dot-glow-red",
      pending: "dot-glow-amber",
    }[status] ?? "";
    return <span className={cn("h-2.5 w-2.5 rounded-full", glowClass, status === "in_progress" && "animate-pulse")} />;
    ```

    If the utilities do NOT exist in globals.css, fall back to tokenized inline styles using oklch/CSS vars for the glow colors — but do NOT create new utility classes in this plan (new tokens are out of scope).

    The inner dot color can stay `var(--success)` / `var(--info)` / `var(--destructive)` / `var(--warning)` since those are already tokenized per audit line 212.
  </action>
  <verify>
    <automated>! grep -E "rgba\(34,197,94|rgba\(96,165,250|rgba\(239,68,68|rgba\(234,179,8" src/components/ui/enrichment-status-dots.tsx && (grep -E "dot-glow-" src/components/ui/enrichment-status-dots.tsx || grep -E "var\(--success\)" src/components/ui/enrichment-status-dots.tsx)</automated>
  </verify>
  <done>
    - Inline boxShadow rgba literals are gone OR replaced with CSS vars.
    - Uses `.dot-glow-*` utilities (preferred) or CSS vars.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Table header + Shimmer — tokenize</name>
  <read_first>
    - src/components/ui/table.tsx (focus line 76 — column header text color)
    - src/components/ui/shimmer.tsx (focus lines 45, 68, 89, 95, 96)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/table.tsx lines 414-420; ui/shimmer.tsx lines 284-297)
  </read_first>
  <files>src/components/ui/table.tsx, src/components/ui/shimmer.tsx</files>
  <action>
    **A. `src/components/ui/table.tsx` at line 76:**
    - Replace `text-[rgba(212,175,55,0.7)]` with `text-gold-text` (Tailwind alias mapping to `var(--gold-text)` which IS `rgba(212,175,55,0.7)`).

    **B. `src/components/ui/shimmer.tsx`:**
    - At lines 45, 68, 89, 95, 96 — various `rgba(255,255,255,0.04)` / `rgba(255,255,255,0.06)` / `rgba(255,255,255,0.08)` backgrounds + borders.
    - Replace backgrounds `rgba(255,255,255,0.04)` with `bg-bg-elevated` (maps to `--bg-elevated: rgba(255,255,255,0.04)` — exact match).
    - Replace `rgba(255,255,255,0.06)` with `bg-border-subtle` (approximation — audit explicitly flags this as acceptable on line 290).
    - Replace `rgba(255,255,255,0.08)` with `bg-border-default` or retain if nothing close exists — the audit says "removes one more rgba literal site" not "replace all perfectly".
    - Replace the border variants (`border-[rgba(255,255,255,0.06)]`) similarly with `border-border-subtle`.
    - Shimmer animation: the audit recommends replacing `animate-pulse` with `shimmer-skeleton` utility (class already defined in globals.css:491-495). Update `ShimmerLine` and `ShimmerBlock` to use `shimmer-skeleton` instead of `animate-pulse` for consistency with `Skeleton` primitive.
  </action>
  <verify>
    <automated>grep -E "text-gold-text" src/components/ui/table.tsx && grep -E "bg-bg-elevated|bg-border-subtle|shimmer-skeleton" src/components/ui/shimmer.tsx</automated>
  </verify>
  <done>
    - `text-gold-text` used in `table.tsx`.
    - `shimmer.tsx` uses tokenized backgrounds and `shimmer-skeleton` utility.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: LogoUpload — error text uses text-destructive token</name>
  <read_first>
    - src/components/ui/logo-upload.tsx (focus line 232)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/logo-upload.tsx finding at lines 385-390)
  </read_first>
  <files>src/components/ui/logo-upload.tsx</files>
  <action>
    At line 232, replace `style={{ color: "oklch(0.62 0.19 22)" }}` with `className="mt-2 text-xs text-destructive"` (or add `text-destructive` to existing className and delete the inline style).

    Do not touch any other styles in this file.
  </action>
  <verify>
    <automated>! grep -E "oklch\(0\.62 0\.19 22\)" src/components/ui/logo-upload.tsx && grep -E "text-destructive" src/components/ui/logo-upload.tsx</automated>
  </verify>
  <done>
    - Error text uses `text-destructive` (token-based).
    - Inline `oklch(0.62 0.19 22)` literal is gone.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Final sweep — grep src/components/ui/ and src/components/prospect/wealth-tier-badge + search consumers for residual hex/rgba gold literals</name>
  <read_first>
    - src/components/ui/ (directory — grep all files)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (to understand what the target 0-matches grep should look like)
  </read_first>
  <files>
    <!-- Target: no new file modifications — this is a verification + cleanup-of-leftovers task. Only edit files if the grep reveals residual literals from Plan 39-01 K4/K7 that weren't fully caught. -->
  </files>
  <action>
    Run the following greps against `src/components/ui/`:
    ```bash
    grep -RE "rgba\(212,175,55" src/components/ui/
    grep -RE "#d4af37" src/components/ui/
    grep -RE "#f0d060" src/components/ui/      # gold-bright literal
    grep -RE "#c4a030|#a08828" src/components/ui/   # tier-3/tier-4 bespoke (should ONLY appear in wealth-tier-badge with comments)
    ```

    Expected results after prior tasks:
    - `rgba(212,175,55` → 0 matches in `src/components/ui/`.
    - `#d4af37` → 0 matches in `src/components/ui/`.
    - `#f0d060` → 0 matches in `src/components/ui/` (was in button.tsx gold-solid variant pre-K7 — verify).
    - `#c4a030` / `#a08828` → ONLY in `wealth-tier-badge.tsx` with the intentional-comment flag from Task 1.

    If any grep reveals a residual literal, locate it and replace with the appropriate CSS variable:
    - `#f0d060` (gold-bright) → `var(--gold-bright)`
    - `rgba(212,175,55,0.08)` → `var(--gold-bg)`
    - `rgba(212,175,55,0.15)` → `var(--gold-bg-strong)`
    - `rgba(212,175,55,0.25)` → `var(--border-gold)`
    - `rgba(212,175,55,0.7)` → `var(--gold-text)`

    Also grep the consumer-side files flagged in audit 05 line 202-208 for wealth-tier-badge (even though the primitive is the ui/ version — verify the audit's file path):
    - `src/app/[orgId]/search/components/wealth-tier-badge.tsx` (if exists — there appears to be a duplicate per the ls output earlier). If duplicate exists, apply the same fix or deprecate the duplicate in favor of the ui/ version (raise this as a question for the reviewer, do not delete silently).

    This task is primarily a VERIFICATION step. If the grep is clean, no file modifications needed. Record the grep output in the summary.
  </action>
  <verify>
    <automated>set -o pipefail; ! grep -RE "rgba\(212,175,55|#d4af37|#f0d060" src/components/ui/</automated>
  </verify>
  <done>
    - `grep -RE "rgba\(212,175,55|#d4af37|#f0d060" src/components/ui/` returns 0 matches (empty output, non-zero exit code from grep, which is the success signal).
    - `grep -RE "#c4a030|#a08828" src/components/ui/` returns matches ONLY in `wealth-tier-badge.tsx` with intentional comments.
    - If `src/app/[orgId]/search/components/wealth-tier-badge.tsx` duplicate exists, it's flagged in the SUMMARY for reviewer attention (not silently modified).
    - `pnpm build` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Tenant-theme end-to-end verification — switch to sapphire, confirm cascade</name>
  <what-built>
    All 8 primitives detokenized. Grep across `src/components/ui/` returns 0 matches for `rgba(212,175,55|#d4af37|#f0d060`.
  </what-built>
  <how-to-verify>
    1. Confirm `src/lib/tenant-theme.ts` defines 8 themes (gold, sapphire, emerald, rose, amber, slate, violet, coral) per the project state from ROADMAP Phase 16 deliverables.
    2. Log in as a tenant_admin for a test tenant.
    3. Go to `/[orgId]/settings/organization`.
    4. Use the `<ThemePicker>` to switch the tenant theme to **sapphire**. Save changes.
    5. Reload the page (hard refresh). Confirm every gold-tinted element across the app now renders **sapphire-blue**, not gold:
       - **Sidebar active nav item:** sapphire background + sapphire text.
       - **Primary CTA (e.g. "Save changes" button on the settings page):** sapphire fill, not gold.
       - **Badge gold variants (e.g. any role pill, wealth-tier badge):** sapphire tint.
       - **Input focus ring:** focus on an Input, see sapphire halo not gold.
       - **Table header text color:** sapphire-tinted (from table.tsx `text-gold-text` now using the overridden `--gold-text`).
       - **Confirmation destructive variant:** icon/border stays red (destructive is NOT gold-family). Verify this doesn't accidentally turn sapphire.
       - **WealthTierBadge tier-1 and tier-2:** sapphire-bright for tier-1, sapphire-primary for tier-2.
       - **WealthTierBadge tier-3 and tier-4:** STAY darker gold (`#c4a030` / `#a08828`). Audit accepted this as intentional value-signal. These are the ONLY visible gold remnants.
       - **Shimmer/Skeleton:** should be neutral white-alpha (tokens not gold).
       - **TagInput chips:** sapphire chips.
       - **Loader spinner border-top:** sapphire.
       - **EnrichmentStatusDots:** stay colorful (green/blue/red/amber — NOT gold-family). Confirm they render with proper colors regardless of tenant theme.

    6. Switch back to gold theme, confirm everything reverts to gold.

    7. Repeat with **emerald** theme for a second-theme sanity check.

    Any element that stays gold while others are sapphire means a literal leaked through. Flag the surface.

    Also run the final grep:
    ```bash
    grep -RE "rgba\(212,175,55|#d4af37|#f0d060" src/components/ui/
    ```
    Must output nothing.
  </how-to-verify>
  <resume-signal>Type "approved" with a note of which theme was used (sapphire / emerald / other). If any surface remained gold, list the specific element + file (so we can patch before merge).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan is strictly CSS-variable substitution in UI primitives. No new state, no new endpoints, no new user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Tampering | Tenant-theme CSS var override | accept | `tenant-theme.ts` CSS var injection is already the existing mechanism. This plan's primitives consume the overridden vars — no new injection vector. |
| T-39-04-02 | Spoofing | Tenant changes theme to mimic another tenant | accept | Tenants can only change their own theme (tenant-scoped RLS). Visual theming is not an identity-verification channel. |

No new attack surface — CSS refactor only.
</threat_model>

<verification>
## Phase-level verification

1. **Grep: no gold hex/rgba in src/components/ui/:**
   ```bash
   grep -RE "rgba\(212,175,55|#d4af37|#f0d060" src/components/ui/
   # Expected: no output, exit code 1 (grep found nothing).
   ```
2. **Grep: intentional tier-3/tier-4 hex only in wealth-tier-badge:**
   ```bash
   grep -RE "#c4a030|#a08828" src/components/ui/
   # Expected: ONLY `src/components/ui/wealth-tier-badge.tsx` lines, with comments explaining intentionality.
   ```
3. **Build passes:** `pnpm build` exits 0.
4. **Tenant-theme switch verified manually** (Task 9 above) — switching to sapphire visually swaps all gold elements to sapphire, except intentional tier-3/tier-4 shades.
</verification>

<success_criteria>
- [ ] All 8 primitive files (wealth-tier-badge, tag-input, loader, confirmation, enrichment-status-dots, table, shimmer, logo-upload) updated per their respective tasks.
- [ ] Grep `rgba(212,175,55|#d4af37|#f0d060` across `src/components/ui/` returns 0 matches.
- [ ] Grep `#c4a030|#a08828` matches ONLY in `wealth-tier-badge.tsx` with explanatory comments.
- [ ] Switching tenant theme to sapphire in a test tenant visibly swaps all gold elements to sapphire (Task 9 verified).
- [ ] `pnpm build` exits 0.
- [ ] No regression to EnrichmentStatusDots (still renders per-source colors correctly after change).
- [ ] No regression to Confirmation destructive variant (still reads as red, not theme-bound).
</success_criteria>

<output>
After completion, create `.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-04-SUMMARY.md`:
- List of 8 primitives edited with before/after class strings.
- Grep output proving 0 matches for gold hex/rgba in `src/components/ui/`.
- Task 9 verification result: which theme was tested + any leaks discovered + how they were patched.
- Notes on any duplicate wealth-tier-badge files discovered (ui/ vs search/components/) and how they were reconciled.
- Screenshot of one primitive rendering under two different tenant themes (e.g. gold vs sapphire button) for record.
</output>
