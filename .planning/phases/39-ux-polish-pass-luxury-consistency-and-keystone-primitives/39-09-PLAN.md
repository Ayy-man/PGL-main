---
phase: 39-ux-polish-pass-luxury-consistency-and-keystone-primitives
plan: 09
type: execute
wave: 7
depends_on:
  - 39-01
  - 39-04
  - 39-08
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/select.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/shimmer.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/data-table/data-table.tsx
  - src/components/admin/reports/reports-table.tsx
  - src/app/admin/reports/[id]/report-detail.tsx
  - src/app/admin/tenants/new/page.tsx
  - src/components/research/research-result-card.tsx
  - src/components/research/channel-status-bar.tsx
  - src/components/prospect/timeline-feed.tsx
  - src/components/admin/tenant-activity-card.tsx
  - src/app/[orgId]/personas/components/live-data-stream.tsx
  - src/components/admin/platform-pulse-modal.tsx
  - src/components/admin/funnel-chart.tsx
  - src/components/charts/usage-chart.tsx
  - src/components/prospect/research-panel.tsx
  - src/components/prospect/add-to-list-dialog-profile.tsx
  - src/app/[orgId]/search/components/add-to-list-dialog.tsx
  - src/components/ui/table.tsx
autonomous: false
requirements:
  - PHASE-39-SCREEN-TOKENS

must_haves:
  truths:
    - "Button motion uses ease-out easing consistent across primitives"
    - "Button ghost variant uses bg-bg-elevated + border-subtle tokens (not raw rgba)"
    - "Input/Textarea inputs use bg-bg-input-custom (not bg-transparent)"
    - "Select check indicator is gold; Select item focus is gold-bg + gold-primary"
    - "Badge success/warning/info use oklch tokens (not hardcoded hex)"
    - "Checkbox has hover border treatment"
    - "Shimmer primitives use shimmer-skeleton class + tokenized backgrounds"
    - "Dialog bg/border moved from inline style to className"
    - "DataTable empty state uses EmptyState primitive"
    - "DataTable row hover preserves gold (not overridden)"
    - "Typography ladder normalized — suspended page H1 has font-serif; no body-text-serif regressions"
    - "Badge radius uses rounded-badge token OR rounded-full + token unused orphan deleted"
    - "Reports + research + admin-tenants blue/emerald leaks replaced with info/success tokens"
    - "7 more EmptyState rollouts close raw 'No X' text sites (timeline-feed, tenant-activity-card, live-data-stream, platform-pulse-modal, funnel-chart, usage-chart, research-panel)"
    - "Dialog-body mini empty states (add-to-list-dialog variants) use EmptyState"
    - "Table column header uses text-gold-text token"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "ease-out + ghost variant tokenized + link variant gold text"
      contains: "ease-out"
    - path: "src/components/ui/badge.tsx"
      provides: "success/warning/info use oklch tokens"
      contains: "bg-success-muted"
    - path: "src/components/ui/checkbox.tsx"
      provides: "hover border"
      contains: "hover:border"
    - path: "src/components/ui/shimmer.tsx"
      provides: "shimmer-skeleton + tokenized bg"
      contains: "shimmer-skeleton"
    - path: "src/components/ui/dialog.tsx"
      provides: "bg + border in className (no inline style)"
      contains: "bg-\\[var\\(--bg-floating\\)\\]"
    - path: "src/components/ui/data-table/data-table.tsx"
      provides: "EmptyState for empty rows"
      contains: "EmptyState"
    - path: "src/components/ui/table.tsx"
      provides: "text-gold-text token on column header"
      contains: "text-gold-text"
  key_links:
    - from: "reports + research + tenants leak sites"
      to: "oklch info/success tokens"
      via: "bg-info-muted + bg-success-muted replace raw tailwind blue/emerald"
      pattern: "bg-info-muted|bg-success-muted"
    - from: "raw 'No X' strings"
      to: "EmptyState primitive"
      via: "7 rollout sites across dashboard/activity/analytics/funnel/usage"
      pattern: "from \"@/components/ui/empty-state\""
---

<objective>
Close remaining findings in `05-design-system.md` not absorbed by other plans. Scope: primitive-level consistency polish (button motion/link variant, input bg, select focus tokens, badge semantic tokens, checkbox hover, shimmer-skeleton class, dialog inline-style removal), typography-ladder residuals (badge radius decision), C-3 color-leak cleanup (blue/emerald → info/success tokens in 6 consumer files), and EmptyState rollout to remaining raw "No X" sites (D-1 through D-10 from the audit).

Purpose: After keystones (39-01), destructive sweep (39-02), role gating (39-03), and theme leak fix (39-04), this plan finishes the design-system-consistency pass. Small, surgical edits across many primitives and consumers. Each is low-risk find-and-replace. Collectively they close ~30+ findings.

Output: 24 files modified. Grep `rgba(212,175,55|#d4af37|text-red-400|text-blue-400|text-yellow-400|bg-red-500|bg-blue-500|bg-emerald-500` across `src/components/ui/` and called-out consumer files returns 0 matches (or matches only in intentional tier-3/4 comments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-CONTEXT.md
@.planning/audits/ux-polish-2026-04-14/05-design-system.md
@src/components/ui/empty-state.tsx
@src/components/ui/button.tsx
@src/components/ui/badge.tsx
@src/app/globals.css
@tailwind.config.ts

<interfaces>
Semantic tokens available (from globals.css + tailwind.config.ts):
- bg-success-muted, text-success, border-success/30
- bg-warning-muted, text-warning, border-warning/30
- bg-info-muted, text-info, border-info/30
- bg-destructive, text-destructive, border-destructive (with /10, /20, /5 opacity modifiers)
- text-gold-text, text-gold-primary, text-gold-bright
- bg-gold-bg, bg-gold-bg-strong, border-border-gold
- bg-bg-elevated, border-border-subtle, border-border-hover, border-border-default
- bg-bg-input-custom (from --bg-input)

EmptyState primitive (from `src/components/ui/empty-state.tsx`):
```tsx
<EmptyState icon={IconComponent} title="..." description="..." variant="default|error">
  <Button>...</Button>  {/* optional CTA as children */}
</EmptyState>
```

Radius tokens (tailwind.config.ts):
- rounded-badge = 20px (defined but unused — this plan resolves the orphan)
- rounded-card = 14px
- rounded-btn = 8px
- rounded-full (Tailwind default; used currently by Badge)

shimmer-skeleton utility class (globals.css:491-495): gold-tinted moving gradient shimmer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Button motion ease-out + ghost tokens + link gold text + Input/Textarea bg-bg-input-custom</name>
  <read_first>
    - src/components/ui/button.tsx (lines 8, 21, 22)
    - src/components/ui/input.tsx (line 11)
    - src/components/ui/textarea.tsx (line 12)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/button.tsx lines 63-89; ui/input.tsx lines 100-105; ui/textarea.tsx lines 116-120)
    - NOTE: Plans 39-01 K4/K6/K7 handled default variant, focus shadow, and gold variants. This task handles the remaining button/input polish.
  </read_first>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/textarea.tsx</files>
  <action>
    A. button.tsx:
    - Line 8 base: append `ease-out` to the existing `transition-all duration-200 active:scale-[0.97]` → `transition-all duration-200 ease-out active:scale-[0.97]`.
    - Ghost variant (21): replace `bg-[rgba(255,255,255,0.03)] border border-[rgba(255,255,255,0.08)] text-foreground/60 rounded-[8px] hover:border-[rgba(255,255,255,0.15)] hover:text-foreground` with `bg-bg-elevated border border-border-subtle text-foreground/60 rounded-[8px] hover:border-border-hover hover:text-foreground`.
    - Link variant (22): replace `text-primary underline-offset-4 hover:underline` with `text-gold-primary underline-offset-4 hover:underline focus-visible:ring-1 focus-visible:ring-ring focus-visible:rounded-sm`.

    B. input.tsx (line 11):
    - Change bg-transparent to bg-bg-input-custom (adds resting elevation, matches card-on-card nesting).

    C. textarea.tsx (line 12):
    - Same bg-transparent → bg-bg-input-custom swap.
  </action>
  <verify>
    <automated>grep -E "ease-out" src/components/ui/button.tsx && grep -E "bg-bg-elevated" src/components/ui/button.tsx && grep -E "text-gold-primary" src/components/ui/button.tsx && grep -E "bg-bg-input-custom" src/components/ui/input.tsx && grep -E "bg-bg-input-custom" src/components/ui/textarea.tsx</automated>
  </verify>
  <done>
    - Button base has ease-out.
    - Ghost variant uses token-based classes (no rgba literals).
    - Link variant uses text-gold-primary.
    - Input/Textarea use bg-bg-input-custom.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Select item focus gold + check indicator gold; Badge semantic token migration + radius decision; Checkbox hover</name>
  <read_first>
    - src/components/ui/select.tsx (lines 121, 128)
    - src/components/ui/badge.tsx (lines 7, 11-12, 19-23)
    - src/components/ui/checkbox.tsx (line 16)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (select 131-142; badge 166-198; checkbox 153-157)
    - tailwind.config.ts (confirm rounded-badge at line 103 — decide keep or delete)
    - NOTE: Plans 39-01 K7/K10 handled gold variants + checked state + selected row. This task does the remaining token migrations.
  </read_first>
  <files>src/components/ui/select.tsx, src/components/ui/badge.tsx, src/components/ui/checkbox.tsx</files>
  <action>
    A. select.tsx:
    - SelectItem hover (121): replace `focus:bg-accent focus:text-accent-foreground` with `focus:bg-gold-bg focus:text-gold-primary`.
    - Check indicator (128): add `className="h-4 w-4 text-gold-primary"` on the <Check> component so the selected option's check reads gold.

    B. badge.tsx:
    - Default variant (11-12): replace `bg-primary text-primary-foreground shadow hover:bg-primary/80` with `bg-gold-bg text-gold-primary border-border-gold` (making the current `gold` variant the default).
    - Success/warning/info variants (19-23):
      - success: `"bg-success-muted text-success border-success/30"`
      - warning: `"bg-warning-muted text-warning border-warning/30"`
      - info: `"bg-info-muted text-info border-info/30"`
    - Radius (line 7): decision — the `rounded-badge` token at `tailwind.config.ts:103` (20px) is declared but unused. Two options:
      - Keep `rounded-full` on Badge (current) and DELETE the `rounded-badge` declaration from tailwind.config.ts (cleaner — removes dead token).
      - Switch Badge to `rounded-badge` (preserves token; less common pattern).
    - Preferred: keep `rounded-full` AND delete `rounded-badge` from tailwind.config.ts. Document this decision in SUMMARY.

    C. checkbox.tsx (16):
    - Add `hover:border-gold-primary/60` to the resting class list (already includes border-border-subtle from Plan 39-01 K10).
  </action>
  <verify>
    <automated>grep -E "focus:bg-gold-bg" src/components/ui/select.tsx && grep -E "bg-success-muted" src/components/ui/badge.tsx && grep -E "bg-info-muted" src/components/ui/badge.tsx && grep -E "hover:border-gold-primary" src/components/ui/checkbox.tsx</automated>
  </verify>
  <done>
    - Select item focus uses gold.
    - Select check indicator is gold.
    - Badge default + success/warning/info variants use tokens.
    - rounded-badge token decision documented (removed OR applied).
    - Checkbox has hover border.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Shimmer primitives — shimmer-skeleton class + tokenized backgrounds</name>
  <read_first>
    - src/components/ui/shimmer.tsx (lines 45, 68, 89, 95, 96)
    - src/app/globals.css (confirm .shimmer-skeleton exists around line 491-495)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/shimmer.tsx lines 284-297)
    - NOTE: Plan 39-04 Task 6 already handled tokenization of rgba(255,255,255,*) backgrounds in shimmer.tsx. Verify if that landed; if so, this task only adds the shimmer-skeleton class migration.
  </read_first>
  <files>src/components/ui/shimmer.tsx</files>
  <action>
    Plan 39-04 Task 6 should have already tokenized backgrounds. Verify and add the shimmer-skeleton class swap:

    - ShimmerLine, ShimmerBlock, ShimmerCard (lines 45, 68, 95, 96): replace `animate-pulse` with `shimmer-skeleton` class. This gives a moving gold-tinted gradient shimmer instead of opacity pulse — matches the Skeleton primitive which already uses shimmer-skeleton.
    - If Plan 39-04 Task 6 did NOT tokenize the backgrounds, replace rgba literals with `bg-bg-elevated` / `bg-border-subtle` per audit line 290.

    Consumers of these primitives (e.g. analytics page loading skeletons from Plan 39-07 Task 6) will inherit the shimmer upgrade automatically.
  </action>
  <verify>
    <automated>grep -E "shimmer-skeleton" src/components/ui/shimmer.tsx</automated>
  </verify>
  <done>
    - Shimmer primitives use shimmer-skeleton class.
    - Backgrounds are token-based (from 39-04 or this task).
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Dialog — move inline style bg/border into className (allow consumer override)</name>
  <read_first>
    - src/components/ui/dialog.tsx (line 44)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (ui/dialog.tsx lines 226-231)
    - NOTE: Plan 39-08 Task 6 also touches dialog.tsx — coordinate. That plan adds backdrop-blur + close button polish. This task removes inline-style so className override works.
  </read_first>
  <files>src/components/ui/dialog.tsx</files>
  <action>
    DialogContent at line 44 currently has `style={{ background: "var(--bg-floating)", border: "1px solid var(--border-default)" }}`. Move this to className:
    - Add `bg-[var(--bg-floating)] border border-[var(--border-default)]` to the Content's className.
    - Delete the `style={{...}}` attribute.

    This allows consumers to override the background via className prop (inline styles have higher specificity). Preserve existing animation classes and size classes.
  </action>
  <verify>
    <automated>grep -E "bg-\[var\(--bg-floating\)\]" src/components/ui/dialog.tsx && ! grep -E "style=\{\{\s*background:.*var\(--bg-floating\)" src/components/ui/dialog.tsx</automated>
  </verify>
  <done>
    - DialogContent has bg + border in className.
    - No inline style for bg/border.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Color leak cleanup — blue/emerald in reports, research, tenants-new → info/success tokens</name>
  <read_first>
    - src/components/admin/reports/reports-table.tsx (line ~67) — if this file doesn't exist, check src/app/admin/reports/reports-table.tsx
    - src/app/admin/reports/[id]/report-detail.tsx (line 81)
    - src/app/admin/tenants/new/page.tsx (line 74)
    - src/components/research/research-result-card.tsx (line 48)
    - src/components/research/channel-status-bar.tsx (line 63)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (C-3 section at lines 484-494)
    - NOTE: Plan 39-05 Task 6 touched reports-table + report-detail — that handled the `investigating` status badge styling. If this task duplicates, skip the duplicate.
  </read_first>
  <files>src/app/admin/reports/reports-table.tsx, src/app/admin/reports/[id]/report-detail.tsx, src/app/admin/tenants/new/page.tsx, src/components/research/research-result-card.tsx, src/components/research/channel-status-bar.tsx</files>
  <action>
    Replace raw Tailwind blue/emerald with semantic tokens:

    A. reports-table.tsx (~line 67): `bg-blue-500/10 text-blue-400` → `bg-info-muted text-info border-info/30`.
    B. report-detail.tsx (line 81): same swap.
    C. tenants/new/page.tsx (line 74): `bg-emerald-500/10 text-emerald-600 dark:text-emerald-400` → `bg-success-muted text-success border-success/30`.
    D. research-result-card.tsx (line 48): `bg-emerald-500` dot → replace with `<span className="h-2 w-2 rounded-full bg-success" />` or equivalent.
    E. channel-status-bar.tsx (line 63): same emerald dot → `bg-success`.

    Confirm plan 39-05 didn't already normalize A/B/C — if it did, mark this task as partially redundant and only apply to D/E.
  </action>
  <verify>
    <automated>! grep -RE "bg-blue-500/10 text-blue-400|bg-emerald-500/10 text-emerald-600" src/app/admin/reports/ src/app/admin/tenants/ && grep -E "bg-info-muted|bg-success-muted" src/app/admin/reports/ -r</automated>
  </verify>
  <done>
    - Blue-500/10 and emerald-500/10 replaced with info-muted/success-muted tokens.
    - research dots use bg-success.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: EmptyState rollout batch 1 — timeline-feed, tenant-activity-card, live-data-stream, platform-pulse-modal, funnel-chart, usage-chart, research-panel</name>
  <read_first>
    - src/components/prospect/timeline-feed.tsx (line 544)
    - src/components/admin/tenant-activity-card.tsx (lines 407, 652)
    - src/app/[orgId]/personas/components/live-data-stream.tsx (line 156)
    - src/components/admin/platform-pulse-modal.tsx (line 509)
    - src/components/admin/funnel-chart.tsx (line 72)
    - src/components/charts/usage-chart.tsx (line 42)
    - src/components/prospect/research-panel.tsx (line 310)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (D-4 through D-9 lines 533-553)
  </read_first>
  <files>src/components/prospect/timeline-feed.tsx, src/components/admin/tenant-activity-card.tsx, src/app/[orgId]/personas/components/live-data-stream.tsx, src/components/admin/platform-pulse-modal.tsx, src/components/admin/funnel-chart.tsx, src/components/charts/usage-chart.tsx, src/components/prospect/research-panel.tsx</files>
  <action>
    Replace raw "No X" text patterns with EmptyState primitive. One per file:

    A. timeline-feed.tsx (544): `<EmptyState icon={Activity} title="No activity yet" description="Actions on this prospect will appear here." />`.
    B. tenant-activity-card.tsx (407, 652): `<EmptyState icon={Activity} title="No activity found" description="No activity recorded for this period." />`.
    C. live-data-stream.tsx (156): `<EmptyState icon={Radio} title="No activity yet" description="Saved searches and enrichment events will stream here." />`.
    D. platform-pulse-modal.tsx (509): `<EmptyState icon={BarChart3} title="No activity data" description="No activity recorded for this timeframe." />`.
    E. funnel-chart.tsx (72): `<EmptyState icon={BarChart3} title="No funnel data yet" description="Search and export prospects to see funnel data." />`.
    F. usage-chart.tsx (42): `<EmptyState icon={LineChart} title="No data this period" description="Select a wider date range or check back after activity." />`.
    G. research-panel.tsx (310): `<EmptyState icon={SearchX} title="No results" description="Try rephrasing your question or broadening the topic." />`.

    Imports: `EmptyState` from `@/components/ui/empty-state`; relevant lucide icons (`Activity, Radio, BarChart3, LineChart, SearchX`).

    If any file is deeply nested (e.g. inside a chart container with fixed height), wrap EmptyState in a container that matches the expected chart dimensions so the layout doesn't collapse.
  </action>
  <verify>
    <automated>grep -E "EmptyState" src/components/prospect/timeline-feed.tsx && grep -E "EmptyState" src/components/admin/tenant-activity-card.tsx && grep -E "EmptyState" src/app/[orgId]/personas/components/live-data-stream.tsx && grep -E "EmptyState" src/components/admin/platform-pulse-modal.tsx && grep -E "EmptyState" src/components/admin/funnel-chart.tsx && grep -E "EmptyState" src/components/charts/usage-chart.tsx && grep -E "EmptyState" src/components/prospect/research-panel.tsx</automated>
  </verify>
  <done>
    - 7 files imported EmptyState and rendered it.
    - Raw "No X" strings removed.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: EmptyState rollout batch 2 — dialog-body mini empty states (3 add-to-list variants) + DataTable + Table column header token</name>
  <read_first>
    - src/components/prospect/add-to-list-dialog-profile.tsx (line 108)
    - src/app/[orgId]/search/components/search-content.tsx (line 1337) — but the list-search fix in Plan 39-07 Task 2 may have addressed this. Verify.
    - src/app/[orgId]/search/components/add-to-list-dialog.tsx (line 110)
    - src/components/ui/data-table/data-table.tsx (lines 132, 144-152)
    - src/components/ui/table.tsx (line 76)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (D-10 at lines 555-557, data-table at 394-406, table at 414-420)
  </read_first>
  <files>src/components/prospect/add-to-list-dialog-profile.tsx, src/app/[orgId]/search/components/add-to-list-dialog.tsx, src/components/ui/data-table/data-table.tsx, src/components/ui/table.tsx</files>
  <action>
    A. add-to-list-dialog-profile.tsx (108) and add-to-list-dialog.tsx (110):
    - Replace the inline "No lists yet" + Icon + Button with `<EmptyState variant="default" icon={ListPlus} title="No lists yet"><Button variant="gold" size="sm" asChild>...</Button></EmptyState>`.
    - Preserve the existing CTA (likely a "Create new list" button).

    B. data-table.tsx:
    - Empty row (144-152): replace `<TableCell ... className="h-24 text-center text-muted-foreground">No results.</TableCell>` with:
      ```tsx
      <TableCell colSpan={columns.length} className="p-0">
        <EmptyState icon={Search} title="No results" description="Try adjusting filters or search terms." />
      </TableCell>
      ```
    - Row hover override (132): change `className="hover:bg-muted/50 transition-colors"` to `className="row-hover-gold transition-colors"` OR drop the override entirely to inherit TableRow's gold hover.

    C. table.tsx (76):
    - Column header: replace `text-[rgba(212,175,55,0.7)]` with `text-gold-text` (Tailwind alias that maps to the same value but is token-aware).

    Imports: EmptyState, ListPlus, Search from lucide-react.
  </action>
  <verify>
    <automated>grep -E "EmptyState" src/components/prospect/add-to-list-dialog-profile.tsx && grep -E "EmptyState" src/app/[orgId]/search/components/add-to-list-dialog.tsx && grep -E "EmptyState" src/components/ui/data-table/data-table.tsx && grep -E "text-gold-text" src/components/ui/table.tsx</automated>
  </verify>
  <done>
    - 3 dialog-body empty states use EmptyState.
    - DataTable empty row uses EmptyState.
    - DataTable row hover preserves gold.
    - table.tsx column header uses text-gold-text token.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Typography ladder outliers — suspended page H1, ConfirmationTitle decision</name>
  <read_first>
    - src/app/suspended/page.tsx (line 35)
    - src/components/ui/confirmation.tsx (line 109 ConfirmationTitle)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (B-1 at 431, B-3 at 440-444, B-4 at 446-450)
    - NOTE: Plan 39-06 Task 5 already added font-serif to suspended H1. Verify and skip if landed.
  </read_first>
  <files>src/components/ui/confirmation.tsx, src/app/suspended/page.tsx</files>
  <action>
    A. suspended/page.tsx (35): confirm Plan 39-06 Task 5 added `font-serif`. If missing, apply: `<h1 className="font-serif text-2xl font-semibold text-foreground">`.

    B. confirmation.tsx (109 ConfirmationTitle):
    - Current: `"text-sm font-semibold text-foreground"`.
    - Decision per audit line 444: "tight inline confirmation titles are short, so font-serif may feel large at text-sm. Either bump to font-serif text-base font-semibold tracking-tight or accept as a deliberate 'small heading' exception."
    - Preferred: KEEP as-is (audit explicitly calls it "small heading exception — Flag, don't auto-fix"). Document the decision in SUMMARY.

    C. Heading scale normalization (B-4): this is documentation-level, not a code change. Note in SUMMARY: "Page H1 scale is 2-step — `font-serif text-3xl font-bold tracking-tight` for app pages, `font-serif text-2xl font-semibold tracking-tight` for auth pages (narrow card context). No code changes; baseline documented."
  </action>
  <verify>
    <automated>grep -E "font-serif" src/app/suspended/page.tsx</automated>
  </verify>
  <done>
    - suspended H1 has font-serif (from 39-06 or this task).
    - ConfirmationTitle decision documented.
    - Heading scale baseline documented.
    - pnpm build exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Design-system consistency walkthrough — primitives + consumers + empty-state coverage</name>
  <what-built>
    30+ design-system-consistency findings closed. Button motion easing + ghost tokenization + link gold. Input/Textarea elevation. Select + Badge + Checkbox polish. Shimmer gold tint. Dialog inline-style removed. 6 color-leak sites cleaned up. 10 EmptyState rollouts. Table column header tokenized.
  </what-built>
  <how-to-verify>
    Run pnpm dev. Walk through:

    1. Buttons: Click any button → feels slightly more deliberate (ease-out). Ghost variant looks softer (bg-bg-elevated). Link variant renders gold underlined text.

    2. Inputs/Textareas: Empty state now has subtle elevation (bg-bg-input-custom) instead of transparent.

    3. Selects: Open any Select dropdown → keyboard-navigate with arrow keys → highlighted option has gold bg + gold text. Selected option's check icon is gold.

    4. Badges: Admin reports table with "investigating" status → renders info-blue using tokens (not raw blue-400). Success status uses success-muted. Warning uses warning-muted.

    5. Checkbox: Hover over unchecked box → border glows gold. Click → fills gold (from Plan 39-01 K10).

    6. Shimmer: Any page with ShimmerCard or ShimmerBlock (analytics loading, api-keys loading) → gold-tinted gradient moves across (not opacity pulse).

    7. Dialog: Close any Dialog, open another → no visual change for consumers (background stays var(--bg-floating)). If a consumer passes className="bg-red-500" (for testing), the red now wins (it didn't before because inline style had higher specificity).

    8. Empty states rollout:
       - Visit a prospect with no activity → timeline-feed shows EmptyState.
       - Visit a tenant with no activity history in admin → tenant-activity-card shows EmptyState.
       - Visit the live-data-stream (persona library) with zero data → EmptyState.
       - Visit platform-pulse-modal with no data → EmptyState.
       - Visit funnel-chart with no activity → EmptyState.
       - Visit usage-chart with narrow date range (no data) → EmptyState.
       - Ask research-panel a question with no results → EmptyState.
       - Open add-to-list dialog (from search or profile) when the user has no lists → EmptyState with Create New List CTA.

    9. DataTable: Visit any data-table with filters that return zero rows → row renders EmptyState with icon + title + description (not raw "No results." text). Hover rows → gold tint preserved.

    10. Table column header: Visible gold tint is unchanged (token maps to same value). Verify by inspecting in devtools that the computed color resolves correctly.

    11. Badge radius: Visit any page with role badges, wealth-tier badges, status badges. If rounded-badge was deleted from tailwind.config.ts, no badges should look different (rounded-full still used). If rounded-badge was applied, badges now have 20px corners instead of fully-round.

    Flag regressions. Particular risks: (a) changing button base easing may subtly alter perceived timing app-wide — acceptable if consistent. (b) Input bg swap from transparent to bg-input-custom may look different against certain backgrounds — verify on auth forms especially.
  </how-to-verify>
  <resume-signal>Type "approved" or list regressed surfaces + findings</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Design-system consistency polish — all CSS class/token substitutions + EmptyState swaps. No new data paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09-01 | Tampering | Tailwind class swaps | accept | Pure CSS refactor. No behavior changes. |
| T-39-09-02 | Information Disclosure | EmptyState descriptions | accept | Copy is static text explaining the empty state. No user-data interpolation into description that wasn't already on page. |

No new attack surface. Consistency polish only.
</threat_model>

<verification>
## Phase-level verification

1. Build passes: pnpm build exits 0.
2. Grep audits:
   ```
   grep -E "ease-out" src/components/ui/button.tsx
   grep -E "bg-bg-input-custom" src/components/ui/input.tsx src/components/ui/textarea.tsx
   grep -E "bg-success-muted|bg-info-muted" src/components/ui/badge.tsx
   grep -E "text-gold-text" src/components/ui/table.tsx
   grep -E "shimmer-skeleton" src/components/ui/shimmer.tsx
   grep -RE "bg-blue-500/10 text-blue-400|bg-emerald-500/10" src/app/admin/ src/components/research/
   # Expected: 0 matches.
   ```
3. EmptyState coverage:
   ```
   grep -l "EmptyState" src/components/prospect/timeline-feed.tsx src/components/admin/tenant-activity-card.tsx src/app/[orgId]/personas/components/live-data-stream.tsx src/components/admin/platform-pulse-modal.tsx src/components/admin/funnel-chart.tsx src/components/charts/usage-chart.tsx src/components/prospect/research-panel.tsx src/components/prospect/add-to-list-dialog-profile.tsx src/app/[orgId]/search/components/add-to-list-dialog.tsx src/components/ui/data-table/data-table.tsx
   # Expected: all 10 paths.
   ```
4. Manual walkthrough per Task 9.
</verification>

<success_criteria>
- [ ] 24 files modified.
- [ ] Button motion + ghost + link variants tokenized and polished.
- [ ] Input + Textarea use bg-bg-input-custom.
- [ ] Select focus + check use gold tokens.
- [ ] Badge default + success/warning/info use semantic tokens.
- [ ] Badge radius decision (rounded-badge deleted OR applied) documented.
- [ ] Checkbox has hover border.
- [ ] Shimmer primitives use shimmer-skeleton class.
- [ ] Dialog bg/border moved to className.
- [ ] 5 blue/emerald leak sites replaced with info/success tokens.
- [ ] 10 EmptyState rollouts close raw "No X" text.
- [ ] DataTable empty state uses EmptyState + row hover gold.
- [ ] table.tsx column header uses text-gold-text token.
- [ ] suspended H1 + ConfirmationTitle typography decisions documented.
- [ ] pnpm build exits 0.
- [ ] Manual walkthrough (Task 9) passes all 11 spot-checks.
</success_criteria>

<output>
After completion, create .planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-09-SUMMARY.md:
- Checklist of audit 05-design-system.md findings closed (reference line numbers).
- Badge radius decision (kept rounded-full vs applied rounded-badge vs deleted the token).
- ConfirmationTitle decision (kept text-sm or bumped to font-serif text-base).
- Heading scale baseline documentation (page H1 = text-3xl, auth H1 = text-2xl).
- Any audits noted as already-handled-by-earlier-plans (to avoid double-work).
- Count of EmptyState rollout sites closed (target 10).
</output>
