---
phase: 39-ux-polish-pass-luxury-consistency-and-keystone-primitives
plan: 05
type: execute
wave: 5
depends_on:
  - 39-01
  - 39-02
files_modified:
  - src/app/admin/admin-nav-links.tsx
  - src/app/admin/admin-sidebar.tsx
  - src/app/admin/loading.tsx
  - src/app/admin/page.tsx
  - src/app/admin/tenants/tenant-table.tsx
  - src/app/admin/tenants/tenant-status-toggle.tsx
  - src/app/admin/tenants/new/page.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/users/new/page.tsx
  - src/app/admin/reports/reports-table.tsx
  - src/app/admin/reports/[id]/report-detail.tsx
  - src/app/admin/analytics/page.tsx
  - src/app/admin/automations/page.tsx
  - src/components/admin/error-feed.tsx
  - src/components/admin/tenant-heatmap.tsx
  - src/components/admin/api-keys/integration-card.tsx
  - src/app/admin/api-keys/page.tsx
autonomous: false
requirements:
  - PHASE-39-SCREEN-ADMIN

must_haves:
  truths:
    - "All admin inline success/error banners migrated to toast() calls (Toaster mounted in 39-01)"
    - "Admin nav items have CSS :hover + focus-visible rings (not inline onMouseEnter)"
    - "System Config stub links show 'Soon' pill + Tooltip (not native title)"
    - "Admin dashboard loading.tsx skeleton matches real layout (stat grid + heatmap + 2-col)"
    - "Admin dashboard polling pauses when tab hidden"
    - "Admin users list has a tenant/role/name filter"
    - "Admin users list has DropdownMenu with Resend invite + Deactivate + Edit role actions"
    - "All raw <select> in admin are replaced with themed <Select> primitive"
    - "Breadcrumbs replace lone ArrowLeft back-links on admin deep pages"
    - "Admin tables have sticky headers on long lists"
    - "EmptyState primitive replaces raw 'No X found' text in admin tables"
  artifacts:
    - path: "src/app/admin/admin-nav-links.tsx"
      provides: "CSS-based hover + focus-visible rings (no onMouseEnter state)"
      contains: "focus-visible"
    - path: "src/app/admin/loading.tsx"
      provides: "skeleton matching actual dashboard shape"
      contains: "Skeleton"
    - path: "src/app/admin/tenants/tenant-table.tsx"
      provides: "sticky header + EmptyState + oklch status tokens"
      contains: "EmptyState"
    - path: "src/app/admin/users/page.tsx"
      provides: "client filter bar + dropdown actions"
      contains: "DropdownMenu"
    - path: "src/app/admin/users/new/page.tsx"
      provides: "themed Select instead of native <select>; Breadcrumbs"
      contains: "from \"@/components/ui/select\""
    - path: "src/app/admin/reports/reports-table.tsx"
      provides: "themed Select; debounced tenant search; Clear filters; skeleton rows"
      contains: "from \"@/components/ui/select\""
    - path: "src/app/admin/reports/[id]/report-detail.tsx"
      provides: "Breadcrumbs; snapshot copy button; toast success; themed Select"
      contains: "Breadcrumbs"
    - path: "src/components/admin/tenant-heatmap.tsx"
      provides: "ghost buttons wired OR disabled with tooltip"
      contains: "Tooltip"
    - path: "src/components/admin/error-feed.tsx"
      provides: "conditional pulse color (red when errors, success when empty); focus rings"
      contains: "var(--success)"
  key_links:
    - from: "admin toggles + forms"
      to: "@/hooks/use-toast"
      via: "replace inline setSuccess/setError with toast()"
      pattern: "toast\\("
    - from: "admin tables"
      to: "@/components/ui/empty-state"
      via: "EmptyState primitive replaces raw 'No X' text"
      pattern: "from \"@/components/ui/empty-state\""
    - from: "admin forms with native select"
      to: "@/components/ui/select"
      via: "themed Select primitive"
      pattern: "from \"@/components/ui/select\""
---

<objective>
Close remaining significant + medium findings in `01-super-admin.md` not absorbed by keystones (39-01) or destructive sweep (39-02). Scope = admin layout + /admin dashboard + /admin/tenants + /admin/users + /admin/reports + /admin/analytics + /admin/api-keys + /admin/automations.

Purpose: Make the admin surface feel as luxury as the tenant surface. Today admin has inline banners (now unblocked by Toaster mount in 39-01), raw native `<select>` dropdowns that break dark theme on macOS/Windows, missing breadcrumbs, non-functional ghost buttons on the heatmap, and skeletons that mismatch the real layout shape. This plan closes 25+ individual findings from the audit by grouping them into 10 focused tasks.

Output: 17 files modified across admin layout + 9 admin routes + admin-surface components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-CONTEXT.md
@.planning/audits/ux-polish-2026-04-14/01-super-admin.md
@src/components/ui/select.tsx
@src/components/ui/breadcrumbs.tsx
@src/components/ui/empty-state.tsx
@src/components/ui/skeleton.tsx
@src/components/ui/dropdown-menu.tsx
@src/hooks/use-toast.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Admin layout nav — CSS hover + focus-visible rings + Soon pill + sidebar collapse tooltip</name>
  <read_first>
    - src/app/admin/admin-nav-links.tsx (lines 91-102, 164-175, 183-204, 194-199)
    - src/app/admin/admin-sidebar.tsx (lines 73-79, 109-120)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (Layout section findings at lines 16-43)
  </read_first>
  <files>src/app/admin/admin-nav-links.tsx, src/app/admin/admin-sidebar.tsx</files>
  <action>
    **A. `src/app/admin/admin-nav-links.tsx`:**
    - Remove the imperative `onMouseEnter`/`onMouseLeave` style mutation in all three `.map()` blocks (lines 91-102, 164-175, 194-199).
    - Replace with Tailwind classes: `className={cn("group ... transition-colors", "hover:bg-[var(--bg-elevated)] hover:text-[var(--text-primary-ds)]", "focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-[var(--gold-primary)]/40 focus-visible:ring-offset-0")}`.
    - For the System Config stubs (lines 183-204):
      - Add a "Soon" pill next to the label: `<span className="ml-auto text-[9px] uppercase tracking-widest" style={{ color: 'var(--text-ghost)' }}>Soon</span>`.
      - Replace native `title="Coming soon"` with a Tooltip: `<Tooltip><TooltipTrigger asChild><button ...></button></TooltipTrigger><TooltipContent>Coming soon</TooltipContent></Tooltip>`.
      - Add `aria-disabled="true"` and `tabIndex={-1}` to the stub buttons.

    **B. `src/app/admin/admin-sidebar.tsx`:**
    - User card at lines 73-79: change `rounded-lg` to `rounded-[12px]`; replace `rgba(255,255,255,0.05)` background with `var(--bg-elevated)`.
    - Collapse toggle at lines 109-120: replace native `title=` with a Tooltip. Add rotate-on-state: `<PanelLeft style={{ transform: collapsed ? 'rotate(180deg)' : 'none', transition: 'transform 200ms ease' }} />`.

    Imports: `Tooltip, TooltipTrigger, TooltipContent` from `@/components/ui/tooltip` (TooltipProvider from plan 39-01 Task 3 at root).
  </action>
  <verify>
    <automated>! grep -E "onMouseEnter.*style" src/app/admin/admin-nav-links.tsx && grep -E "focus-visible:ring" src/app/admin/admin-nav-links.tsx && grep -E "Tooltip" src/app/admin/admin-sidebar.tsx</automated>
  </verify>
  <done>
    - No inline onMouseEnter+style mutation in admin-nav-links.tsx.
    - focus-visible rings present.
    - Admin sidebar user card radius is 12px, background uses CSS var.
    - Collapse toggle uses Tooltip.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Admin dashboard — layout-matching skeleton + visibility-guard polling + pause/refresh UX</name>
  <read_first>
    - src/app/admin/loading.tsx (lines 1-14)
    - src/app/admin/page.tsx (lines 177-180, 224-233, 236-242)
    - src/components/admin/tenant-heatmap.tsx (lines 194-212, 312-341)
    - src/components/admin/error-feed.tsx (lines 63-92, 188-197, 266-288)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (findings at lines 48-117)
  </read_first>
  <files>src/app/admin/loading.tsx, src/app/admin/page.tsx, src/components/admin/tenant-heatmap.tsx, src/components/admin/error-feed.tsx</files>
  <action>
    **A. `src/app/admin/loading.tsx`:**
    - Replace the current 5-row skeleton with a layout-matching one:
      ```tsx
      import { Skeleton } from "@/components/ui/skeleton";
      export default function AdminLoading() {
        return (
          <div className="space-y-6">
            <Skeleton className="h-8 w-[200px] rounded-[8px]" />
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
              {Array.from({ length: 4 }).map((_, i) => <Skeleton key={i} className="h-[180px] rounded-[14px]" />)}
            </div>
            <Skeleton className="h-[360px] rounded-[14px]" />
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              <Skeleton className="h-[280px] rounded-[14px]" />
              <Skeleton className="h-[280px] rounded-[14px]" />
            </div>
          </div>
        );
      }
      ```

    **B. `src/app/admin/page.tsx`:**
    - At lines 177-180 (setInterval), wrap the fetchAll call in a visibility guard: `if (document.visibilityState === "visible") fetchAll(true);`. Optionally also listen to `visibilitychange` to pause/resume — a simple visibility-check-inside-the-callback is acceptable.
    - At lines 236-242 (Updated ticker), wrap the header-right block in a button that calls `fetchAll(true)` with a Tooltip "Force refresh". Keep the existing spinner when `isRefreshing`.
    - At lines 224-233 (System Status), wrap the status span in a Tooltip explaining the threshold: `"Operational: <1 error in last 24h · Degraded: 1-9 · Incident: 10+"`.

    **C. `src/components/admin/tenant-heatmap.tsx`:**
    - Ghost buttons at lines 312-341 ("Filter View", "Provision New Tenant") — wire "Provision New Tenant" as `<Link href="/admin/tenants/new">` or (if that's premature) disable with Tooltip "Coming soon". Pick the safer option: if a clear implementation exists (the Provision link is obvious), wire it; otherwise disable.
    - Row-action icons at lines 194-212 ("Impersonate", "View Logs", "Suspend Tenant") — if they have no handlers, add `disabled` + `<Tooltip>Coming soon</Tooltip>` rather than leaving silent no-ops.

    **D. `src/components/admin/error-feed.tsx`:**
    - Lines 188-197 + 228-237 pulse dot: make color conditional: `background: (data?.data.length ?? 0) > 0 ? "oklch(0.62 0.19 22)" : "var(--success)"`.
    - Lines 266-288 pagination buttons: add `rounded-[8px]` + `focus-visible:ring-1 focus-visible:ring-[var(--gold-primary)]/40 focus-visible:outline-none hover:bg-white/[0.08] transition-colors`.
    - Lines 63-92 error entries: either remove `cursor-pointer` + `entry-hover` from rows (honest — no detail route yet), OR wire onClick to `/w-team/admin/errors` per the MEMORY note (ensure the route actually accepts click-deep-links). Safer: remove cursor-pointer until detail route is ready.

    Keep existing functionality intact.
  </action>
  <verify>
    <automated>grep -E "Skeleton" src/app/admin/loading.tsx && grep -E "visibilityState" src/app/admin/page.tsx && grep -E "Tooltip|tooltip" src/components/admin/tenant-heatmap.tsx</automated>
  </verify>
  <done>
    - `Skeleton` primitives used in loading.tsx (layout-matching shape).
    - Visibility guard present in polling setInterval.
    - Heatmap ghost buttons either wired OR disabled with tooltip.
    - Error feed pulse is green when data is empty, red when errors exist.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Admin tenants — sticky header + EmptyState + oklch status + a11y row + toast for status toggle</name>
  <read_first>
    - src/app/admin/tenants/tenant-table.tsx (lines 27-61, 66-74, 77-82, 89-97, 119-123)
    - src/app/admin/tenants/tenant-status-toggle.tsx (lines 30-42)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (lines 121-163)
  </read_first>
  <files>src/app/admin/tenants/tenant-table.tsx, src/app/admin/tenants/tenant-status-toggle.tsx</files>
  <action>
    **A. `tenant-table.tsx`:**
    - Sticky header on `<thead>`: add `className="sticky top-0 z-10"` and `style={{ background: "var(--bg-elevated)" }}`.
    - Inactive status badge at 89-97: change `bg-destructive/10 text-destructive` to `style={{ background: "color-mix(in oklch, oklch(0.62 0.19 22) 10%, transparent)", color: "oklch(0.62 0.19 22)" }}` for consistency with rest of admin.
    - Empty state at 66-74 + 119-123: replace the single-line "No tenants found" with `<EmptyState icon={Building2} title="No tenants yet" description="Create your first real-estate team to start onboarding users."><Button asChild variant="gold"><Link href="/admin/tenants/new"><Plus className="h-4 w-4" /> Create Tenant</Link></Button></EmptyState>`. This is inside a `<td colSpan={N}>` wrapper.
    - Row click affordance at 77-82: add `tabIndex={0}` + `onKeyDown` handler for Enter/Space. Keep the existing onClick. Wrap the Name cell in Tooltip "View tenant details" OR add `title="View tenant details"` to the `<tr>`.
    - Add a client-side filter input above the table filtering by name/slug via `useMemo` — mirror reports-table pattern. Reference `src/app/admin/reports/reports-table.tsx` filter input style.

    **B. `tenant-status-toggle.tsx`:**
    - Lines 30-42: add `<Loader2 className="h-3 w-3 animate-spin" />` inline while `isPending`.
    - On catch, call `toast({ variant: "destructive", title: "Failed to update tenant", description: err.message })` via `useToast`.
    - On success, fire `toast({ title: "Tenant updated", description: \`${tenantName} is now ${newStatus}.\` })`.

    Imports for tenant-table: `EmptyState` from `@/components/ui/empty-state`, `Tooltip` from `@/components/ui/tooltip`, `Building2`, `Plus` from `lucide-react`, `Button` from `@/components/ui/button`, `Link` from `next/link`.
    Imports for status-toggle: `Loader2` from `lucide-react`, `useToast` from `@/hooks/use-toast`.
  </action>
  <verify>
    <automated>grep -E "EmptyState" src/app/admin/tenants/tenant-table.tsx && grep -E "sticky top-0" src/app/admin/tenants/tenant-table.tsx && grep -E "toast\(" src/app/admin/tenants/tenant-status-toggle.tsx</automated>
  </verify>
  <done>
    - `EmptyState` imported in tenant-table.tsx; sticky header present.
    - Client-side filter works in the table.
    - Status-toggle fires toasts on success + failure.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Admin tenants/new — Input primitive + slug auto-derive + inline validation + unsaved-changes guard + spinner + Breadcrumbs + toast</name>
  <read_first>
    - src/app/admin/tenants/new/page.tsx (lines 28-40, 50-56, 67, 82-140, 141-146)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (lines 167-215)
    - src/components/ui/input.tsx, src/components/ui/breadcrumbs.tsx
  </read_first>
  <files>src/app/admin/tenants/new/page.tsx</files>
  <action>
    - Replace raw `<input>` elements at lines 83-90, 97-105, 115-121 with the `Input` primitive. Drop the manual hand-rolled className.
    - Add slug auto-derive: track `slugDirty` state; when `!slugDirty`, `useEffect` sets slug from name: `.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "")`.
    - Add `slugError` state; onBlur validate slug against `/^[a-z0-9-]+$/`; render inline `<p className="text-xs text-destructive">Use only lowercase letters, numbers, and hyphens</p>` below the field.
    - Cancel link (141-146): if any input has a non-empty value, open a `<Confirmation isDestructive>` via `<Dialog>` "Discard this new tenant?". Only navigate on confirm.
    - Replace the lone back/cancel link + H1 scan at lines 50-56 with `<Breadcrumbs items={[{ label: "Admin", href: "/admin" }, { label: "Tenants", href: "/admin/tenants" }, { label: "Create" }]} />`.
    - Submit button (133-140): add `<Loader2 className="h-3.5 w-3.5 animate-spin" />` when isPending. Button variant stays per default (now gold-solid from 39-01 K4).
    - Success banner + setTimeout (28-40): replace with a persistent toast (`toast({ title: "Tenant created", description: ... })`) and `router.push("/admin/tenants")` immediately. Toast survives the route change.
    - Brand Theme label (127-131): add helper `<p className="text-xs text-muted-foreground">This theme colors the tenant's sidebar, buttons, and accents.</p>`.

    Imports: `Input`, `Breadcrumbs`, `Confirmation`, `Dialog`, `Loader2`, `useToast`.
  </action>
  <verify>
    <automated>grep -E "from \"@/components/ui/input\"" src/app/admin/tenants/new/page.tsx && grep -E "Breadcrumbs" src/app/admin/tenants/new/page.tsx && grep -E "slugDirty|useToast|toast\(" src/app/admin/tenants/new/page.tsx</automated>
  </verify>
  <done>
    - Input primitive used for all 3 form fields.
    - Slug auto-derives from name (slugDirty logic present).
    - Breadcrumbs replace lone back link.
    - Submit spinner + toast on success.
    - Cancel shows Confirmation when form is dirty.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Admin users list + new user — filter bar + DropdownMenu actions + themed Select + RoleBadge</name>
  <read_first>
    - src/app/admin/users/page.tsx (lines 8-43, 48-70, 82-117, 90-92, 108-113, 124-127, 148-153)
    - src/app/admin/users/new/page.tsx (lines 53-60, 109-122, 124-149)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (lines 220-300)
    - src/components/ui/select.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/breadcrumbs.tsx
  </read_first>
  <files>src/app/admin/users/page.tsx, src/app/admin/users/new/page.tsx</files>
  <action>
    **A. `src/app/admin/users/page.tsx`:**
    - Convert to a client component wrapper (or add a client sub-component for interactivity) that renders a client-side filter bar above the table: tenant `<Select>` populated from joined tenants.name set, role `<Select>`, and an email/name search `<Input>`. Filter client-side via `useMemo`.
    - Sticky header: same pattern as tenants table (`sticky top-0 z-10`).
    - EmptyState: replace "No users found" (72-77, 124-127) with `<EmptyState icon={UserPlus} title="No users yet" description="Invite your first agent or assistant."><Button asChild variant="gold"><Link href="/admin/users/new">Invite User</Link></Button></EmptyState>`.
    - Role badge mapping: create a local `<RoleBadge role={user.role} />` component returning:
      - super_admin → `<Badge variant="gold-solid">Super Admin</Badge>`
      - tenant_admin → `<Badge variant="gold">Admin</Badge>`
      - agent → `<Badge variant="default">Agent</Badge>` (neutral tan)
      - assistant → `<Badge variant="outline">Assistant</Badge>` (ghost)
      Use the helper to also humanize the role label.
    - Actions cell (108-113): replace the lone `<UserStatusToggle>` with a `<DropdownMenu>` containing "Resend invite" (triggers resendInvite server action — already exists per memory `8b68a28`), "Deactivate/Activate" (calls existing toggle), "Edit role" (can be a placeholder/link to a future edit flow OR disabled with Tooltip "Coming soon" if no edit flow exists today). Each action fires a toast.
    - Email cell: wrap email in a copy-to-clipboard button (reuse the pattern from `reports/[id]/report-detail.tsx:329-333`) + toast "Copied to clipboard".

    **B. `src/app/admin/users/new/page.tsx`:**
    - Replace both native `<select>` elements (lines 109-122, 134-147) with the themed `Select` primitive from `@/components/ui/select.tsx` (Radix-based).
    - Replace the "Loading tenants..." text at 128-133 with `<Skeleton className="h-9 w-full rounded-[8px]" />`.
    - Add Breadcrumbs at 53-60: `<Breadcrumbs items={[{ label: "Admin", href: "/admin" }, { label: "Users", href: "/admin/users" }, { label: "Invite" }]} />`.
    - Add Cmd/Ctrl+Enter submit shortcut via `onKeyDown` on the form.
    - Submit button spinner + toast same as tenants/new (Task 4 pattern).

    Imports: `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`; `DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger` from `@/components/ui/dropdown-menu`; `Breadcrumbs`, `Badge`, `Skeleton`, `useToast`.
  </action>
  <verify>
    <automated>grep -E "from \"@/components/ui/select\"" src/app/admin/users/new/page.tsx && grep -E "DropdownMenu" src/app/admin/users/page.tsx && grep -E "Breadcrumbs" src/app/admin/users/new/page.tsx && grep -E "RoleBadge|super_admin.*gold-solid|tenant_admin.*gold" src/app/admin/users/page.tsx</automated>
  </verify>
  <done>
    - Themed Select replaces native `<select>` in users/new.
    - Users list has DropdownMenu actions per row.
    - Role badge has 4 visually-distinct variants.
    - Breadcrumbs on users/new.
    - Filter bar works client-side.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Admin reports list + detail — themed Select, debounced search, Clear filters, skeleton rows, Breadcrumbs, copy button, toast migration</name>
  <read_first>
    - src/app/admin/reports/reports-table.tsx (lines 64-82, 144-146, 159-234, 267-275, 301-313, 323-326)
    - src/app/admin/reports/[id]/report-detail.tsx (lines 78-95, 329-333, 367-377, 395-407, 444-457, 496-566, 584-595, 605-628, 637-646)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (Reports sections at lines 322-425)
  </read_first>
  <files>src/app/admin/reports/reports-table.tsx, src/app/admin/reports/[id]/report-detail.tsx</files>
  <action>
    **A. `reports-table.tsx`:**
    - Status + category selects at lines 160-192 → replace native `<select>` with themed `Select` primitive.
    - Tenant input at 144-146: add a 300ms debounce using `setTimeout` + cleanup inside `useEffect`. Remove the Search button, OR keep as accessibility fallback but let typing auto-fire. When tenantInput changes, the debounced effect fires fetchRows.
    - Clear filters ghost button: when `status || category || tenantQuery`, render `<Button variant="ghost" size="sm" onClick={clearAll}>Clear filters</Button>`.
    - Loading state (267-275, 323-326): replace "Loading…" single-row text with 5 skeleton rows (match tenant-heatmap `SkeletonRow` pattern or inline `<Skeleton>` row).
    - "View" button at 301-313: add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--gold-primary)]/40`.
    - StatusBadge map at 64-82: extract to a shared helper or leave inline but replace raw Tailwind `bg-red-500/10 text-red-400`, `bg-blue-500/10` with oklch tokens per the fix in the audit.

    **B. `report-detail.tsx`:**
    - Back link at 367-377 → `<Breadcrumbs items={[{ label: "Admin", href: "/admin" }, { label: "Issue Reports", href: "/admin/reports" }, { label: shortReportId }]} />`.
    - Snapshot `<pre>` at 444-457: add a copy button (reuse copied-state pattern from lines 121/329-333/395-407).
    - Context section expand/collapse at 496-566: wrap content in a max-h transition:
      ```tsx
      className={cn("overflow-hidden transition-[max-height] duration-300 ease-in-out", contextExpanded ? "max-h-[800px]" : "max-h-0")}
      ```
      and always mount (don't conditional-render).
    - Status selects at 605-617: replace native `<select>` with themed `Select`.
    - Destructive status transitions at 335-357 + 605-628: before calling PATCH for close/wontfix/duplicate, open a `<Confirmation isDestructive>` inside a `<Dialog>`: "Close this issue? This cannot be undone without a status rewind."
    - Admin notes textarea at 637-646: add `<p className="text-[10px] text-right" style={{ color: "var(--text-ghost)" }}>{notes.length}/2000</p>` below; enforce a 2000-char limit via `maxLength` or `onChange` guard.
    - Save success banner at 584-595: migrate to toast (`toast({ title: "Changes saved" })`) now that Toaster is mounted (from 39-01 K2). Remove the inline `success` banner.
    - Copy full report button at 395-407: wrap in a `<span aria-live="polite" className="sr-only">{copied ? "Full report copied to clipboard" : ""}</span>`.

    Imports: `Select*`, `Breadcrumbs`, `Confirmation`, `Dialog`, `useToast`.
  </action>
  <verify>
    <automated>grep -E "from \"@/components/ui/select\"" src/app/admin/reports/reports-table.tsx && grep -E "Breadcrumbs" src/app/admin/reports/[id]/report-detail.tsx && grep -E "Confirmation" src/app/admin/reports/[id]/report-detail.tsx && grep -E "useToast|toast\(" src/app/admin/reports/[id]/report-detail.tsx</automated>
  </verify>
  <done>
    - Themed Select in both report files.
    - Breadcrumbs replace lone back link.
    - Destructive status transitions gated by Confirmation.
    - Save success migrated to toast.
    - Copy snapshot button added.
    - Clear filters button appears when filters active.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Admin analytics placeholder + automations polish</name>
  <read_first>
    - src/app/admin/analytics/page.tsx (lines 1-34)
    - src/app/admin/automations/page.tsx (lines 67, 84-93, 90-93, 115-127, 124-127, 131-138, 155-166)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (lines 303-318 analytics; 475-516 automations)
  </read_first>
  <files>src/app/admin/analytics/page.tsx, src/app/admin/automations/page.tsx</files>
  <action>
    **A. `analytics/page.tsx`:**
    - Replace static placeholder boxes (six 112px + one 300px) with `<Skeleton>` primitives from DS (which use the shimmer-skeleton class):
      - 6 × `<Skeleton className="h-28 rounded-xl" />`
      - 1 × `<Skeleton className="h-[300px] rounded-xl" />`
    - Add exit text near the Coming Soon pill: `<p className="text-xs text-muted-foreground mt-2">In the meantime, see the <Link href="/admin">Command Center</Link> for real-time metrics.</p>`.

    **B. `automations/page.tsx`:**
    - Refresh button hover at lines 67 + 115-127: delete `refreshHovered` state + onMouseEnter/Leave handlers. Replace with Tailwind:
      ```
      className="p-2 rounded-[8px] transition-colors duration-200 text-[var(--text-secondary-ds)] hover:text-[var(--gold-primary)] hover:bg-[var(--gold-bg)] focus-visible:ring-1 focus-visible:ring-[var(--gold-primary)]/40"
      ```
    - Replace native `title="Refresh"` at 124-127 with `<Tooltip>`.
    - Polling at 90-93: same visibility guard as admin dashboard — `if (document.visibilityState === "visible") fetchAll(true);`.
    - "Last updated" indicator (84-93): mirror the pattern from admin dashboard page.tsx:78-82. Extract `formatSecondsAgo` into `src/lib/utils.ts` (if not already a shared util) and consume here.
    - Error banner at 131-138: change `rounded-[14px] p-4` to `rounded-[8px] border px-4 py-3` to match admin's alert banner convention.
    - AutomationHealthCard skeleton (155-166): change `Array.from({ length: 2 })` to `Array.from({ length: 4 })` to match the grid's `md:grid-cols-2` + likely 2-row layout.

    Imports: Tooltip, Skeleton.
  </action>
  <verify>
    <automated>grep -E "Skeleton" src/app/admin/analytics/page.tsx && grep -E "visibilityState" src/app/admin/automations/page.tsx && ! grep -E "refreshHovered" src/app/admin/automations/page.tsx</automated>
  </verify>
  <done>
    - Analytics placeholder uses Skeleton primitives.
    - Automations hover state uses CSS classes (no imperative state).
    - Visibility guard present.
    - Error banner radius matches admin convention.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Admin api-keys integration-card polish + page-level Refresh demotion + card skeletons</name>
  <read_first>
    - src/components/admin/api-keys/integration-card.tsx (lines 126-136, 219-244)
    - src/app/admin/api-keys/page.tsx (lines 77-92, 127-134)
    - .planning/audits/ux-polish-2026-04-14/01-super-admin.md (lines 428-470)
    - .planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-02-PLAN.md Task 6 (lines 394-413) — Plan 39-02 wired `pendingDisableMock` state + `<Confirmation isDestructive>` dialog onto integration-card.tsx. This task MUST preserve that wiring intact.
    - PRESERVE: the `pendingDisableMock` state handler, the Confirmation dialog JSX, and the `onConfirm`/`onCancel` handlers added by 39-02. Only add env-var icons, test-result timestamp, Refresh demotion, and card skeletons. Do NOT remove or rename any 39-02 additions.
  </read_first>
  <files>src/components/admin/api-keys/integration-card.tsx, src/app/admin/api-keys/page.tsx</files>
  <action>
    **A. `integration-card.tsx`:**
    - Env var previews at 126-136: add an `<XCircle className="h-3 w-3" />` icon next to unset values, `<CheckCircle2 className="h-3 w-3" />` next to set ones.
    - Test result at 219-244: add a `latestTestedAt` timestamp displayed as "Tested 3m ago" — extract or reuse `formatSecondsAgo` from Task 7.

    **B. `page.tsx`:**
    - Demote page-level Refresh (77-92) to `variant="ghost"` or an icon-only `<RotateCw>` button.
    - Loading state (127-134): replace bare spinner with 6 card skeletons matching IntegrationCard dimensions. Reuse `animate-pulse` bar pattern from `tenant-heatmap.tsx` or use `<Skeleton>` + `<ShimmerBlock>` primitives.

    Imports: `XCircle, CheckCircle2, RotateCw` from lucide-react; `Skeleton` from `@/components/ui/skeleton`.
  </action>
  <verify>
    <automated>grep -E "XCircle|CheckCircle2" src/components/admin/api-keys/integration-card.tsx && grep -E "Skeleton|ShimmerBlock" src/app/admin/api-keys/page.tsx</automated>
  </verify>
  <done>
    - Env var icons present.
    - Test timestamp rendered.
    - Page-level Refresh is now ghost/icon-only.
    - Card skeletons on loading.
    - `pnpm build` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Admin surface walkthrough — all areas touched, no regressions</name>
  <what-built>
    25+ admin findings closed across layout/dashboard/tenants/users/reports/analytics/api-keys/automations. Summary:
    - Native selects replaced with themed Select in 3 screens.
    - Breadcrumbs mounted on 3 deep pages.
    - EmptyState primitive replaces raw "No X" in tenant + user tables.
    - Admin nav gets CSS hover + focus rings + Soon pills + Tooltip on collapse.
    - Dashboard has layout-matching skeleton + visibility-guarded polling + force-refresh button.
    - Heatmap ghost buttons either wired or disabled with tooltip.
    - Reports detail has destructive Confirmation on close.
    - Toasts replace inline banners across create-tenant, save-report, tenant-status-toggle.
  </what-built>
  <how-to-verify>
    Run `pnpm dev`. Log in as super_admin. Walk through each area:

    1. **Layout + sidebar:**
       - Tab through nav items → see gold focus ring (not just hover).
       - Hover System Config stub link → see "Soon" pill + themed tooltip (not native title).
       - Click sidebar collapse → icon rotates; tooltip appears on hover.

    2. **/admin (Dashboard):**
       - Hard refresh → loading skeleton matches real layout shape.
       - Hover "Updated Xs ago" → see Force Refresh tooltip + clickable.
       - Hover "OPERATIONAL" → see threshold explanation tooltip.
       - Switch to another tab for 2 min, come back → polling resumed (no hammering while hidden — check network tab).
       - Heatmap → "Provision New Tenant" either routes or is disabled (not silent no-op).
       - Error feed → pulse dot is green when no errors, red when errors.

    3. **/admin/tenants:**
       - Sticky header stays visible when scrolling 20+ rows.
       - Empty state (in a fresh tenant DB, or force via filter) → EmptyState primitive.
       - Tab to a row → gold outline visible.
       - Toggle a tenant's status → toast fires (not just text flip).

    4. **/admin/tenants/new:**
       - Breadcrumbs at top (Admin → Tenants → Create).
       - Type a name → slug auto-fills. Type in slug to override → stops auto-filling.
       - Invalid slug on blur → red inline error.
       - Fill form, click Cancel → see Discard confirmation dialog.
       - Submit → spinner on button; on success toast appears, route changes.

    5. **/admin/users:**
       - Filter bar at top: tenant select + role select + search input all filter client-side.
       - Tenant Select is themed (not native browser dropdown).
       - Role badges visually differ (super_admin solid gold vs assistant outline).
       - Row action dropdown has Resend invite + Deactivate + Edit role (or Coming soon).
       - Copy email toasts "Copied".

    6. **/admin/users/new:**
       - Breadcrumbs.
       - Role select + tenant select are themed (dark popover, not OS widget).
       - Loading tenants → skeleton, not plain text.
       - Cmd+Enter submits.

    7. **/admin/reports:**
       - Status + category selects are themed.
       - Type in tenant filter → debounced network call (not per-keystroke).
       - Clear filters button appears when any filter active.
       - Loading state shows skeleton rows (not single "Loading…" text).

    8. **/admin/reports/[id]:**
       - Breadcrumbs at top.
       - Snapshot `<pre>` has copy button → toasts "Copied".
       - Context section expand/collapse is smoothly animated (max-height transition).
       - Status select is themed.
       - Attempt to close → Confirmation dialog opens first.
       - Save → toast fires.
       - Admin notes show character counter.

    9. **/admin/analytics:**
       - 6 stat skeletons + 1 chart skeleton animate (shimmer).
       - "Coming Soon" pill has a "see /admin for real-time metrics" exit link.

    10. **/admin/automations:**
        - Refresh button hover is CSS-driven (no flash).
        - Tooltip appears on hover.
        - Last-updated ticker increments.
        - Visibility pauses polling.

    11. **/admin/api-keys:**
        - Env var icons (green check / red X) clarify state.
        - Test result shows timestamp.
        - Loading shows 6 card skeletons.

    Flag any surface where a regression introduced (e.g. select doesn't open, confirmation blocks UI, toast doesn't fire).
  </how-to-verify>
  <resume-signal>Type "approved" or list specific surfaces + findings that regressed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All admin UI polish — underlying server actions + role gates unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Elevation of Privilege | Admin UI polish for super_admin routes | accept | All `/admin/*` routes require super_admin role server-side (existing `requireSuperAdmin` guard). This plan does NOT change access control — only polishes the UI for already-authorized operators. |
| T-39-05-02 | Tampering | Client-side filter on users list | accept | Users list server-renders the full cross-tenant payload (only readable by super_admin). Client filter is presentation-only; no filter-bypass exposes data the user wasn't already authorized to see. |
| T-39-05-03 | Information Disclosure | Copy-to-clipboard on email | accept | Email is already visible on the page. Copy action doesn't expose new data. |

No new attack surface. UI-only polish for admin-authorized operators.
</threat_model>

<verification>
## Phase-level verification

1. **Build passes:** `pnpm build` exits 0.
2. **Grep audits:**
   ```
   grep -RE "from \"@/components/ui/select\"" src/app/admin/users/new/page.tsx src/app/admin/reports/reports-table.tsx src/app/admin/reports/\[id\]/report-detail.tsx
   # All 3 expected.
   grep -RE "Breadcrumbs" src/app/admin/tenants/new/page.tsx src/app/admin/users/new/page.tsx src/app/admin/reports/\[id\]/report-detail.tsx
   # All 3 expected.
   grep -RE "EmptyState" src/app/admin/tenants/tenant-table.tsx src/app/admin/users/page.tsx
   # All 2 expected.
   grep -RE "visibilityState" src/app/admin/page.tsx src/app/admin/automations/page.tsx
   # Both expected.
   ```
3. **Manual admin walkthrough** per Task 9 above.
</verification>

<success_criteria>
- [ ] 17 files modified per list.
- [ ] All 3 native `<select>` → themed `<Select>` migrations (users/new, reports-table, report-detail).
- [ ] 3 Breadcrumbs mounted (tenants/new, users/new, report-detail).
- [ ] EmptyState replaces raw "No X" in tenants + users tables.
- [ ] Admin dashboard + automations pause polling when tab hidden.
- [ ] Toasts replace inline banners (status-toggle, create-tenant, save-report).
- [ ] Destructive report status transitions gated by Confirmation.
- [ ] No regression to authz — admin routes still super_admin-only.
- [ ] `pnpm build` exits 0.
- [ ] Manual walkthrough (Task 9) passes 11 spot-checks.
</success_criteria>

<output>
After completion, create `.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-05-SUMMARY.md`:
- Checklist of audit 01-super-admin.md findings closed (reference line numbers).
- List of remaining audit-01 findings intentionally deferred (e.g. Task 5's "Edit role" might be Coming Soon if no server action exists).
- Files touched and net LOC delta.
- Manual walkthrough notes from Task 9.
</output>
