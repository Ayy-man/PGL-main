---
phase: 39-ux-polish-pass-luxury-consistency-and-keystone-primitives
plan: 08
type: execute
wave: 6
depends_on:
  - 39-01
  - 39-05
files_modified:
  - src/components/layout/sidebar.tsx
  - src/app/admin/admin-sidebar.tsx
  - src/components/layout/nav-items.tsx
  - src/components/layout/top-bar.tsx
  - src/components/layout/command-search.tsx
  - src/components/layout/mobile-sidebar.tsx
  - src/components/layout/mobile-bottom-nav.tsx
  - src/components/ui/toast.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/app/[orgId]/error.tsx
  - src/app/admin/error.tsx
  - src/app/global-error.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - PHASE-39-SCREEN-SHELL

must_haves:
  truths:
    - "Sidebar collapse fades labels instead of hard-snapping"
    - "Sidebar and admin-sidebar both use ease-out transition"
    - "Tenant sidebar collapse toggle uses CSS hover (no onMouseEnter state)"
    - "Cmd+\\ or Cmd+B toggles sidebar"
    - "Sidebar header is a Link to dashboard (clicks return to /)"
    - "Active nav item has left gold accent pill"
    - "Saved Searches nav uses Bookmark icon (distinct from Team's Users icon)"
    - "TopBar bell is either functional or visibly disabled (no dead affordance)"
    - "TopBar avatar has a real click target (Link to /settings)"
    - "Mobile header has Search button that opens CommandSearch"
    - "Admin shell CommandSearch hides Cmd+K kbd (no dead shortcut hint)"
    - "CommandSearch trigger uses real button (not div role=button)"
    - "Toast close X is opacity-60 by default (touch-visible)"
    - "Toast viewport mobile-anchors to bottom (not top)"
    - "Dialog and Sheet overlays have backdrop-blur-sm"
    - "Sheet has shadow-2xl + var(--bg-floating) + border (matches Dialog)"
    - "Dialog close button has 40px touch target + hover gold"
    - "Error pages use EmptyState variant=error (deduplicated) + serif font-semibold"
    - "A .focus-ring-gold utility exists in globals.css"
    - "Tenant sidebar footer has a user card matching admin sidebar"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "fade labels, ease-out, Link header, Cmd+\\\\ handler, user card footer"
      contains: "Link"
    - path: "src/components/layout/nav-items.tsx"
      provides: "left gold accent pill, Bookmark icon for Saved Searches"
      contains: "Bookmark"
    - path: "src/components/layout/top-bar.tsx"
      provides: "bell disabled or wired, avatar Link"
      contains: "Link"
    - path: "src/components/layout/command-search.tsx"
      provides: "onOpenAutoFocus, real button trigger, admin hides kbd"
      contains: "onOpenAutoFocus"
    - path: "src/components/ui/dialog.tsx"
      provides: "backdrop-blur-sm, 40px close target, hover gold"
      contains: "backdrop-blur"
    - path: "src/components/ui/sheet.tsx"
      provides: "var(--bg-floating), border, shadow-2xl, backdrop-blur, SheetTitle font-serif"
      contains: "font-serif"
    - path: "src/app/global-error.tsx"
      provides: "font-semibold serif"
      contains: "font-serif"
    - path: "src/app/[orgId]/error.tsx"
      provides: "EmptyState variant=error"
      contains: "EmptyState"
    - path: "src/app/admin/error.tsx"
      provides: "EmptyState variant=error"
      contains: "EmptyState"
    - path: "src/app/globals.css"
      provides: ".focus-ring-gold utility"
      contains: "focus-ring-gold"
  key_links:
    - from: "sidebar.tsx + admin-sidebar.tsx"
      to: "next/link"
      via: "header block wrapped in Link"
      pattern: "<Link"
    - from: "nav-items.tsx"
      to: "lucide-react Bookmark icon"
      via: "Saved Searches icon distinction"
      pattern: "Bookmark"
    - from: "Dialog + Sheet overlays"
      to: "backdrop-blur-sm CSS utility"
      via: "visual modal separation"
      pattern: "backdrop-blur"
---

<objective>
Close remaining findings in 04-shell-cross-cutting.md not absorbed by keystones. Scope: sidebar chrome (tenant + admin), nav items, top bar, command palette, mobile shell, toast viewport anchoring, modals/sheets overlay + close + SheetTitle font, error pages, focus-ring utility consolidation.

Purpose: The shell is the persistent chrome every user sees all day. Today admin and tenant shells diverge in 7 visible ways. Mobile has no search entry. Sidebar labels hard-snap. TopBar has two dead affordances. CommandSearch in admin shows Cmd+K hint for a non-functional control. Close the "generic SaaS" cracks.

Output: 14 files modified. 30+ findings closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-CONTEXT.md
@.planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md
@.planning/audits/ux-polish-2026-04-14/05-design-system.md
@src/app/globals.css
@tailwind.config.ts

<interfaces>
Existing primitives:
- Link from next/link — wraps sidebar header
- Bookmark from lucide-react — swap for Saved Searches
- Tooltip from @/components/ui/tooltip — TooltipProvider mounted at root (Plan 39-01 K3)
- EmptyState from @/components/ui/empty-state — has variant="error"

Admin sidebar user card pattern (from admin-sidebar.tsx:71-107): rounded card with avatar + name + email + role. Copy into tenant sidebar.

Keyboard shortcut pattern:
useEffect with window keydown listener checking (metaKey or ctrlKey) and key === "\\" or "b", preventDefault + toggle.

Focus-ring utility CSS (to add to globals.css inside @layer utilities):
.focus-ring-gold { @apply focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--gold-primary)] focus-visible:ring-offset-2; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sidebar (tenant + admin) — fade labels, ease-out, CSS hover, keyboard shortcut, Link header, user card footer</name>
  <read_first>
    - src/components/layout/sidebar.tsx (lines 45-51, 54-82, 69, 91, 96-109)
    - src/app/admin/admin-sidebar.tsx (lines 32, 40-62, 71-107)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (Sidebar + Cross-role sections)
  </read_first>
  <files>src/components/layout/sidebar.tsx, src/app/admin/admin-sidebar.tsx</files>
  <action>
    A. sidebar.tsx:
    - Line 45: change transition-[width] duration-200 ease-in-out to transition-[width] duration-300 ease-out.
    - Header block (54-82): wrap in Link href={`/${orgId}`} className="cursor-pointer hover:bg-white/[0.02] transition-colors rounded-[8px]".
    - Tenant-name span (69) + footer text: wrap labels with className="transition-opacity duration-150" style={{ opacity: collapsed ? 0 : 1, pointerEvents: collapsed ? "none" : "auto" }}. Keep nodes mounted.
    - Collapse toggle (96-109): delete setToggleHovered state + onMouseEnter/Leave. Replace with Tailwind group-hover classes: hover:bg-white/[0.08] hover:border-white/[0.15] border border-white/[0.08] bg-white/[0.04] transition-colors. Update title to "Collapse sidebar (Cmd+\\)".
    - Add Cmd+\\ keyboard handler that calls the existing toggle function.
    - Footer: replace "Phronesis v1.0" with a user card matching admin-sidebar.tsx:71-107. Inline the JSX — do NOT extract a shared component (CONTEXT.md locks no-new-primitives for this phase). Read the SessionUser from props/context (grep how admin-sidebar receives user). If the two sidebars diverge in a future phase, a shared primitive can be introduced then.

    B. admin-sidebar.tsx:
    - Line 32: change transition-all duration-200 to transition-[width] duration-200 ease-out.
    - Header block (40-62): wrap in Link href="/admin" with same class pattern as tenant.
    - Optional: unify header shape — pick 40x40 rounded-lg for both tenant + admin.

    Imports: Link from next/link.
  </action>
  <verify>
    <automated>grep -E "<Link" src/components/layout/sidebar.tsx && grep -E "<Link" src/app/admin/admin-sidebar.tsx && grep -E "ease-out" src/components/layout/sidebar.tsx && ! grep -E "setToggleHovered" src/components/layout/sidebar.tsx</automated>
  </verify>
  <done>
    - Both sidebar headers are Links to dashboard.
    - ease-out transitions.
    - No toggleHovered state.
    - Tenant sidebar has user card footer.
    - Cmd+\\ keyboard shortcut registered.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NavItems — left gold accent pill + Bookmark icon for Saved Searches + ghost-hover</name>
  <read_first>
    - src/components/layout/nav-items.tsx (lines 36, 41, 67-83, 71-73)
    - src/app/globals.css (.ghost-hover ~293-295; admin-section-heading::before ~452-466 for left-pill pattern)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 56-96)
  </read_first>
  <files>src/components/layout/nav-items.tsx</files>
  <action>
    - Line 36 (Saved Searches): change icon from Users to Bookmark. Add Bookmark to imports.
    - Line 41 (Team): keep Users icon.
    - Link render (67-83): make Link className include "relative" so pill can position absolutely. Add conditional pill: when isActive, render `<span className="absolute left-0 top-1/2 -translate-y-1/2 h-5 w-[3px] rounded-r-full bg-[var(--gold-primary)] transition-opacity" aria-hidden />`.
    - Lines 71-73: replace transition-all duration-200 + inline conditional hover with `ghost-hover transition-[background-color,color]`. Keep active style (gold-bg + gold-primary text).
  </action>
  <verify>
    <automated>grep -E "Bookmark" src/components/layout/nav-items.tsx && grep -E "bg-\[var\(--gold-primary\)\]" src/components/layout/nav-items.tsx && grep -E "relative" src/components/layout/nav-items.tsx</automated>
  </verify>
  <done>
    - Saved Searches uses Bookmark icon.
    - Active nav item has left gold accent pill.
    - Narrow transition; ghost-hover used.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TopBar — disable/remove bell, avatar Link, CSS hover, mobile search entry</name>
  <read_first>
    - src/components/layout/top-bar.tsx (lines 16, 23-25, 28-46, 36-43, 48-62)
    - src/components/layout/mobile-sidebar.tsx (lines 56-67)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 100-134)
  </read_first>
  <files>src/components/layout/top-bar.tsx, src/components/layout/mobile-sidebar.tsx</files>
  <action>
    A. top-bar.tsx:
    - Bell (28-46): prefer REMOVE the button entirely (no notifications feature exists). Alternative: add disabled, change aria-label to "Notifications — coming soon", wrap in Tooltip, apply opacity-50 + cursor-not-allowed.
    - Bell hover (36-43): delete inline onMouseEnter/onMouseLeave. If keeping bell, use className="border border-[var(--border-subtle)] hover:border-[var(--border-hover)] text-[var(--text-secondary-ds)] hover:text-[var(--text-primary-ds)] transition-colors duration-200".
    - Avatar (48-62): wrap in Link href={`/${orgId}/settings`}. Remove redundant cursor-pointer if Link covers it.

    B. mobile-sidebar.tsx (56-67):
    - Add a Search icon button to the mobile header. Fire a custom DOM event when clicked: `window.dispatchEvent(new CustomEvent('command-search:open'))`.
    - CommandSearch listens for this event (wired in Task 4).

    Imports: Link from next/link; Search from lucide-react.
  </action>
  <verify>
    <automated>grep -E "<Link" src/components/layout/top-bar.tsx && ! grep -E "onMouseEnter.*borderColor" src/components/layout/top-bar.tsx && grep -E "command-search:open" src/components/layout/mobile-sidebar.tsx</automated>
  </verify>
  <done>
    - Bell removed or visibly disabled.
    - Avatar wrapped in Link to settings.
    - No inline hover handlers.
    - Mobile header has Search button firing command-search:open event.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CommandSearch — skew animation, onOpenAutoFocus, real button trigger, admin hides kbd, focus ring, listen for open event</name>
  <read_first>
    - src/components/layout/command-search.tsx (lines 57-66, 70-72, 195-204, 227-237, 270, 332-363)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 140-190)
  </read_first>
  <files>src/components/layout/command-search.tsx</files>
  <action>
    - Line 270: remove slide-in-from-left-1/2 and slide-out-to-left-1/2 from the Content className (they skew the centered palette). Keep zoom-in-95 fade-in-0.
    - Lines 70-72: replace setTimeout(() => inputRef.current?.focus(), 50) with Radix onOpenAutoFocus on Content: onOpenAutoFocus={(e) => { e.preventDefault(); inputRef.current?.focus(); }}.
    - Trigger (227-237): replace <div role="button" tabIndex={0} onKeyDown={...}> with <button type="button" className="text-left w-full max-w-[320px] cursor-pointer">. Drop onKeyDown — Radix Trigger handles Enter/Space on native buttons.
    - Result buttons (195-204): add focus-visible:ring-1 focus-visible:ring-[var(--border-gold)] focus-visible:ring-inset focus:outline-none.
    - Admin fallback (332-363): return null when !orgId (hide the widget entirely). Removes the dead Cmd+K hint.
    - Add a window.addEventListener('command-search:open', () => setOpen(true)) in a useEffect, cleaned up on unmount. Wires the mobile search trigger.

    Preserve the existing Cmd+K keyboard handler at 57-66.
  </action>
  <verify>
    <automated>! grep -E "slide-in-from-left-1/2" src/components/layout/command-search.tsx && grep -E "onOpenAutoFocus" src/components/layout/command-search.tsx && grep -E "command-search:open" src/components/layout/command-search.tsx</automated>
  </verify>
  <done>
    - slide-in-from-left-1/2 removed.
    - onOpenAutoFocus replaces setTimeout.
    - Trigger is real button.
    - Result buttons have focus rings.
    - Admin shell hides CommandSearch (returns null).
    - Listens for command-search:open event.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Toast viewport mobile anchoring + close opacity + action hover gold + destructive focus tokens</name>
  <read_first>
    - src/components/ui/toast.tsx (lines 12-23 viewport, 65 ToastAction, 79-83 close, 80 destructive)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 213-231)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (lines 358-376)
    - NOTE: Plan 39-01 K5 handled toast default variant bg + serif title. This is the remaining viewport/close/action/destructive polish.
  </read_first>
  <files>src/components/ui/toast.tsx</files>
  <action>
    - Viewport (12-23): change to bottom-anchored on mobile + desktop (consistent direction). className="fixed bottom-0 z-[100] flex max-h-screen w-full flex-col p-4 pb-[calc(3.5rem+env(safe-area-inset-bottom))] sm:right-0 sm:p-4 sm:pb-4 md:max-w-[420px]".
    - Root Toast animation: swap slide-in-from-top-full to slide-in-from-bottom-full on mobile so direction matches.
    - Close button (79-83): change opacity-0 group-hover:opacity-100 to opacity-60 hover:opacity-100.
    - ToastAction (65): replace hover:bg-secondary with hover:bg-[var(--gold-bg)] hover:text-[var(--gold-primary)] hover:border-[var(--border-gold)].
    - ToastClose destructive (80): replace text-red-300 text-red-50 focus:ring-red-400 focus:ring-offset-red-600 with text-destructive-foreground focus:ring-destructive.
  </action>
  <verify>
    <automated>grep -E "slide-in-from-bottom-full" src/components/ui/toast.tsx && grep -E "opacity-60" src/components/ui/toast.tsx && grep -E "var\(--gold-bg\)" src/components/ui/toast.tsx</automated>
  </verify>
  <done>
    - Toast viewport bottom-anchored.
    - Close opacity-60 default.
    - Action hover gold.
    - Destructive uses tokens.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Dialog + Sheet backdrop-blur + close target + Sheet SheetTitle font-serif + bg/border/shadow</name>
  <read_first>
    - src/components/ui/dialog.tsx (lines 24, 44, 48)
    - src/components/ui/sheet.tsx (lines 24, 33-34, 111)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 237-268)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (lines 240-255)
  </read_first>
  <files>src/components/ui/dialog.tsx, src/components/ui/sheet.tsx</files>
  <action>
    A. dialog.tsx:
    - Overlay (24): add backdrop-blur-sm.
    - Content (44): move inline style bg + border into className: bg-[var(--bg-floating)] border border-[var(--border-default)] so consumers can override via className.
    - Close button (48): change p-2 to p-2.5 and right-3 top-3 to right-2 top-2 for 40px touch target. Add hover:text-[var(--gold-primary)].

    B. sheet.tsx:
    - Overlay (24): add backdrop-blur-sm.
    - sheetVariants (33-34): replace "fixed z-50 gap-4 bg-background p-6 shadow-lg transition ease-in-out ..." with "fixed z-50 gap-4 bg-[var(--bg-floating)] border border-[var(--border-default)] p-6 shadow-2xl transition ease-in-out ...".
    - SheetTitle (111): change "text-lg font-semibold text-foreground" to "font-serif text-lg sm:text-[22px] font-semibold leading-none tracking-tight text-foreground" matching DialogTitle.
  </action>
  <verify>
    <automated>grep -E "backdrop-blur" src/components/ui/dialog.tsx && grep -E "backdrop-blur" src/components/ui/sheet.tsx && grep -E "font-serif" src/components/ui/sheet.tsx && grep -E "hover:text-\[var\(--gold-primary\)\]" src/components/ui/dialog.tsx</automated>
  </verify>
  <done>
    - Both overlays have backdrop-blur-sm.
    - Dialog close is 40px + hover gold.
    - Sheet uses bg-floating + border + shadow-2xl.
    - SheetTitle uses font-serif.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Error pages — font-semibold + EmptyState consolidation + error.digest display</name>
  <read_first>
    - src/app/global-error.tsx (lines 15-17, 22-25)
    - src/app/[orgId]/error.tsx (entire file)
    - src/app/admin/error.tsx (entire file)
    - src/components/ui/empty-state.tsx (confirm variant="error" exists)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 391-409)
  </read_first>
  <files>src/app/global-error.tsx, src/app/[orgId]/error.tsx, src/app/admin/error.tsx</files>
  <action>
    A. global-error.tsx (15-17): change font-serif text-3xl font-bold to font-serif text-3xl font-semibold.

    B. [orgId]/error.tsx and admin/error.tsx:
    - Consolidate both files to use EmptyState variant="error":
      ```tsx
      'use client';
      import { EmptyState } from "@/components/ui/empty-state";
      import { Button } from "@/components/ui/button";
      import { AlertTriangle } from "lucide-react";
      export default function Error({ error, reset }) {
        return (
          <EmptyState variant="error" icon={AlertTriangle} title="Something went wrong" description={error.message || "An unexpected error occurred."}>
            <Button variant="gold" onClick={reset}>Try again</Button>
            {error.digest && <p className="text-xs font-mono text-muted-foreground mt-4">Error ID: {error.digest}</p>}
          </EmptyState>
        );
      }
      ```
    - Both files can be nearly identical. Keep 10-12 lines each.

    Imports: EmptyState, Button, AlertTriangle.
  </action>
  <verify>
    <automated>grep -E "font-semibold" src/app/global-error.tsx && ! grep -E "font-bold" src/app/global-error.tsx && grep -E "EmptyState" src/app/[orgId]/error.tsx && grep -E "EmptyState" src/app/admin/error.tsx</automated>
  </verify>
  <done>
    - global-error uses font-semibold.
    - Both error.tsx files use EmptyState variant=error.
    - error.digest displayed.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Add .focus-ring-gold utility to globals.css + apply to breadcrumbs</name>
  <read_first>
    - src/app/globals.css (find @layer utilities block)
    - src/components/ui/breadcrumbs.tsx (lines 40-47)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 421-425)
    - .planning/audits/ux-polish-2026-04-14/05-design-system.md (lines 332-338)
  </read_first>
  <files>src/app/globals.css, src/components/ui/breadcrumbs.tsx</files>
  <action>
    A. globals.css: Locate the @layer utilities block (or create one at the end). Add:
      ```css
      .focus-ring-gold {
        @apply focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--gold-primary)] focus-visible:ring-offset-2;
      }
      ```
      Confirm @apply works in this file by checking for existing @apply usages.

    B. breadcrumbs.tsx (40-47):
    - Delete onMouseEnter/onMouseLeave handlers.
    - Replace inline color style with className="hover:text-[var(--gold-primary)] focus-ring-gold transition-colors duration-150".
  </action>
  <verify>
    <automated>grep -E "focus-ring-gold" src/app/globals.css && grep -E "focus-ring-gold" src/components/ui/breadcrumbs.tsx && ! grep -E "onMouseEnter.*color" src/components/ui/breadcrumbs.tsx</automated>
  </verify>
  <done>
    - .focus-ring-gold in globals.css.
    - Breadcrumbs use the utility + CSS hover.
    - pnpm build exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Mobile bottom-sheet — remove fake drag handle OR mark aria-hidden</name>
  <read_first>
    - src/components/layout/mobile-bottom-nav.tsx (lines 223-228, 265-271)
    - .planning/audits/ux-polish-2026-04-14/04-shell-cross-cutting.md (lines 452-457)
  </read_first>
  <files>src/components/layout/mobile-bottom-nav.tsx</files>
  <action>
    Drag handles at 223-228 and 267-271 are visual-only (no drag-dismiss gesture). Overlay-tap already dismisses via Radix default.

    Preferred: REMOVE the handle markup from both sheets entirely to eliminate the false affordance.
    Alternative: keep the visual indicator but mark aria-hidden="true" and ensure there's no click/onPointerDown handler implying interactivity.

    Per audit, choice (a) is cleaner. Document the decision in SUMMARY.
  </action>
  <verify>
    <automated>grep -c "drag" src/components/layout/mobile-bottom-nav.tsx; echo ok</automated>
  </verify>
  <done>
    - Drag handles removed OR marked aria-hidden with decision noted.
    - pnpm build exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Manual verification — shell consistency + cross-role parity</name>
  <what-built>
    30+ shell findings closed. Sidebar + top-bar + command-palette + mobile + toast + dialog + sheet + error pages all polished. .focus-ring-gold utility in globals.css.
  </what-built>
  <how-to-verify>
    Run pnpm dev. Walk through both tenant and admin shells:

    1. Sidebar (tenant + admin): Click the header logo → routes to dashboard. Hover collapse toggle → smooth CSS hover. Press Cmd+\\ → sidebar collapses smoothly with labels fading. Tenant sidebar footer shows user card matching admin's pattern.

    2. Nav items: Saved Searches has Bookmark icon (distinct from Team's Users). Active item has a left gold accent pill. Tab through items → gold focus rings (via focus-ring-gold from Task 8 if applied to nav items, or existing rings).

    3. Top bar: Bell is gone OR disabled with tooltip. Clicking avatar routes to /settings. No inline-style flashes on hover.

    4. Command palette: Cmd+K → palette opens with pure fade-scale (no skew). Typing is instant (no 50ms delay). Tab through results → gold focus ring visible. In /admin → CommandSearch widget is gone entirely (no dead Cmd+K hint).

    5. Mobile: Shrink viewport. Mobile header has a Search icon. Tap it → CommandSearch opens.

    6. Toasts: Fire a toast on mobile viewport → appears from bottom (not top). Close X is visible (opacity-60). Try ToastAction hover → gold bg.

    7. Dialogs + Sheets: Open any Dialog → overlay has subtle blur. Close button is larger (easier tap). Hover close → turns gold. Open any Sheet (e.g. prospect slide-over) → title renders in Cormorant Garamond serif (not DM Sans). Sheet has shadow-2xl + var(--bg-floating) surface.

    8. Error pages: Force an error (e.g. throw in a page component). Both tenant + admin error pages render EmptyState variant=error with AlertTriangle + "Something went wrong" + Try Again button + error ID. global-error (test via iframe or by crashing the root layout) uses font-semibold (not font-bold).

    9. Breadcrumbs: Click breadcrumb items → smooth CSS color transition. Tab → gold focus ring. No JS handler jank.

    10. Mobile bottom-sheet: Tap the + button → sheet opens. No drag handle (or handle is aria-hidden). Tap outside → closes.

    Flag any regressions. Common risk: Link-wrapping the sidebar header may change hover behavior on child elements — verify the toggle button (if nested) still works independently.
  </how-to-verify>
  <resume-signal>Type "approved" or list regressed surfaces + findings</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Shell UI polish on existing authenticated chrome.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08-01 | Information Disclosure | Tenant sidebar user card | accept | Same info already surfaced in admin sidebar. RLS ensures the user only sees their own session data. |
| T-39-08-02 | Tampering | Custom DOM event (command-search:open) | accept | Event payload is empty. Listener only opens a dialog; no auth-sensitive action. Any attacker-controlled script already has full DOM access. |
| T-39-08-03 | Spoofing | Avatar wrapped in Link to /settings | accept | Link target is deterministic (`/${orgId}/settings`). No URL manipulation. |
| T-39-08-04 | DoS | Keyboard shortcut handler | accept | Single event listener, debounced by the handled keypress. No amplification. |

No new attack surface.
</threat_model>

<verification>
## Phase-level verification

1. Build passes: pnpm build exits 0.
2. Key grep checks:
   ```
   grep -E "<Link" src/components/layout/sidebar.tsx src/app/admin/admin-sidebar.tsx
   grep -E "Bookmark" src/components/layout/nav-items.tsx
   grep -E "onOpenAutoFocus" src/components/layout/command-search.tsx
   grep -E "backdrop-blur" src/components/ui/dialog.tsx src/components/ui/sheet.tsx
   grep -E "font-serif" src/components/ui/sheet.tsx
   grep -E "EmptyState" src/app/[orgId]/error.tsx src/app/admin/error.tsx
   grep -E "focus-ring-gold" src/app/globals.css src/components/ui/breadcrumbs.tsx
   ```
3. Manual walkthrough per Task 10.
</verification>

<success_criteria>
- [ ] 14 files modified.
- [ ] Sidebars use Link + ease-out + fade labels + CSS hover + Cmd+\\ shortcut + user card footer.
- [ ] NavItems has left gold pill + Bookmark icon for Saved Searches.
- [ ] TopBar bell removed or disabled; avatar wraps Link; mobile header has Search entry.
- [ ] CommandSearch uses onOpenAutoFocus + real button trigger + hides in admin.
- [ ] Toast viewport bottom-anchored mobile; close opacity-60; action hover gold.
- [ ] Dialog + Sheet overlays have backdrop-blur-sm.
- [ ] Sheet has shadow-2xl + var(--bg-floating) + border + font-serif SheetTitle.
- [ ] Dialog close button is 40px touch target + hover gold.
- [ ] Error pages use EmptyState variant=error; global-error uses font-semibold.
- [ ] .focus-ring-gold utility added + breadcrumbs consume it.
- [ ] Mobile sheet drag handles removed or aria-hidden.
- [ ] pnpm build exits 0.
- [ ] Manual walkthrough (Task 10) passes all 10 spot-checks.
</success_criteria>

<output>
After completion, create .planning/phases/39-ux-polish-pass-luxury-consistency-and-keystone-primitives/39-08-SUMMARY.md:
- Checklist of audit 04-shell-cross-cutting.md findings closed with line references.
- Decision on bell (removed vs disabled).
- Decision on mobile sheet drag handle (removed vs aria-hidden).
- Whether sidebar header shape unification landed (both 40x40 rounded-lg?) or deferred.
- Manual walkthrough notes from Task 10.
</output>
