---
phase: 41-tutorial-onboarding-flows
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - src/lib/onboarding/empty-state-copy.ts
  - src/lib/onboarding/__tests__/empty-state-copy.test.ts
  # NOTE: src/app/[orgId]/page.tsx removed 2026-04-15 — Plan 04 owns that file in Wave 3.
  # Plan 04 Task 3 will apply emptyStateCopy("dashboard") to the dashboard as part of its mount work.
  - src/app/[orgId]/lists/components/lists-page-client.tsx
  - src/app/[orgId]/personas/page.tsx
  - src/components/activity/activity-log-viewer.tsx
  - src/app/[orgId]/search/components/advanced-filters-panel.tsx
  - src/app/[orgId]/search/components/bulk-actions-bar.tsx
  - src/components/prospect/profile-view.tsx
  - src/app/[orgId]/search/components/prospect-result-card.tsx
autonomous: true
requirements:
  - PHASE-41-EMPTY-STATE-TUTORIALS
  - PHASE-41-TOOLTIP-HELP
must_haves:
  truths:
    - "Dashboard with zero personas shows an EmptyState with a 'Create your first saved search' CTA linking to /personas"
    - "Lists page with zero lists shows an EmptyState with a 'Create your first list' CTA"
    - "Personas page with zero personas shows an EmptyState with a 'Create your first saved search' CTA"
    - "Activity viewer with zero rows shows an EmptyState with 'Complete your first search to see activity here' CTA"
    - "Copy uses real-estate-agent language — no 'persona' jargon without definition"
    - "Advanced Filters toggle has a Tooltip (1-2 sentences)"
    - "Enrichment status dots have a Tooltip explaining what the dot means"
    - "Bulk actions bar has a Tooltip on its primary Enrich Selection button"
    - "Wealth tier badges have a Tooltip explaining the tier meaning"
    - "emptyStateCopy(surface) pure helper returns canonical copy per surface — tested"
  artifacts:
    - path: src/lib/onboarding/empty-state-copy.ts
      provides: "EMPTY_STATE_COPY map + emptyStateCopy(surface) pure helper"
      exports: ["EMPTY_STATE_COPY", "emptyStateCopy"]
    - path: src/lib/onboarding/__tests__/empty-state-copy.test.ts
      provides: "Coverage of 4 surface keys + unknown key fallback"
  key_links:
    - from: src/app/[orgId]/page.tsx
      to: src/lib/onboarding/empty-state-copy.ts
      via: "emptyStateCopy('dashboard') consumed by existing EmptyState children slot"
      pattern: "emptyStateCopy"
    - from: src/app/[orgId]/search/components/advanced-filters-panel.tsx
      to: src/components/ui/tooltip.tsx
      via: "Tooltip wrapping the toggle control"
      pattern: "Tooltip|TooltipContent"
---

<objective>
Polish the empty-state experience on 4 surfaces and add exactly 4 explanatory Tooltips to dense controls. Zero new abstractions — we extend the existing `EmptyState` primitive via its `children` slot and use the existing shadcn Tooltip.

Purpose: A real estate agent landing on an empty Dashboard / Lists / Personas / Activity page today sees "no data" and bails. We replace that with a one-CTA nudge that says "do this next."

Output: Pure copy map (tested), 4 EmptyState CTA upgrades, 4 Tooltip additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-tutorial-onboarding-flows/41-CONTEXT.md
@src/components/ui/empty-state.tsx
@src/components/ui/tooltip.tsx

Existing `EmptyState` API (children prop is the CTA slot):

```
interface EmptyStateProps {
  icon?: LucideIcon;
  title: string;
  description?: string;
  children?: React.ReactNode;
  className?: string;
  variant?: "default" | "error";
}
```

Tooltip primitive at `src/components/ui/tooltip.tsx` is shadcn over `@radix-ui/react-tooltip`. Standard usage wraps the trigger with `TooltipTrigger asChild` and places explanatory text in `TooltipContent`. Check whether `TooltipProvider` is mounted at the app root before adding redundant providers.

### File ownership note: Plan 02 <-> Plan 05 overlap

Plans 02 and 05 both edit:
- `src/app/[orgId]/search/components/advanced-filters-panel.tsx`
- `src/app/[orgId]/search/components/bulk-actions-bar.tsx`
- `src/components/prospect/profile-view.tsx`

Plan 02 adds a single `data-tour-id` attribute per file. Plan 05 adds a Tooltip wrap. Plan 05 is Wave 3, Plan 02 is Wave 2 — Plan 05 executes after Plan 02 commits. The edits are semantically disjoint (attribute add vs JSX wrap) and will not collide line-wise.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy map + pure helper + unit tests</name>
  <files>src/lib/onboarding/empty-state-copy.ts, src/lib/onboarding/__tests__/empty-state-copy.test.ts</files>
  <behavior>
    - `EMPTY_STATE_COPY` has exactly 4 keys: "dashboard", "lists", "personas", "activity".
    - Each entry has `title`, `body`, `ctaLabel`, and `ctaHref(orgId: string) => string`.
    - `emptyStateCopy("dashboard")` returns the entry; `emptyStateCopy("unknown" as any)` returns a safe fallback (NOT null, NOT throw).
    - The "personas" entry explicitly includes "saved search" in title or body.
  </behavior>
  <action>
Create `src/lib/onboarding/empty-state-copy.ts`:

```
export type EmptyStateSurface = "dashboard" | "lists" | "personas" | "activity";

export interface EmptyStateCopyEntry {
  title: string;
  body: string;
  ctaLabel: string;
  ctaHref: (orgId: string) => string;
}

export const EMPTY_STATE_COPY: Record<EmptyStateSurface, EmptyStateCopyEntry> = {
  dashboard: {
    title: "Find your first prospects",
    body: "Start with a saved search — describe who you want to reach in plain English, then enrich for contacts and wealth signals.",
    ctaLabel: "Create your first saved search",
    ctaHref: (orgId) => `/${orgId}/personas`,
  },
  lists: {
    title: "No lists yet",
    body: "Save enriched prospects into a list to track outreach, take notes, and export when you're ready.",
    ctaLabel: "Create your first list",
    ctaHref: (orgId) => `/${orgId}/lists`,
  },
  personas: {
    title: "Save a search to reuse it",
    body: "A saved search remembers your criteria so you can re-run it anytime. Think of it as a reusable search recipe.",
    ctaLabel: "Create your first saved search",
    ctaHref: (orgId) => `/${orgId}/personas`,
  },
  activity: {
    title: "No activity yet",
    body: "Complete your first search and enrichment to see activity tracked here — every search, every enrichment, every list change.",
    ctaLabel: "Start a search",
    ctaHref: (orgId) => `/${orgId}/search`,
  },
};

const FALLBACK: EmptyStateCopyEntry = {
  title: "Nothing to show yet",
  body: "Get started by creating your first item.",
  ctaLabel: "Get started",
  ctaHref: (orgId) => `/${orgId}`,
};

export function emptyStateCopy(surface: EmptyStateSurface | string): EmptyStateCopyEntry {
  return (EMPTY_STATE_COPY as Record<string, EmptyStateCopyEntry>)[surface] ?? FALLBACK;
}
```

Create `src/lib/onboarding/__tests__/empty-state-copy.test.ts`:

```
import { describe, it, expect } from "vitest";
import { EMPTY_STATE_COPY, emptyStateCopy } from "../empty-state-copy";

describe("EMPTY_STATE_COPY", () => {
  it("has exactly 4 known surfaces", () => {
    expect(Object.keys(EMPTY_STATE_COPY).sort()).toEqual(["activity", "dashboard", "lists", "personas"]);
  });
  it("personas copy defines 'saved search' in agent-friendly terms", () => {
    const text = [
      EMPTY_STATE_COPY.personas.title,
      EMPTY_STATE_COPY.personas.body,
      EMPTY_STATE_COPY.personas.ctaLabel,
    ].join(" ").toLowerCase();
    expect(text).toContain("saved search");
  });
  it("ctaHref substitutes orgId", () => {
    expect(EMPTY_STATE_COPY.dashboard.ctaHref("abc")).toBe("/abc/personas");
    expect(EMPTY_STATE_COPY.activity.ctaHref("xyz")).toBe("/xyz/search");
  });
});

describe("emptyStateCopy", () => {
  it("returns entry for known surface", () => {
    expect(emptyStateCopy("dashboard").title).toMatch(/prospects/i);
  });
  it("returns fallback for unknown surface (no null, no throw)", () => {
    const fb = emptyStateCopy("not-a-real-surface");
    expect(fb.title).toBeTruthy();
    expect(typeof fb.ctaHref).toBe("function");
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run src/lib/onboarding/__tests__/empty-state-copy.test.ts --reporter=verbose</automated>
  </verify>
  <done>All tests green; "persona" jargon proven absent without definition; fallback behavior covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update 4 EmptyState usages with CTA children</name>
  <files>src/app/[orgId]/page.tsx, src/app/[orgId]/lists/components/lists-page-client.tsx, src/app/[orgId]/personas/page.tsx, src/components/activity/activity-log-viewer.tsx</files>
  <behavior>
    - Each surface renders EmptyState with a Button-as-Link child sourced from `EMPTY_STATE_COPY`.
    - Copy matches the helper (no inline strings — consume from helper so future copy changes are one-file edits).
    - No other changes to these files.
  </behavior>
  <action>
For each file, locate the existing `<EmptyState` call site (grep within the file). Replace its title/description/children with helper-driven values using this pattern:

```
import Link from "next/link";
import { EmptyState } from "@/components/ui/empty-state";
import { Button } from "@/components/ui/button";
import { emptyStateCopy } from "@/lib/onboarding/empty-state-copy";

const copy = emptyStateCopy("dashboard"); // or "lists" / "personas" / "activity"

return (
  <EmptyState title={copy.title} description={copy.body}>
    <Button asChild>
      <Link href={copy.ctaHref(orgId)}>{copy.ctaLabel}</Link>
    </Button>
  </EmptyState>
);
```

Surface mapping:
- `src/app/[orgId]/page.tsx` — use `emptyStateCopy("dashboard")` for the dashboard getting-started empty slot (NOT the admin checklist slot — those are independent).
- `src/app/[orgId]/lists/components/lists-page-client.tsx` — use `emptyStateCopy("lists")` when `lists.length === 0`.
- `src/app/[orgId]/personas/page.tsx` — use `emptyStateCopy("personas")` when `personas.length === 0`.
- `src/components/activity/activity-log-viewer.tsx` — use `emptyStateCopy("activity")` when the activity array is empty.

For `activity-log-viewer.tsx`: if the component doesn't have access to `orgId`, accept `orgId` as a new optional prop OR use `useParams<{orgId:string}>()` if it's already a client component. Minimal change only.

If a surface doesn't currently render an `<EmptyState>` at all, add one using the same pattern.

Constraint — minimal edits: add the import(s), call the helper, render children. No refactor, no style changes, no layout changes.
  </action>
  <verify>
    <automated>set -e; npx tsc --noEmit; FILES=$(grep -rln "emptyStateCopy(" src/app src/components | wc -l); echo "emptyStateCopy call sites: $FILES"; test "$FILES" -ge 4</automated>
  </verify>
  <done>tsc clean; helper imported in all 4 files; no literal copy strings duplicated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 4 Tooltips to dense controls</name>
  <files>src/app/[orgId]/search/components/advanced-filters-panel.tsx, src/app/[orgId]/search/components/bulk-actions-bar.tsx, src/components/prospect/profile-view.tsx, src/app/[orgId]/search/components/prospect-result-card.tsx</files>
  <behavior>
    - Advanced Filters toggle wrapped in Tooltip: "Narrow results by industry, title, location, wealth tier, and more."
    - Enrichment status dots in profile view each wrapped in Tooltip with a one-sentence explanation per dot (contact, web signals, SEC filings, AI summary).
    - Bulk actions bar Enrich Selection button wrapped in Tooltip: "Uses about 1 credit per prospect. Pulls email, phone, wealth signals, and recent news."
    - Wealth tier badge on prospect-result-card wrapped in Tooltip: "Estimated net worth. Tier is derived from public filings, company ownership, and observed signals."
    - Each Tooltip uses existing shadcn primitive; copy is 1-2 sentences max.
  </behavior>
  <action>
For each file, wrap the specific control with the Tooltip pattern:

```
<Tooltip>
  <TooltipTrigger asChild>
    {/* existing control JSX */}
  </TooltipTrigger>
  <TooltipContent>Short explanation.</TooltipContent>
</Tooltip>
```

1. `advanced-filters-panel.tsx` — wrap the toggle button (the one that expands/collapses the filters panel). After Plan 02, this element carries `data-tour-id="advanced-filters-toggle"` — keep that attribute on the inner element; the Tooltip is the outer wrapper.

2. `bulk-actions-bar.tsx` — wrap the primary Enrich Selection Button. After Plan 02, the root element carries `data-tour-id="bulk-actions-bar"` — keep that attribute on the outer div; wrap only the specific Button in the Tooltip.

3. `profile-view.tsx` — locate the enrichment status dots component (grep for "dot" / "status-dot" / specific source labels). Wrap each dot individually so each has its own copy. After Plan 02, this file carries `data-tour-id="profile-summary"` — unrelated, leave it.

4. `prospect-result-card.tsx` — locate the wealth tier badge (grep for "tier" / "Wealth" / "Tier"). Wrap in Tooltip with the wealth-tier explanation.

If `TooltipProvider` is not already mounted at the app root (check `src/app/layout.tsx`), add it there ONCE — do not mount per-component. Providers at the leaf level cause re-mount thrash on hover.

Constraint — minimal edits: add import(s) for `Tooltip`, `TooltipTrigger`, `TooltipContent` and wrap the control. No restyling, no layout changes.

No component tests — matches phase test strategy (no RTL). Tooltip copy may live inline since there's no reuse.
  </action>
  <verify>
    <automated>set -e; npx tsc --noEmit; COUNT=$(grep -l "TooltipContent" src/app/\[orgId\]/search/components/advanced-filters-panel.tsx src/app/\[orgId\]/search/components/bulk-actions-bar.tsx src/components/prospect/profile-view.tsx src/app/\[orgId\]/search/components/prospect-result-card.tsx 2>/dev/null | wc -l); echo "tooltip files: $COUNT"; test "$COUNT" -ge 4</automated>
  </verify>
  <done>tsc clean; 4 files each contain a TooltipContent usage; TooltipProvider lives at app root only.</done>
</task>

</tasks>

<verification>
- `rg 'emptyStateCopy\(' src/app src/components` — at least 4 distinct file matches.
- `rg 'TooltipContent' src/app/\[orgId\]/search/components/ src/components/prospect/` — at least 4 matches.
- `npx vitest run src/lib/onboarding/__tests__/empty-state-copy.test.ts` — all green.
- `npx tsc --noEmit` — clean.
- Grep sanity: confirm no literal copy "Create your first list" appears outside the helper file.
</verification>

<success_criteria>
- Dashboard (zero personas), Lists (zero lists), Personas (zero personas), Activity (zero activity) all show an EmptyState with a visible CTA Button that deep-links to the relevant create flow.
- Advanced Filters toggle shows a tooltip on hover.
- Enrichment dots each show a source-specific tooltip.
- Enrich Selection button shows a credit-cost tooltip.
- Wealth tier badge shows a tier explanation on hover.
- All copy is agent-friendly (no "persona" without "saved search" context).
</success_criteria>

<output>
After completion, create `.planning/phases/41-tutorial-onboarding-flows/41-05-SUMMARY.md` documenting:
- The 4 EmptyState surfaces and their new CTA wiring.
- Where `TooltipProvider` lives (root layout vs leaf).
- The 4 tooltip sites (file + which control).
- The pure `emptyStateCopy` helper and its test coverage.
</output>
