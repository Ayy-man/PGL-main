---
phase: 40-instant-ux-pass-demo-critical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md
autonomous: true
requirements:
  - PHASE-40-AUDIT-DOC
must_haves:
  truths:
    - "40-AUDIT.md exists in the phase directory listing all 28 mutating surfaces"
    - "Every surface has a strategy column: optimistic | realtime | skeleton | none"
    - "Every surface has a phase assignment: 40 | 40.1"
    - "The 11 in-scope Phase 40 surfaces match the table in 40-CONTEXT.md specifics section"
    - "The ~17 parked surfaces match the Deferred Ideas section in 40-CONTEXT.md"
    - "The ~8 unverified admin routes from recon are confirmed present or marked as not-found"
  artifacts:
    - path: ".planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md"
      provides: "28-surface audit table with strategy + phase column"
      min_lines: 120
      contains: "| Surface | File | Strategy | Phase |"
  key_links:
    - from: "40-AUDIT.md"
      to: "Phase 40.1 planning"
      via: "phase column filter where Phase = 40.1"
      pattern: "40\\.1"
---

<objective>
Produce the canonical audit artifact `40-AUDIT.md` listing every confirmed mutating surface in the app with (a) its remediation strategy and (b) the phase in which it ships. This is the source-of-truth handoff document for Phase 40.1 so no work gets lost.

Purpose: The recon agent found 28 confirmed mutating surfaces + ~8 unverified admin routes. Phase 40 only addresses 11. The remaining ~17 + any of the 8 that actually exist need to be enumerated now so the developer has a checklist when they start 40.1 after the Maggie demo.

Output: `.planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-instant-ux-pass-demo-critical-slice/40-CONTEXT.md
@.planning/ROADMAP.md

The recon-derived surface list is baked into CONTEXT under `<specifics>` (the 11 in-scope table) and `<deferred>` (the ~17 parked). The ~8 unverified admin surfaces need filesystem verification before they go into the audit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify unverified admin routes on disk</name>
  <files>(read-only discovery — no files modified)</files>
  <action>
Run filesystem checks for each suspected admin route from recon. Use the Grep/Glob tools (not bash find). Expected candidates to verify:

- `src/app/api/admin/tenants/route.ts` (createTenantAction / toggleTenantStatusAction invocation path)
- `src/app/api/admin/tenants/[tenantId]/route.ts`
- `src/app/api/admin/api-keys/**/route.ts`
- `src/app/api/admin/users/**/route.ts`
- `src/app/api/admin/automations/**/route.ts`
- Any `actions.ts` under `src/app/admin/**`

For each candidate, record: exists (yes/no), HTTP methods exported (POST/PATCH/DELETE), one-line purpose. This list feeds Task 2. Do NOT modify any code. Output goes directly into the audit doc in Task 2.
  </action>
  <verify>
    <automated>test -d src/app/api/admin &amp;&amp; find src/app/api/admin -name "route.ts" | wc -l</automated>
  </verify>
  <done>A scratch list of verified admin routes is in hand (in the agent's working memory or a brief scratch note) with method + existence status for each candidate. No files modified yet.</done>
</task>

<task type="auto">
  <name>Task 2: Write 40-AUDIT.md with full 28-surface table</name>
  <files>.planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md</files>
  <action>
Create `40-AUDIT.md` with the following structure (use Write tool, not heredoc):

```markdown
# Phase 40 — Mutating Surface Audit

**Generated:** 2026-04-15 by Plan 40-01
**Source:** Recon agent scan + 40-CONTEXT.md + Task 1 admin-route verification
**Purpose:** Enumerate every mutating surface in the app with its chosen instant-UX strategy and the phase in which it ships. Phase 40.1 works from this table.

## Legend
- **Strategy:** `optimistic` = local state update before server response, `realtime` = Supabase channel pushes server state, `skeleton` = loading placeholder, `none` = no instant-UX treatment planned (either fast enough or low-value)
- **Phase:** `40` = this phase (demo-critical), `40.1` = post-Maggie rollout, `—` = not addressed

## Server Actions (3 files)
| # | Surface | File:Line | Strategy | Phase | Notes |
|---|---------|-----------|----------|-------|-------|
| 1 | createListAction | src/app/[orgId]/lists/actions.ts:38 | optimistic | 40 | Plan 05 |
| 2 | deleteListAction | src/app/[orgId]/lists/actions.ts:66 | optimistic | 40 | Plan 05, undo toast |
| 3 | updateMemberStatusAction | src/app/[orgId]/lists/actions.ts:84 | optimistic | 40.1 | high-freq, in-table dropdown |
| 4 | updateMemberNotesAction | src/app/[orgId]/lists/actions.ts:102 | optimistic | 40.1 | debounce + optimistic |
| 5 | removeFromListAction | src/app/[orgId]/lists/actions.ts:120 | optimistic | already done | list-member-table.tsx:142 |
| 6 | addToListAction | src/app/[orgId]/lists/actions.ts:139 | optimistic | 40.1 | dialog flow |
| 7 | createPersonaAction | (verify path) | optimistic | 40.1 | |
| 8 | updatePersonaAction | (verify path) | optimistic | 40.1 | |
| 9 | deletePersonaAction | (verify path) | optimistic | 40.1 | |
| 10 | createTenantAction | (from Task 1) | optimistic | 40.1 | admin-only |
| 11 | toggleTenantStatusAction | (from Task 1) | optimistic | 40.1 | admin-only |

## Route Handlers — Tenant App
| # | Surface | File | Strategy | Phase | Notes |
|---|---------|------|----------|-------|-------|
| 12 | POST/DELETE /api/prospects/[id]/tags | src/app/api/prospects/[prospectId]/tags/route.ts | optimistic | 40 | Plan 06 |
| 13 | PATCH /api/prospects/[id]/profile | src/app/api/prospects/[prospectId]/profile/route.ts | optimistic | 40 | Plan 06 inline edit |
| 14 | PATCH /api/prospects/[id]/notes | src/app/api/prospects/[prospectId]/notes/route.ts | optimistic | 40.1 | |
| 15 | POST /api/search/[searchId]/dismiss | src/app/api/search/[searchId]/dismiss/route.ts | optimistic | 40 | Plan 06 |
| 16 | POST /api/apollo/bulk-enrich | src/app/api/apollo/bulk-enrich/route.ts | skeleton | 40 | Plan 07 |
| 17 | POST /api/search/[searchId]/extend | src/app/api/search/[searchId]/extend/route.ts | skeleton | 40 | Plan 07 |
| 18 | POST /api/prospects/[id]/activity | src/app/api/prospects/[prospectId]/activity/route.ts | optimistic | 40.1 | |
| 19 | POST /api/prospects/[id]/signals/mark-seen | (verify path) | optimistic | 40.1 | |
| 20 | POST /api/research/pin | (verify path) | optimistic | 40.1 | |
| 21 | PATCH /api/research/sessions/[id] | (verify path) | optimistic | 40.1 | |
| 22 | POST /api/search/[searchId]/refresh | (verify path) | realtime | 40.1 | fires Inngest |
| 23 | POST /api/issue-reports | (verify path) | optimistic | 40.1 | fire-and-forget |
| 24 | Research message send (streaming) | src/components/prospect/research-panel.tsx | skeleton | 40 | Plan 07 verify-only |

## Route Handlers — Admin
| # | Surface | File | Strategy | Phase | Notes |
|---|---------|------|----------|-------|-------|
| 25 | (admin route from Task 1) | ... | optimistic | 40.1 | |
| 26 | (admin route from Task 1) | ... | optimistic | 40.1 | |
| 27 | (admin route from Task 1) | ... | optimistic | 40.1 | |
| 28 | (admin route from Task 1) | ... | optimistic | 40.1 | |

## Realtime Channels
| # | Channel | Table | Filter | Status | Phase | Notes |
|---|---------|-------|--------|--------|-------|-------|
| R1 | `list-re-enrich-${prospectId}` | prospects | `id=eq.${prospectId}` | exists | already done | list-member-table.tsx:186 |
| R2 | `prospect-${prospect.id}` | prospects | `id=eq.${prospect.id}` | exists | already done | profile-view.tsx:251 |
| R3 | `saved-search-enrichment-${tenantId}` | saved_search_prospects | `tenant_id=eq.${tenantId}` | new | 40 | Plan 02 |
| R4 | `prospects-enriched-${tenantId}` | prospects | `tenant_id=eq.${tenantId}` + client-side reconcile | new | 40 | Plan 03 |

## Phase 40 Scope Summary
- **11 surfaces** addressed this phase
- Breakdown: 2 realtime + 5 optimistic + 3 skeleton + 1 polling-fallback utility = 11
- Remaining ~17 surfaces deferred to Phase 40.1 (see phase column `40.1`)

## Phase 40.1 Checklist
Filter this table for `Phase = 40.1` — that is the checklist for the next phase. Do not replan those; the strategy column is already decided.
```

After writing, populate the "verify path" entries using Grep to find actual files. If a candidate does NOT exist on disk, mark it `NOT FOUND — recon false positive` in the Notes column rather than omitting. Total rows must be ≥ 28 confirmed surfaces.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md &amp;&amp; grep -c "^| " .planning/phases/40-instant-ux-pass-demo-critical-slice/40-AUDIT.md</automated>
  </verify>
  <done>`40-AUDIT.md` exists, contains at least 28 surface rows across the tables (server actions + route handlers + admin + realtime), and every row has strategy + phase columns populated. The 11 Phase-40 rows match CONTEXT specifics. No TODOs left in the Notes column — every unverified route is resolved.</done>
</task>

</tasks>

<verification>
- `40-AUDIT.md` exists and is > 120 lines
- Phase = 40 rows count to exactly 11
- Phase = 40.1 rows count to ≥ 17
- No surface has strategy = blank
- Every Phase-40 row appears in 40-CONTEXT.md `<specifics>` table (one-to-one)
</verification>

<success_criteria>
The developer (or Phase 40.1 planner) can open `40-AUDIT.md`, filter for `Phase = 40.1`, and have a complete ordered work queue without re-running recon.
</success_criteria>

<output>
After completion, create `.planning/phases/40-instant-ux-pass-demo-critical-slice/40-01-SUMMARY.md`.
</output>
