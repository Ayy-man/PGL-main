---
phase: 37-llm-prompt-overhaul-fix-hallucination-model-selection-weak-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/openrouter.ts
  - src/lib/enrichment/edgar.ts
autonomous: true
requirements:
  - D-01
  - D-09

must_haves:
  truths:
    - "All 10 LLM prompts use GPT-4o-mini as default model"
    - "LLM requests have 25s timeout instead of 15s"
    - "SEC resolver uses individual-company evaluation instead of industry-blanket rule"
  artifacts:
    - path: "src/lib/ai/openrouter.ts"
      provides: "Central LLM gateway with GPT-4o-mini default and 25s timeout"
      contains: "openai/gpt-4o-mini"
    - path: "src/lib/enrichment/edgar.ts"
      provides: "SEC resolver with improved heuristic"
      contains: "PRIVATE"
  key_links:
    - from: "src/lib/ai/openrouter.ts"
      to: "all 9 callers"
      via: "DEFAULT_MODEL constant"
      pattern: "openai/gpt-4o-mini"
---

<objective>
Swap default LLM model from Claude 3.5 Haiku to GPT-4o-mini across the entire platform and fix the SEC resolver heuristic.

Purpose: GPT-4o-mini provides better structured JSON output and instruction following for the same cost tier. The SEC resolver heuristic currently uses an industry-blanket rule that incorrectly classifies some public companies as private.
Output: Updated openrouter.ts gateway (model + timeout), updated edgar.ts resolver prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/llm-prompt-layers.md
@src/lib/ai/openrouter.ts
@src/lib/enrichment/edgar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap default model to GPT-4o-mini and increase timeout (D-01)</name>
  <files>src/lib/ai/openrouter.ts</files>
  <read_first>src/lib/ai/openrouter.ts</read_first>
  <action>
In `src/lib/ai/openrouter.ts`, make these exact changes:

1. Change line 9 (`DEFAULT_MODEL`) from:
   ```
   const DEFAULT_MODEL = "anthropic/claude-3.5-haiku";
   ```
   to:
   ```
   const DEFAULT_MODEL = "openai/gpt-4o-mini";
   ```

2. Change the JSDoc comment on line 1-6 to replace "anthropic/claude-3.5-haiku" with "openai/gpt-4o-mini" in the Model line.

3. Change the `@param model` JSDoc on line 37 to say `(default openai/gpt-4o-mini)`.

4. Change line 69 (`AbortSignal.timeout`) from:
   ```
   signal: AbortSignal.timeout(15_000),
   ```
   to:
   ```
   signal: AbortSignal.timeout(25_000),
   ```

5. Update the comment on line 36 from "Claude Haiku" to "GPT-4o-mini".

Per D-01: After this change, every LLM call defaults to GPT-4o-mini. The dossier in claude.ts already uses `DOSSIER_MODEL = "openai/gpt-4o-mini"` explicitly, so it is unaffected. No other caller passes an explicit model override.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/PGL-main && grep -c "openai/gpt-4o-mini" src/lib/ai/openrouter.ts && grep -c "25_000" src/lib/ai/openrouter.ts && grep -c "anthropic/claude-3.5-haiku" src/lib/ai/openrouter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "openai/gpt-4o-mini" src/lib/ai/openrouter.ts` returns the DEFAULT_MODEL line
    - `grep "25_000" src/lib/ai/openrouter.ts` returns 1 match (the timeout)
    - `grep "anthropic/claude-3.5-haiku" src/lib/ai/openrouter.ts` returns 0 matches
  </acceptance_criteria>
  <done>DEFAULT_MODEL is "openai/gpt-4o-mini", timeout is 25s, zero references to claude-3.5-haiku remain in openrouter.ts</done>
</task>

<task type="auto">
  <name>Task 2: Replace SEC resolver industry-blanket heuristic with individual-company evaluation (D-09)</name>
  <files>src/lib/enrichment/edgar.ts</files>
  <read_first>src/lib/enrichment/edgar.ts</read_first>
  <action>
In `src/lib/enrichment/edgar.ts`, find the SEC company resolver system prompt (the `chatCompletion()` call around line 188-201). The current system prompt has rule 5:

```
5. Startups, biotech companies without IPOs, regional firms, and most companies with fewer than 500 employees are almost always private — return "PRIVATE" for these.
```

Replace rule 5 with:

```
5. Evaluate each company individually. Do not apply blanket rules by industry — some biotech, fintech, and energy companies ARE publicly traded. Base your answer on whether you specifically know THIS company to be listed on a US exchange.
```

Keep all other rules (1-4, 6) exactly as they are. Keep the two-stage post-verification logic (ticker list match + live SEC API ping) unchanged.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/PGL-main && grep -c "Evaluate each company individually" src/lib/enrichment/edgar.ts && grep -c "Startups, biotech" src/lib/enrichment/edgar.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Evaluate each company individually" src/lib/enrichment/edgar.ts` returns 1 match
    - `grep "Startups, biotech" src/lib/enrichment/edgar.ts` returns 0 matches
    - The two-stage verification code after the chatCompletion call is unchanged (lines ~205-257)
  </acceptance_criteria>
  <done>SEC resolver rule 5 uses individual-company evaluation instead of industry-blanket heuristic. All other rules and post-verification unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenRouter API | LLM responses flow back into application logic |
| SEC EDGAR API | External data consumed by parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Spoofing | openrouter.ts | accept | Model swap is config change; auth via API key unchanged |
| T-37-02 | Tampering | edgar.ts resolver | mitigate | Two-stage post-verification (ticker list + live SEC API) remains intact after prompt change; LLM output never trusted without verification |
</threat_model>

<verification>
- `pnpm build` completes without errors
- `grep -r "anthropic/claude-3.5-haiku" src/lib/ai/openrouter.ts` returns 0 matches
- `grep "openai/gpt-4o-mini" src/lib/ai/openrouter.ts` returns DEFAULT_MODEL line
- `grep "25_000" src/lib/ai/openrouter.ts` returns timeout line
- `grep "Evaluate each company individually" src/lib/enrichment/edgar.ts` returns 1 match
</verification>

<success_criteria>
All LLM calls default to GPT-4o-mini with 25s timeout. SEC resolver uses individual-company evaluation. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/37-llm-prompt-overhaul-fix-hallucination-model-selection-weak-p/37-01-SUMMARY.md`
</output>
