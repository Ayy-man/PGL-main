---
phase: 29
plan: 05
type: execute
wave: 4
depends_on:
  - 29-04
files_modified:
  - src/app/[orgId]/search/components/persona-pills.tsx
autonomous: false
requirements:
  - LDU-15

must_haves:
  truths:
    - "pnpm build exits 0 with no new TypeScript errors"
    - "pnpm lint (or eslint) exits 0 or only reports pre-existing violations"
    - "The legacy persona-pills.tsx file is deleted since no consumer remains after Plan 04"
    - "User visually confirms both tabs render correctly on /[orgId]/search in a live browser session"
    - "User visually confirms Discover tab -> Saved Searches navigation flow works"
    - "User visually confirms sidebar collapse/expand and localStorage persistence across reload"
    - "User visually confirms Phase 28 behaviors (refresh, dismiss, undo, show dismissed toggle) work inside the new layout"
  artifacts:
    - path: "src/app/[orgId]/search/components/persona-pills.tsx"
      provides: "DELETED — no longer imported anywhere"
      contains: ""
  key_links: []
---

<objective>
Finalize Phase 29 with build verification, stale reference cleanup (delete `persona-pills.tsx`), and a human-verify checkpoint covering all decisions D-01 through D-23.

Purpose: Prevents dead code accumulation and confirms the refactor is behaviorally complete before marking the phase done.
Output: Deleted `persona-pills.tsx`; verified build; approved visual QA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-lead-discovery-ui-redesign-collapsible-search-rail-and-view-/29-CONTEXT.md
@.planning/phases/29-lead-discovery-ui-redesign-collapsible-search-rail-and-view-/29-UI-SPEC.md
@src/app/[orgId]/search/components/search-content.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete legacy persona-pills.tsx and verify build + lint</name>
  <files>src/app/[orgId]/search/components/persona-pills.tsx</files>
  <read_first>
    - src/app/[orgId]/search/components/search-content.tsx (confirm no residual import of PersonaPills)
    - src/app/[orgId]/search/components/persona-pills.tsx
  </read_first>
  <action>
**Step 1 — Verify zero remaining consumers:**

Run a codebase-wide grep to confirm nothing imports or references `PersonaPills` or `persona-pills`:

```bash
grep -rn "PersonaPills\\|persona-pills" "src/" || echo "NO REMAINING REFERENCES"
```

The only hit allowed is the file itself (`src/app/[orgId]/search/components/persona-pills.tsx`). Any other hit means Plan 04 left a dangling reference and must be fixed before proceeding.

**Step 2 — Delete the file:**

```bash
rm "src/app/[orgId]/search/components/persona-pills.tsx"
```

**Step 3 — Verify build + typecheck:**

```bash
pnpm build 2>&1 | tail -30
```

Build must exit 0. If there are new TypeScript errors, surface them and do NOT mark the task complete.

**Step 4 — Run lint (tolerate pre-existing violations):**

```bash
pnpm lint 2>&1 | tail -40 || true
```

Capture the lint output in the summary. New lint violations introduced by Phase 29 plans must be fixed; pre-existing warnings (e.g., unrelated raw-color violations in other files) can be noted but do not block.

**Step 5 — Verify the design system CSS variables used by new components are defined in globals.css:**

```bash
for token in "bg-card-gradient" "bg-elevated" "border-default" "border-gold" "border-hover" "border-subtle" "gold-bg" "gold-primary" "gold-text" "text-primary-ds" "text-secondary-ds" "text-tertiary" "text-ghost"; do
  if grep -q "\\-\\-${token}" "src/app/globals.css"; then
    echo "OK  --${token}"
  else
    echo "MISSING --${token}"
  fi
done
```

All tokens must print "OK". If any print "MISSING", the component must be updated to use the existing token name (do NOT add new CSS variables in this phase — the UI-SPEC says CSS variables are unchanged per D-21).
  </action>
  <verify>
    <automated>! test -f "src/app/[orgId]/search/components/persona-pills.tsx" && pnpm build 2>&1 | tail -5 | grep -q "Compiled successfully\\|Generating static pages\\|Route\\|Finalizing" && grep -rn "PersonaPills\\|persona-pills" src/ | grep -v "persona-pills.tsx.bak" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/[orgId]/search/components/persona-pills.tsx` does NOT exist (deleted)
    - `grep -rn "PersonaPills" src/` returns zero hits
    - `grep -rn "persona-pills" src/` returns zero hits (the import path is gone)
    - `pnpm build` exits 0
    - All required CSS variables (`--gold-bg`, `--border-gold`, `--text-primary-ds`, `--text-secondary-ds`, `--text-tertiary`, `--gold-primary`, `--bg-card-gradient`, `--bg-elevated`, `--border-default`, `--border-hover`, `--border-subtle`, `--text-ghost`) are present in `src/app/globals.css`
  </acceptance_criteria>
  <done>persona-pills.tsx deleted, build clean, no dangling references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA — all decisions D-01 through D-23</name>
  <files>src/app/[orgId]/search/components/search-content.tsx, src/app/[orgId]/search/components/discover-tab.tsx, src/app/[orgId]/search/components/saved-searches-tab.tsx, src/app/[orgId]/search/components/search-sidebar-rail.tsx, src/app/[orgId]/search/components/saved-search-view-header.tsx</files>
  <action>
Pause execution and present the `<what-built>` summary and the `<how-to-verify>` checklist below to the user. Do NOT proceed past this checkpoint until the user replies with `approved` (or describes issues to fix). Start the dev server with `pnpm dev` if the user requests it, and capture any console errors they report. This is a human-only verification step — there is no automation that can replace it.
  </action>
  <what-built>
The Lead Discovery page has been refactored from a single stacked layout to a two-tab experience:
- Tab 1 "Discover": centered hero h1 "Find high-net-worth prospects", NL search bar, collapsible filters, Search + Save as new search buttons, saved searches shortcut list.
- Tab 2 "Saved Searches": left collapsible sidebar rail (240px expanded / 48px collapsed with tooltips) + right results panel showing saved search name as h1, subtitle with inline refresh, bulk actions bar, results table. All Phase 28 behaviors preserved: dismiss, refresh, showDismissed toggle, NEW badge, undo.
  </what-built>
  <how-to-verify>
1. **Start the dev server** (if not already running):
   ```
   pnpm dev
   ```

2. **Navigate to** `http://localhost:3000/{your-org-slug}/search` (log in first if needed).

3. **Verify Tab 1 (Discover) — default on page load [D-01, D-02, D-04]:**
   - [ ] Two tab buttons visible at top: "Discover" and "Saved Searches". "Discover" is active (gold underline).
   - [ ] Centered hero with h1 "Find high-net-worth prospects" in serif font.
   - [ ] Subtitle "Use natural language to surface wealthy individuals matching your criteria".
   - [ ] NL search bar full-width below hero.
   - [ ] Collapsible "Filters" chevron below NL bar. Click to expand — Advanced Filters panel appears.
   - [ ] Two buttons: "Search" (gold border) and "Save as new search" (ghost).
   - [ ] Saved searches shortcut list below (if any exist): shows dot + name + count + chevron arrow.

4. **Verify Discover → Saved Searches navigation [D-07, D-08, D-09, D-19]:**
   - [ ] Type a keyword in the NL bar, press Enter → you are switched to the "Saved Searches" tab and results appear in the right panel.
   - [ ] Click "Save as new search" → PersonaFormDialog opens.
   - [ ] Click a shortcut list item on Discover tab → you are switched to Saved Searches tab with that search selected and highlighted in the sidebar.
   - [ ] Go back to Discover → results are NOT visible on Discover tab (confirmed D-09).

5. **Verify Tab 2 (Saved Searches) sidebar rail [D-10, D-11, D-12, D-13, D-14, D-15]:**
   - [ ] Sidebar is ~240px wide by default, lists all saved searches with colored dots + names.
   - [ ] Active search is highlighted with gold background + border.
   - [ ] Hovering non-active items shows gold-tinted hover state.
   - [ ] Click the chevron toggle at top of sidebar → rail collapses to ~48px showing only dots.
   - [ ] Hover over a dot in collapsed state → tooltip appears on the right with the full search name.
   - [ ] Click a dot in collapsed state → that search becomes selected; results load in right panel.
   - [ ] Click chevron again → rail expands back to 240px.
   - [ ] "+ New" button at bottom of sidebar → clicking opens PersonaFormDialog.
   - [ ] Reload the page (Cmd+R) → collapsed state persists (localStorage check).

6. **Verify Tab 2 view header [D-16, D-17]:**
   - [ ] When a saved search is selected, the h1 shows the saved search NAME (NOT "Lead Discovery").
   - [ ] Below h1: subtitle in format `{N} prospects · Last refreshed: {X} ago [↺]`.
   - [ ] The refresh icon button is inline in the subtitle (not a separate toolbar row).
   - [ ] Hovering the refresh button turns it gold.
   - [ ] Clicking refresh spins the icon and updates `{X} ago`.

7. **Verify Tab 2 empty states [D-18]:**
   - [ ] If no search is selected (fresh load on Saved Searches tab), the right panel shows "Select a saved search" empty state.
   - [ ] (Optional — skip if you have saved searches) If you had zero saved searches, the right panel would show "No saved searches yet".

8. **Verify Phase 28 behaviors still work inside the new layout:**
   - [ ] Select a saved search → results load.
   - [ ] "Show dismissed (N)" toggle works (top-right of view header area).
   - [ ] Single-row dismiss button on a prospect row still fires and shows undo toast.
   - [ ] Select multiple prospects → BulkActionsBar appears with Add to List, Export CSV, Enrich Selection, Dismiss Selected buttons.
   - [ ] "Dismiss Selected" shows the confirmation Dialog; confirming dismisses.
   - [ ] NEW badge is still visible on newly surfaced prospects.
   - [ ] Click a prospect row → ProspectSlideOver opens.

9. **Verify browser console is clean:**
   - [ ] Open DevTools console → no red errors related to the new components.

10. **Responsive spot check:**
    - [ ] Resize to mobile viewport (375px) → tab bar still accessible; layout does not produce horizontal overflow.

**Report issues by:**
- Listing which checkbox failed
- Including a screenshot if visual
- Pasting console errors if any

**Approve by typing:** `approved`
  </how-to-verify>
  <verify>
    <automated>echo "Human verification required — no automated check. Checkpoint resolved when user types 'approved'."</automated>
  </verify>
  <done>User types `approved` after running through every checklist item; any reported issues have been fixed and re-verified.</done>
  <resume-signal>Type "approved" to finish Phase 29, or describe what's broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Deletion + verification — no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-16 | Information Disclosure | Dead code retention | mitigate | Deleting persona-pills.tsx removes unused deterministic color hashing logic; same logic now lives in the shared `lib/persona-color.ts` consumed by the new components. No behavior change, just dead code removal. |
| T-29-17 | Tampering | Human verification blind spots | mitigate | The checkpoint has explicit decision-by-decision checkboxes (D-01 through D-23) to force the verifier to eyeball every contract. |
</threat_model>

<verification>
- persona-pills.tsx deleted
- `pnpm build` exits 0
- Human checkpoint approved with all Phase 29 decisions visually confirmed
</verification>

<success_criteria>
- Dead code removed
- Build clean
- User signs off on all D-01 through D-23 visual behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/29-lead-discovery-ui-redesign-collapsible-search-rail-and-view-/29-05-SUMMARY.md`
</output>
