---
phase: 260414-u9w-issue-report-ticket-lifecycle-close-reop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/reports/[id]/route.ts
  - src/app/api/admin/reports/route.ts
  - src/app/admin/reports/[id]/report-detail.tsx
  - src/app/admin/reports/[id]/timeline-card.tsx
  - src/app/admin/reports/page.tsx
  - src/app/admin/reports/reports-table.tsx
autonomous: false
requirements:
  - TICKET-CLOSE-BTN
  - TICKET-REOPEN-BTN
  - TICKET-TIMELINE-LABELS
  - TICKET-OVERDUE-CLOSED
  - TICKET-COPY-TIMELINE

must_haves:
  truths:
    - "Admin sees a prominent 'Close ticket' button on open/investigating tickets that pre-fills status=resolved and requires a resolution note before submit"
    - "Admin sees a 'Reopen ticket' button on resolved/wontfix/duplicate tickets that sends PATCH status=open and clears resolved_at/resolved_by"
    - "Timeline renders close transitions (open/investigating → resolved/wontfix/duplicate) with a distinct visual label (e.g. 'Closed as resolved' with green dot) vs ordinary status changes"
    - "Timeline renders reopen transitions (resolved/wontfix/duplicate → open/investigating) with a distinct 'Reopened' label and red dot"
    - "Admin reports list page shows an 'Overdue Closed' section grouping tickets where status ∈ {resolved,wontfix,duplicate} AND resolved_at < now() - 30 days, visually de-emphasized for skim"
    - "Copy full report output includes a '## Timeline' section with chronological `[time] actor event (from→to or note)` lines, plus resolved_by/resolved_at metadata in the Admin section"
    - "Server PATCH writes status_changed events for both close and reopen directions (no schema change — reuses status_changed with from/to)"
    - "Server PATCH clears resolved_at and resolved_by when status transitions from a closed status to a non-closed status (reopen)"
  artifacts:
    - path: "src/app/api/admin/reports/[id]/route.ts"
      provides: "PATCH handler that handles reopen (clears resolved_at/resolved_by) and does not block reopen via the close-requires-note guard"
      contains: "resolved_at"
    - path: "src/app/api/admin/reports/route.ts"
      provides: "GET handler supporting ?filter=overdue_closed which queries closed statuses with resolved_at < now - 30d"
      contains: "overdue_closed"
    - path: "src/app/admin/reports/[id]/report-detail.tsx"
      provides: "Close ticket button, Reopen button, Copy full report Timeline section, resolved-by metadata in export"
      contains: "handleClose"
    - path: "src/app/admin/reports/[id]/timeline-card.tsx"
      provides: "Distinct labels/colors for close vs reopen (reusing status_changed event type)"
      contains: "Closed as"
    - path: "src/app/admin/reports/page.tsx"
      provides: "Overdue Closed section above/below main reports list"
      contains: "Overdue Closed"
    - path: "src/app/admin/reports/reports-table.tsx"
      provides: "Visual treatment for overdue closed rows (dimmed, resolved_at column)"
      contains: "resolved_at"
  key_links:
    - from: "src/app/admin/reports/[id]/report-detail.tsx"
      to: "/api/admin/reports/[id]"
      via: "fetch PATCH with {status, admin_notes} — reopen uses {status:'open', admin_notes:'Reopened: <reason>'}"
      pattern: "fetch.*api/admin/reports"
    - from: "src/app/admin/reports/page.tsx"
      to: "/api/admin/reports?filter=overdue_closed"
      via: "Supabase admin client query with status.in.(resolved,wontfix,duplicate) and resolved_at lt threshold"
      pattern: "overdue_closed|resolved_at"
    - from: "src/app/admin/reports/[id]/timeline-card.tsx"
      to: "IssueReportEventWithActor.status_changed rows"
      via: "renderVariant switch branches on from/to being in CLOSED_STATUSES to pick dot color + headline"
      pattern: "CLOSED_STATUSES|from_status"
    - from: "src/app/admin/reports/[id]/report-detail.tsx buildFullLog"
      to: "events state"
      via: "map events array to chronological `[time] actor event` lines in ## Timeline section"
      pattern: "## Timeline"
---

<objective>
Ship the ticket lifecycle follow-ups to Phase 38: an explicit Close ticket button, a Reopen button (with server-side clear of resolved_at/resolved_by), distinct timeline rendering for close vs reopen, an Overdue Closed section on the admin reports list, and a Timeline section in the Copy full report output.

Purpose: Make triage flows explicit instead of relying on the status dropdown + free-text note. Make closed-but-stale tickets visible for cleanup. Make exports a complete audit log.

Output:
- PATCH handler that correctly handles reopen (clears resolution metadata, writes status_changed event in both directions)
- GET list endpoint supporting ?filter=overdue_closed
- Report detail page with Close ticket / Reopen buttons in the Admin Actions card
- Timeline card labeling close vs reopen distinctly (no new event_type — reuse status_changed with from/to)
- Admin reports list page with an Overdue Closed section (dimmed treatment, skim-friendly)
- Copy full report including chronological ## Timeline section with resolved_by/resolved_at metadata
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/admin/reports/[id]/report-detail.tsx
@src/app/admin/reports/[id]/timeline-card.tsx
@src/app/api/admin/reports/[id]/route.ts
@src/app/api/admin/reports/route.ts
@src/app/admin/reports/page.tsx
@src/app/admin/reports/reports-table.tsx
@src/types/database.ts

<interfaces>
<!-- Extracted from codebase. Executor should use these directly. -->

From src/types/database.ts (lines 324-392):
```typescript
export type IssueStatus =
  | "open"
  | "investigating"
  | "resolved"
  | "wontfix"
  | "duplicate";

export type IssueReportEventType =
  | "reported"
  | "status_changed"
  | "note_added"
  | "viewed_by_admin"
  | "screenshot_expired";

// DO NOT add a new event type. CHECK constraint on issue_report_events.event_type
// would require a migration. Reuse "status_changed" with from_status/to_status
// and let the UI label it "Closed as resolved" / "Reopened" based on direction.

export interface IssueReportEvent {
  id: string;
  report_id: string;
  event_type: IssueReportEventType;
  actor_user_id: string | null;
  actor_role: IssueReportActorRole;
  from_status: IssueStatus | null;
  to_status: IssueStatus | null;
  note: string | null;
  created_at: string;
}

export interface IssueReportEventWithActor extends IssueReportEvent {
  actor: { id: string; email: string; full_name: string | null } | null;
}
```

From src/app/admin/reports/[id]/report-detail.tsx (current shape):
```typescript
const CLOSED_STATUSES: IssueStatus[] = ["resolved", "wontfix", "duplicate"];
// state: report, status, notes, error, success, isPending, events, setEvents
// handleSave PATCHes /api/admin/reports/{id} with { status, admin_notes }
// On success: setReport(merge), setEvents(append data.event), setSuccess(...)
```

Server PATCH already:
- Writes status_changed event with from_status=current.status, to_status=body.status
- Sets resolved_by/resolved_at when transitioning INTO resolved
- Blocks close (target ∈ closed statuses) when notes empty
- Does NOT clear resolved_by/resolved_at on reopen (BUG to fix)
- Does NOT set resolved_by/resolved_at for wontfix/duplicate (only resolved — leave that, but DO clear on reopen regardless of which closed status it was)

From src/app/admin/reports/[id]/timeline-card.tsx renderVariant:
```typescript
case "status_changed": {
  const from = event.from_status ? STATUS_LABEL[event.from_status] : "—";
  const to = event.to_status ? STATUS_LABEL[event.to_status] : "—";
  // Current: blue dot, "changed status: {from} → {to}"
  // Needs: distinct styling when to ∈ CLOSED, and when from ∈ CLOSED && to ∉ CLOSED
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — reopen clears resolution metadata + overdue_closed list filter</name>
  <files>src/app/api/admin/reports/[id]/route.ts, src/app/api/admin/reports/route.ts</files>
  <action>
Two server edits — keep both changes minimal and localized.

**Edit A — src/app/api/admin/reports/[id]/route.ts (PATCH handler, around lines 75-205):**

1. Define `CLOSED_STATUSES` constant at module scope if not already shared (it already exists at line 75).
2. In the update-payload construction block (around lines 131-145), add a reopen branch: when `body.status !== undefined` AND `CLOSED_STATUSES.includes(current.status)` AND `!CLOSED_STATUSES.includes(body.status)`, set `update.resolved_by = null` and `update.resolved_at = null`. Apply this BEFORE the existing "resolved" branch (they are mutually exclusive — transitioning to resolved is always from a non-closed status by definition, but guard anyway).
3. Fix the close-guard to NOT block reopen: the guard at lines 115-123 triggers when `CLOSED_STATUSES.includes(nextStatus)` — on reopen, `nextStatus` is open/investigating so the guard already skips. But verify: reopen with empty note body and closed current state must succeed. Add a test case by inspection: `PATCH {status:'open'}` with empty `admin_notes` on a resolved ticket → must update row, write status_changed event, clear resolved_at/resolved_by.
4. DO NOT add a new event_type. The existing status_changed event insert block (around lines 170-185) already captures from_status/to_status and writes on any status change — reopen will naturally write a status_changed event with from=resolved/wontfix/duplicate and to=open/investigating. The client distinguishes via from/to values.
5. The existing response shape `{ report: data, event: newEvent }` must include `resolved_by` and `resolved_at` in the `report` select. The current select at line 154 already returns `"id, status, admin_notes, resolved_by, resolved_at, updated_at"` — good, no change needed.

**Edit B — src/app/api/admin/reports/route.ts (GET handler):**

1. Add a new query param `filter` that accepts `"overdue_closed"` (plus the existing status/category/tenant params).
2. When `filter === "overdue_closed"`:
   - Compute threshold: `new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString()`
   - Apply `query.in("status", ["resolved", "wontfix", "duplicate"])` AND `query.lt("resolved_at", threshold)`
   - Order by `resolved_at` ascending (oldest overdue first) instead of `created_at` desc
   - Explicit status/category params still apply on top (allow `?filter=overdue_closed&category=bug`)
3. Ensure `resolved_at` is in the SELECT list — it already is (line 37). Good.
4. Preserve existing pagination (limit/offset).

Per the CONTEXT constraint: no schema changes, no new event_type. Reuse status_changed + client-side labeling.
  </action>
  <verify>
    <automated>
# Type-check both files compile cleanly
npx tsc --noEmit 2>&1 | grep -E "(src/app/api/admin/reports)" || echo "no ts errors in target files"

# Manual smoke test via curl (requires running dev server + super_admin session cookie):
# 1. PATCH reopen: curl -X PATCH http://localhost:3000/api/admin/reports/{resolved-id} \
#      -H "Content-Type: application/json" \
#      -d '{"status":"open"}' --cookie "..."
#    Response must show resolved_at:null, resolved_by:null, and event with from_status in CLOSED_STATUSES.
# 2. GET overdue_closed: curl 'http://localhost:3000/api/admin/reports?filter=overdue_closed' --cookie "..."
#    Must return only reports with status ∈ closed AND resolved_at older than 30 days.
    </automated>
  </verify>
  <done>
- PATCH on a resolved ticket with body `{status:"open"}` returns 200, clears resolved_at + resolved_by, writes status_changed event (from=resolved, to=open).
- PATCH close-guard still blocks `{status:"resolved"}` with empty notes.
- GET `/api/admin/reports?filter=overdue_closed` returns only closed-status rows with resolved_at < now-30d, sorted by resolved_at ASC.
- `npx tsc --noEmit` produces no errors in the two modified files.
- No DB migrations. No new event_type values. No changes to issue_report_events schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Report detail UI — Close/Reopen buttons, distinct timeline labels, Copy-full-report Timeline section</name>
  <files>src/app/admin/reports/[id]/report-detail.tsx, src/app/admin/reports/[id]/timeline-card.tsx</files>
  <action>
Three client edits — bundle because they all live in the detail page.

**Edit A — report-detail.tsx: Close ticket button (Admin Actions card, after the Save changes button ~line 669):**

1. Add state: `const [closeDialogOpen, setCloseDialogOpen] = useState(false)` and `const [closeNote, setCloseNote] = useState("")`.
2. Show the Close button ONLY when `!CLOSED_STATUSES.includes(report.status)` (i.e. ticket is open/investigating).
3. Button label: "Close ticket" — use gold-outlined button style consistent with Save changes. Place it as a secondary action below Save.
4. Clicking opens an inline dialog/panel (not a modal — keep it in-card to avoid portal complexity): reveals a small form with:
   - A resolution dropdown pre-set to `resolved` (options: resolved / wontfix / duplicate — reuse STATUS_OPTIONS filtered to CLOSED_STATUSES)
   - A required textarea labeled "Resolution note" bound to `closeNote`
   - "Confirm close" button (disabled if closeNote.trim() empty) and "Cancel"
5. Confirm calls a new `handleClose` handler that PATCHes `{status: <chosen closed status>, admin_notes: closeNote}`. On success: updates report, appends event, clears dialog, sets success message. This is a SINGLE roundtrip (server writes status_changed event and sets resolved_at/resolved_by).
6. Also sync the top status select to the new closed status so the UI state is consistent.

**Edit B — report-detail.tsx: Reopen ticket button:**

1. Show a "Reopen ticket" button ONLY when `CLOSED_STATUSES.includes(report.status)`. Replace or sit alongside Save changes (it does not make sense to edit-save a closed ticket without reopening).
2. Optional reason prompt — use browser `window.prompt("Reason for reopening (optional)")` OR a small inline note field. Keep it simple: prompt is acceptable per constraints ("optional 'reopened' note").
3. Handler PATCHes `{status: "open", admin_notes: reason ? \`Reopened: \${reason}\` : report.admin_notes}`. Note: if user cancels the prompt (null return) do nothing. If empty string, send status only. Server will clear resolved_at/resolved_by.
4. On success: update state. Do NOT clear admin_notes locally unless server returned it cleared; merge from response.

**Edit C — report-detail.tsx buildFullLog (~line 287-328): add Timeline section:**

1. Build a `timelineLines` array from the `events` state (chronological ASC — events arrive ASC from the API, so no reversal needed here):
   ```
   ## Timeline
   - [2026-04-14T10:00:00Z] Tenant reported (bug): "Description snippet..."
   - [2026-04-14T10:15:00Z] Admin Jane viewed
   - [2026-04-14T11:00:00Z] Admin Jane changed status: Open → Investigating — "looking into it"
   - [2026-04-14T12:00:00Z] Admin Jane closed as Resolved — "fixed in deploy xyz"
   - [2026-04-14T15:00:00Z] Admin John reopened (Resolved → Open) — "regression reported"
   ```
2. Format each line: `[${event.created_at}] ${actorLabel} ${eventLabel}${note ? ` — "${note}"` : ""}`
   - actorLabel: actor.full_name ?? actor.email ?? actor_role fallback (e.g. "System" for screenshot_expired)
   - eventLabel by event_type:
     - reported → "reported"
     - status_changed → when to_status ∈ CLOSED: `closed as ${STATUS_LABEL[to_status]} (${from_status}→${to_status})`; when from_status ∈ CLOSED && to_status ∉ CLOSED: `reopened (${from_status}→${to_status})`; else: `changed status (${from_status}→${to_status})`
     - note_added → "added note"
     - viewed_by_admin → "viewed"
     - screenshot_expired → "screenshot expired"
3. Insert timelineLines into `buildFullLog` BEFORE the existing `buildDebugPrompt()` append, as a `## Timeline` section. Also ensure the existing Admin block already includes `- **Resolved at:** ...` and `- **Resolved by:** ...` (they already exist in lines 323-324 conditionally — leave as is).

**Edit D — timeline-card.tsx renderVariant status_changed case (~line 79-101): distinct close vs reopen rendering:**

1. Add a local constant at the top of the file: `const CLOSED_STATUSES: IssueStatus[] = ["resolved", "wontfix", "duplicate"];`
2. In the `status_changed` branch, before returning the default variant, check:
   - `const isClose = event.to_status && CLOSED_STATUSES.includes(event.to_status);`
   - `const isReopen = event.from_status && CLOSED_STATUSES.includes(event.from_status) && event.to_status && !CLOSED_STATUSES.includes(event.to_status);`
3. If `isClose`: return variant with `dotColor: "var(--success, #22c55e)"` and headline `<><span className="font-semibold">{actorName(event.actor)}</span> closed as {STATUS_LABEL[event.to_status]}</>`. Preserve the note body.
4. If `isReopen`: return variant with `dotColor: "var(--destructive, #ef4444)"` and headline `<><span className="font-semibold">{actorName(event.actor)}</span> reopened ticket</>`. Note body preserved.
5. Else: fall through to existing generic "changed status: {from} → {to}" with blue dot.

Do NOT change the `IssueReportEventType` union. Do NOT touch the DB.

Accessibility: new buttons need `cursor-pointer` (via Tailwind or inline) and readable disabled states (already covered by `disabled:opacity-60 disabled:cursor-not-allowed` pattern used on Save).
  </action>
  <verify>
    <automated>
# Lint + type-check the two files
npx tsc --noEmit 2>&1 | grep -E "(report-detail|timeline-card)" || echo "no ts errors in target files"
npx next lint --file src/app/admin/reports/\[id\]/report-detail.tsx --file src/app/admin/reports/\[id\]/timeline-card.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <done>
- On an open/investigating ticket detail page: "Close ticket" button is visible, expands to a form with status pre-set to resolved + required note, calls PATCH on confirm, updates UI + timeline in place.
- On a resolved/wontfix/duplicate ticket detail page: "Reopen ticket" button is visible, PATCHes status=open (optional reason prepended to note), server clears resolved_at/resolved_by, timeline gets a "reopened" entry.
- Timeline card renders close events with green dot + "closed as X" label and reopen events with red dot + "reopened ticket" label; other status changes keep the blue "changed status" label.
- Copy full report output contains a `## Timeline` section listing all events chronologically with actor, event label, and note.
- No new event_type values. No DB migrations. No schema changes.
- `npx tsc --noEmit` clean on the modified files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin reports list — Overdue Closed section</name>
  <files>src/app/admin/reports/page.tsx, src/app/admin/reports/reports-table.tsx</files>
  <action>
Add a dedicated "Overdue Closed" section above the main reports table on `/admin/reports`.

**Edit A — page.tsx (server component):**

1. In parallel with the existing reports query, run a second supabase query for overdue-closed:
   ```typescript
   const thirtyDaysAgoIso = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString();
   const { data: overdueClosed } = await admin
     .from("issue_reports")
     .select(`
       id, category, description, status, target_type, target_id, target_snapshot,
       screenshot_path, page_path, created_at, resolved_at,
       tenants:tenant_id ( id, name, slug ),
       users:user_id ( id, email, full_name )
     `)
     .in("status", ["resolved", "wontfix", "duplicate"])
     .lt("resolved_at", thirtyDaysAgoIso)
     .order("resolved_at", { ascending: true })
     .limit(25);
   ```
2. Use `Promise.all([mainQuery, overdueQuery])` to keep the page load parallel.
3. Pass `overdueClosed` to a new component/section. Render the section ONLY if `overdueClosed?.length > 0` — no need to show an empty state that clutters the page.

**Edit B — page.tsx layout:**

Structure:
```
<h1>Issue Reports</h1>
<p>subtitle</p>

{overdueClosed.length > 0 && (
  <section className="surface-admin-card rounded-[14px] p-5 space-y-3 opacity-90">
    <div className="flex items-center justify-between">
      <h2 className="text-[11px] font-semibold uppercase tracking-wider" style={{color:"var(--admin-text-secondary)"}}>
        Overdue Closed ({overdueClosed.length})
      </h2>
      <span className="text-xs text-muted-foreground">
        Closed over 30 days ago — review for archival
      </span>
    </div>
    <OverdueClosedList reports={overdueClosed} />
  </section>
)}

<ReportsTable ... /> // existing
```

**Edit C — reports-table.tsx: export a new lightweight `OverdueClosedList` component (same file to avoid a new file):**

1. Reuses the row shape from ReportsTable but accepts `resolved_at` (extend the local ReportRow interface with `resolved_at?: string | null`).
2. Render as a compact table (or a card list on mobile) with columns: Tenant, Category, Closed ago (from resolved_at), Status badge, View link.
3. Use dimmed visual treatment: parent section has `opacity-90`, rows use `text-muted-foreground` for secondary columns, status badge remains full color so admins can still see if it's resolved vs wontfix vs duplicate.
4. "Closed ago" uses the existing `formatRelativeTime` helper applied to `resolved_at` (e.g. "45d ago", "2 months ago"). Safe-guard null resolved_at (shouldn't happen given the query filter, but return "—" if null).
5. Each row links to `/admin/reports/{id}` via existing pattern.

Accessibility: section uses proper heading order (h2), the table has thead + th elements, links have descriptive text ("View report" not just an icon).

Do NOT load this on the client via the existing `/api/admin/reports` endpoint — the page is a server component and we already have the data at SSR time. (Task 1's `?filter=overdue_closed` endpoint exists for potential future client-side refresh, but is not required for this initial render.)

Per CLAUDE.md: use Lucide icons (not emojis), use CSS var tokens (`var(--admin-text-secondary)`, `var(--border-subtle)`), match the existing `surface-admin-card` styling.
  </action>
  <verify>
    <automated>
# Type-check
npx tsc --noEmit 2>&1 | grep -E "(admin/reports/page|reports-table)" || echo "no ts errors"

# Lint the touched files
npx next lint --file src/app/admin/reports/page.tsx --file src/app/admin/reports/reports-table.tsx 2>&1 | tail -20

# Manual: visit /admin/reports as super_admin. If there are any resolved/wontfix/duplicate
# reports older than 30 days, an "Overdue Closed" section renders above the main table.
# If none, the section is hidden.
    </automated>
  </verify>
  <done>
- `/admin/reports` renders an "Overdue Closed" section above the main table when at least one ticket has status ∈ {resolved,wontfix,duplicate} AND resolved_at < now-30d.
- The section displays a compact table of overdue-closed tickets with tenant, category, "closed Xd ago", status badge, and a View link.
- Visual treatment is clearly de-emphasized (opacity or muted text) so it reads as a review/archive queue rather than active work.
- The section is hidden when there are no overdue-closed tickets.
- No new API route required — server component queries Supabase directly.
- `npx tsc --noEmit` clean. No emoji icons, no hardcoded color hex outside CSS vars.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify end-to-end ticket lifecycle</name>
  <what-built>
    Close ticket button, Reopen button, reopen server logic, distinct close/reopen timeline labels, Overdue Closed section, Copy full report Timeline export.
  </what-built>
  <how-to-verify>
    As super_admin on http://localhost:3000:

    **Close flow:**
    1. Navigate to `/admin/reports`, click into an open ticket
    2. Click "Close ticket" in the Admin Actions card
    3. Verify the inline form appears with status pre-set to resolved and a required note field
    4. Try submitting with empty note → button disabled
    5. Enter a resolution note, confirm → ticket status flips to resolved, timeline shows a new entry with green dot + "closed as Resolved"
    6. Verify `resolved_at` and `resolved_by` appear in the Context collapsible

    **Reopen flow:**
    7. On the now-resolved ticket, click "Reopen ticket"
    8. In the prompt, type a reason (or leave blank and OK)
    9. Verify status flips to open, `resolved_at` and `resolved_by` disappear from the Context panel, timeline shows a new entry with red dot + "reopened ticket"

    **Copy full report:**
    10. Click "Copy full report" at the top of the detail page
    11. Paste into any editor → verify a `## Timeline` section appears with chronological lines including both the close and reopen events, plus the reporter's initial event and any admin views.
    12. Verify resolved_by/resolved_at metadata appears in the Admin section when the ticket is closed.

    **Overdue Closed (optional — requires seed data):**
    13. If you have any ticket with resolved_at > 30 days old, visit `/admin/reports` — an "Overdue Closed" section appears above the main table, dimmed, listing those tickets. If no such data exists, section should be hidden (not empty state).

    **Server sanity:**
    14. In Supabase SQL editor: `SELECT event_type, from_status, to_status, note, created_at FROM issue_report_events WHERE report_id = '<the-ticket-id>' ORDER BY created_at;` — verify both close and reopen are captured as `status_changed` rows with correct from/to, no new event_type rows.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 checks pass, or describe any issue (e.g. "reopen didn't clear resolved_at", "timeline label wrong for wontfix", "copy-full-report missing Timeline section").</resume-signal>
</task>

</tasks>

<verification>
Full phase verification:

1. **No schema changes:** `git diff` shows no migrations under `supabase/migrations/` and no edits to the `issue_report_events` table definition.
2. **Types preserved:** `src/types/database.ts` `IssueReportEventType` union is unchanged (still 5 values).
3. **PATCH reopen clears metadata:** PATCHing a closed ticket with `{status:"open"}` returns `resolved_at:null, resolved_by:null` in the response.
4. **PATCH close still guards:** PATCHing `{status:"resolved"}` on an open ticket with empty admin_notes returns 400.
5. **Overdue filter matches spec:** API query (or direct DB query) for status ∈ closed AND resolved_at < now-30d returns only qualifying rows.
6. **Timeline rendering:** Inspect DOM on a detail page with both close and reopen events — distinct dot colors (green for close, red for reopen, blue for other status changes) and distinct headline text.
7. **Copy full report audit completeness:** Exported markdown contains `## Timeline` section with one line per event, in chronological order, actor names present.
8. **Build passes:** `npm run build` exits 0.
</verification>

<success_criteria>
- [ ] Close ticket button on open/investigating tickets with required resolution note
- [ ] Reopen button on closed tickets; server clears resolved_at + resolved_by
- [ ] Timeline distinguishes close (green, "closed as X") and reopen (red, "reopened ticket") without new event types
- [ ] Admin reports list shows Overdue Closed section when relevant (status ∈ closed AND resolved_at > 30 days)
- [ ] Copy full report includes chronological ## Timeline section + resolved_by/resolved_at metadata
- [ ] No DB migrations or schema changes
- [ ] `npx tsc --noEmit` and `npm run build` pass clean
</success_criteria>

<output>
After completion, create `.planning/quick/260414-u9w-issue-report-ticket-lifecycle-close-reop/260414-u9w-01-SUMMARY.md` capturing:
- Files modified and net LOC delta
- Close/Reopen UX decisions (inline dialog vs prompt) and why
- Any deviations from the plan (e.g. if reopen needed a proper modal instead of prompt)
- Manual test results from Task 4 checklist
</output>
