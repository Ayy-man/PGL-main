/**
 * Claude AI prospect summary generation (stub).
 * Requires ANTHROPIC_API_KEY to be configured.
 */
export async function generateProspectSummary(
  _prospectData: Record<string, unknown>
): Promise<string> {
  // TODO: Implement when Anthropic API key is configured
  return "AI summary generation not yet configured.";
}
