// Re-export from canonical UI primitive — this local copy was orphaned (no consumers).
// The canonical version uses var(--gold-*) CSS tokens (theme-aware).
// DO NOT add hex literals here — use src/components/ui/wealth-tier-badge.tsx instead.
export { WealthTierBadge } from "@/components/ui/wealth-tier-badge";
